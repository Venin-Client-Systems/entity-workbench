# Implementation status

This is an initial integrated development implementation. None of the three milestones is declared complete. The supported-release target remains Windows 11 x64, macOS Apple Silicon and macOS Intel.

| Area | Implemented and verified | Remaining work |
|---|---|---|
| Product design | Native editable Figma frames for industrial transactions, statement mapping, findings, collection receipts/exceptions, document-job/extraction review, image OCR, transaction patterns, period comparisons and selected-page PDF OCR; graphite/amber palette, joined instruments, square controls, bundled Inter/JetBrains Mono; desktop/compact exported comparisons, axe and keyboard checks | Remaining workflow frames, long-content refinement, icon review, manual accessibility and final visual sign-off |
| Workspace | Rust-owned SQLite, revision history, content-addressed originals, strict commands, recoverable backup/restore, transactional v1–v4→v5 upgrades with consistent pre-migration backups and failure injection, finding review invalidation for pre-v3 records and original/OCR/report recovery, newer-schema refusal | Further migrations and interrupted-upgrade coverage; full Windows ACL implementation; workspace management UI |
| Evidence | Retained originals, UTF-8 text/CSV/TSV import, hash deduplication, source rows, escaped review; confined Mac development extraction of TXT/PDF/DOCX into bounded immutable unreviewed derivatives; designed derivative inspector; native bundled PDF import/parse/restart check; canonical PNG/JPEG OCR derivatives with designed unreviewed recognition review; durable selected-page PDF render/OCR jobs with native restart verification; opt-in retained image word regions with verified raster/TSV/result chains and native raster/word inspection | PDF page/cell regions; reviewed word anchors; complex tables; remaining Office/image/email formats; derivative acceptance and installed release-runtime validation |
| Document jobs | Rust-owned durable queue, reserved attempt identities, bounded manual retry, cancellation, joined desktop shutdown with one-shot ownership unlock, exclusive coordinator ownership, atomic derivative publication, orphan quarantine, backup/restore, shared document/image/PDF/retained-region queue and designed queue/cancel/retry/derivative controls | Collection/analysis integration, pagination/recipes, supervisor-crash process recovery, in-flight Windows canonical cancellation and installed-runtime verification |
| Transactions | Exact decimals, separate currencies, review decisions, reversible corrections, probable duplicate examples, explicit transfer pairs, source-linked totals, reusable CSV/TSV mappings, full-row validation preview and mapping provenance; revision-bound description totals, cash/refund candidates and explained recurring debits plus exact two-period comparison with full source drillthrough; bounded revision-bound transaction-page and literal-search backends; whole-ledger account/currency facets; bounded versioned source reads for analytical drillthrough; source-order balance decorations, broad transfer counterpart pages and complete filtered JSON export backends | XLSX/PDF mapping and extraction; reviewed merchant identity; account-flow graphs; paginated analytical snapshots |
| Identity | General entity and observation authoring, namespaces/leading zeros, validated source anchors and original-integrity-checked excerpts, reviewed-value comparison, keep-separate/defer decisions, merges/reversals and correction history | Calibrated Splink linkage, grouped identity views, bulk review and advanced comparison tools |
| Local discovery | Selected-host direct HTTPS collection, robots rules, DNS pinning, redirect checks, request/hop/time bounds, complete-response originals, canonical receipts, designed request/retention review and local snapshot exports, distinct policy/transport/quota/no-content outcomes | Measured cold-corpus discovery; durable frontier/resume, cancellation, source-specific adapters, browser capture, full broad-web coverage |
| Corpus search | Real Lucene engine and Rust supervisor; Boolean, phrase, proximity, fuzzy and fielded engine tests | Cross-platform packaged runtime and isolated worker verification; large manifests and pagination |
| Geography | Local MapLibre coordinate view; geodesic/uncertainty domain rules | Address packs, branch review UI, historical candidate authoring, regional basemaps |
| Assessment | Question/hypothesis authoring, alternatives/gaps, multiple supporting/contradictory citations, explicit finding review and re-review, source inspection, immutable self-contained HTML snapshots with versioned transaction calculation/source links and complete review partitions, JSON transaction export; canonical immutable DOCX snapshots with frozen model/artifact storage, paged capture/save interface and typed uncertain-request recovery; metadata-only desktop report catalogue, verified explicit immutable HTML retrieval and bounded finding-history pages with native verification; revision-bound citation catalogue and exact selected metadata readers adopted in assessment forms | Actual native DOCX/Word checks and remaining report design, CSV/Parquet/graph exports; report assembly and exhibit controls |
| Java engines | Separate parser/search/PDF-render JVMs; hash-bound text/PDF/DOCX extraction; bounded selected-page scan rendering with crop/rotation/raster provenance; supervised Lucene jobs with request-specific access, acknowledgement validation, damaged-index recovery, cancellation and explicit cleanup; actual hostile Mac development probes; separate Windows Server AppContainer parser/search recipe campaign with real document/query and role-boundary results | Python/Chromium compatibility, signed helpers, Intel runtime probes, in-flight Windows canonical cancellation and remaining search/OCR routes, hard aggregate disk/RSS bounds and crash recovery |
| OCR adapter | Relocated app-local Tesseract 5.5.2 and pinned English model; canonical bounded PGM input; explicit blank/recognized states; exact raster binding; eight actual Mac OCR/hostile-runtime checks; bounded confined PNG/JPEG decoding, original-to-raster binding, immutable canonical image jobs/UI and backup/restore; separate selected-page scan PDF rendering/OCR with canonical jobs/UI; opt-in bounded raster word-region engine and canonical retention/restore | General PDF features, reviewed word anchors, extraction acceptance, additional languages and signed cross-platform distribution |
| Python engines | Development DuckDB adapter preserves exact decimal scale and Parquet drillthrough; NetworkX reviewed paths and spaCy phrase candidates; pinned Apple Silicon CPython and 58 locked wheels assembled offline with independently verified files/RECORDs; static arm64 load-reference review | Actual confined imports and native loading; relocation execution; canonical snapshot/protocol and transfer-selection integration; Spatial extension; calibrated Splink; app integration and confinement |
| Distribution | All three native targets compile in source CI; Apple Silicon development app with separate Java parser/Lucene/image-decoder/PDF-renderer and English OCR staging; versioned offline runtime inventory producer and validator with explicit component ownership and actual partial-staging rejection | Complete runtime trees and validation on all targets; Windows Fixed WebView2; offline clean-machine tests; signing/notarization |
| Performance | Actual 100,000-row canonical core baseline on the available 16 GiB M1; complete denominators and exact-money checks; separate payload diagnostic and explicit report retrieval reduce a 198 MB refresh to 84 MB while preserving saved bytes; paired transaction-page measurements with exact response checks and repaired redundant empty-result scan | Remaining large-corpus pagination and native interaction measurements, required 10,000 pages/1,000 scans and concurrent maps/graphs; qualified p95 and total runtime memory |

## Scope clarification

The implementation request was clarified to prohibit external search providers. Direct public website collection and application-owned local indexing are authorised. There is no provider-selection dependency or API-key setup. The broader open-web coverage requirement is still unproven; a small collected corpus does not satisfy it.

## Gate policy

The release checker fails while any required gate is unpassed. A successful source build, local engine test or development sandbox probe must not be relabelled as a complete approved release. Development `.app` builds are not signed/notarized product releases.

The [versioned acceptance ledger](release-evidence/README.md) now binds observations to exact source, installer, acceptance-policy and fixture-catalogue hashes. Required OS versions, retained negative outcomes, review receipts and freshness are checked before qualification. The repository still contains no release candidate or acceptance observations; all twelve gates remain unpassed. Ten small synthetic inputs establish initial regression expectations, not completed end-to-end scenarios.

## Delivery programme

The [23 September–23 December programme](delivery/THREE-MONTH-PLAN.md) tracks 41 required work items through six fortnightly sprints and a release buffer. [GitHub programme #4](https://github.com/Venin-Client-Systems/entity-workbench/issues/4) owns the milestones and dependency graph. The target assumes concurrent engineering capacity and early platform/access validation; it is not a promise to waive remaining release gates. EW-01 implements the inventory contract; packaging remains incomplete.

The owner authorized parallel agents on independent issues on 24 September. EW-02 supplies evidence-backed release checks. Separate branches supply EW-05's frozen discovery benchmark/offline scorer and EW-08's release-access readiness register. Their live collection, independent relevance review, confirmed access and support decisions remain outstanding. Changes are integrated through reviewable PRs and exact-revision CI; parallel implementation does not bypass issue dependencies or release review.

The owner confirmed that only the current development Mac and one human owner are available. Windows/Intel release-test environments and independent human review are delivery gaps. Local implementation and hosted source CI continue; clean-install acceptance, signing and the complete-release date remain conditional on the missing evidence and access.


## Durable PDF page OCR and native review — 24 September 2026

The canonical queue now accepts an explicitly selected PDF page and bounded integer DPI, using command v9, document-job v3 and a separate PDF-extraction v1 contract. A request key binds original, method, page and DPI; document/image/PDF work shares the two-worker limit. Separate renderer and OCR processes publish one validated immutable derivative with original/raster/result identities, exact geometry, worker identities and explicit incomplete/blocked outcomes. Text remains unreviewed and is not silently accepted or indexed. Cancellation, stale attempts, interrupted ownership and unknown process exit retain the existing fail-closed queue behavior. The [job contract](processing/PDF-JOBS.md) documents this scope.

Clean core integration `4608ece605c4fcddfc80051c92474386112c8005` passed all **nine native PDF job/publication tests** using the combined app staging on macOS 26.6.2 arm64. The exact source/runtime/fixture report SHA-256 is `1eb308c50c1f8ecfe1437a5b19d9c0ae5b20c9d3e605af3127f4f9e58bb98490`. Publication, retry/attempt identity, invalid returned results, originals-aware backup/restore and separate worker outcomes are exercised. Four portable failure-evidence cases run in source CI.

The integrated UI at `07ee9cf` passes **148 ordinary Rust tests**, strict Clippy and the production/native development build; 18 native/runtime cases are explicitly excluded from the ordinary suite. The real-core browser suite passed **50/50** after correcting a collection-export test that focused Close before the real workspace refresh re-enabled it. The original combined run passed 49/50 and is retained locally. The repaired test deliberately delays that real refresh, checks disabled then enabled state, and verifies focus wrapping without fixed sleeps or fabricated responses. All eight PDF workflows passed in both combined runs. Five new PDF accessibility observations have zero axe violations; editable Figma frames, desktop/compact comparisons and checksummed images are in [the design handoff](design/PDF-PAGE-OCR.md).

The freshly built native app imported the 16,314-byte synthetic `scan.pdf` through its file chooser, queued page 1 of 2 at 144 DPI, and visibly moved from Running to Completed with one derivative. Review showed the expected synthetic text, a 1200×230 raster, CropBox `[0, 0, 600, 115]`, zero rotation and actual affine `[2, 0, 0, -2, 0, 230]`. A normal quit and restart preserved the same job, attempt, extraction, text and all three hashes without another attempt; workspace revision remained 27. The operator record binds the executable, source, fixture, runtime identities and native suite, SHA-256 `07b5a72a50f0fe2b60f09d6c3372091bfbcd2e8ef844d776e613ec0702b3b9e4`.

This establishes a native development workflow on the available Mac. General PDF features, word regions/accepted anchors, cross-platform specialist runtime execution, signed helper confinement, complete offline installation and release artifacts remain unfinished. These observations do not measure application-wide network traffic or qualify clean installed packages. All twelve complete-release gates remain unpassed.


## OCR word-region engine — 24 September 2026

The separate opt-in [word-region engine](security/OCR-WORD-REGIONS.md) returns bounded page/block/paragraph/line/word boxes relative to the assigned raster, exact word text and explicitly uncalibrated Tesseract confidence. A fixed recipe produces paired text/TSV outputs using the existing inventoried English runtime. Rust checks hierarchy, order, bounds, unique identities, finite scores, complete rows, text agreement and exact raster/output bindings. Existing text-only OCR, job and extraction schemas remain unchanged. This engine creates no accepted facts or original-document anchors and does not yet persist regions in canonical jobs.

Clean integrated source `2fa8737c318ff7538d3e1c06e9f0fad55b1b143a` passed **154 ordinary Rust tests** (20 native/runtime cases explicitly excluded), strict Clippy, five portable failure/history evidence cases and **eight native region/contract tests** on macOS 26.6.2 arm64. The native report binds source, runtime and synthetic fixtures, SHA-256 `5c99c38e80de1f613f57b9b047b00d917a5dd260e5ee4bc7f5512bbc57513af9`. It includes actual recognition with word boxes covering raster ink, blank input, malformed/oversized/truncated output, hostile access, direct networking, cancellation/reaping and output limits. The separately signed increment also passed all eight legacy native OCR checks, release/debug Clippy, unchanged schema generation and bounded independent code review.

Paired output validation cannot establish recognition accuracy or detect a worker that fabricates mutually consistent text and regions. Canonical retention, original-page mapping, reviewed anchors, UI and installed cross-platform execution remain subsequent work. The portable failure recorder is included in all three source-CI targets; source compilation does not qualify runtime confinement. No complete-release gate passes from this increment.


## Windows foundation, scale baseline and snapshot consistency — 25 September 2026

The designed PDF/region source `e62077dabc3c30d5ef8e3ba5369f90698b4e147b` passed [all four hosted source jobs](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36013001638). Later changes require their own hosted checks.

The Windows foundation is now integrated as a separate development crate. [Native run 36013049020](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36013049020) passed 29 native tests, all 18 combined permission/delivery predicates, exact worker ACL-rewrite denial with a successful unconfined positive control, separately identified host-injected empty-DACL inspection/cleanup, the remaining resource/hostile modes, and copied Java 21 assigned input/output. The actual tested source was PR merge `3fb76818687a127ae277ec3ed8ee320aea4f10a1`, with parents `e62077d` and `ad03ea0`; its tree matched the local merge before the evidence note. The sanitized report SHA-256 is `47aafad44f9de606f4fcb2de541b5afe72b70c3201ad7c478f75d39ceb088b36`. [Windows review details](security/WINDOWS-WORKERS.md) preserve all fifteen earlier failures and distinguish precise prevention from host-created recovery. The observed host is Windows Server 2022, not Windows 11 installation acceptance. Core Windows parser/search/OCR adapters remain disabled; no release gate passes.

The [100,000-transaction baseline](performance/OBSERVATION-2026-09-24.md) completed all six canonical operations with four samples each on the available 16 GiB Apple M1. All accepted/pending/rejected/deferred partitions and transfer source lists remained explainable. First calls ranged from about 270 ms for an empty analytical scope to 1,660 ms for HTML publication; the largest repeated HTML call was 1,752 ms. These small, cache-uncontrolled core samples exclude native IPC, JavaScript filtering/rendering, documents/OCR and concurrent maps/graphs. They do not qualify p95 or application responsiveness. The observation is retained unchanged at its measured source.

The separate [payload diagnostic](performance/PAYLOAD-DIAGNOSTIC.md) measured an actual View response of 83,986,629 bytes, rising to 198,449,390 after one saved HTML snapshot. Transactions consume 47,110,803 serialized bytes, review decisions 25,568,901 and the one-report array 114,462,763. This motivates narrower revision-bound reads and explicit detail retrieval; no transaction, decision or old report is deleted. The generated HTML itself is 113,592,530 bytes, and its measurement child's peak RSS includes restore/readback/oracle allocations. Browser performance at that size remains unmeasured.

Review also reproduced a correctness defect in whole-workspace reads: revision and record tables were read in separate implicit transactions. A second connection could commit a source between those reads, placing later content under an older revision in both UI responses and saved reports. `Workspace::view` now uses one SQLite read snapshot. Three deterministic tests failed before the fix; four pass afterward, covering a committed concurrent writer in WAL mode, blocked-then-resumed writing with the normal DELETE journal, immutable report revision/content and transaction release after a failed decode. The full core suite passes **158 ordinary tests**, with 20 native/runtime tests explicitly excluded, plus strict Clippy. Independent bounded review found no further actionable issue. The fix may hold the existing read lock longer; large-workspace contention remains part of performance work.

After integration, all four affected real-core assessment/investigation browser workflows pass. The performance example passes four Rust checks and eight portable failure-evidence checks; strict Clippy includes the new example. The 100,000-row timing observation remains bound to its original source rather than being relabelled after the snapshot correction.


## Retained image regions — 25 September 2026

The opt-in image-region coordinator and private derivative store are integrated at `896df06`. [The contract](processing/IMAGE-REGIONS.md) adds command v10 and processing-job v4 without changing the historical schemas. Storage schema 4 has a backed-up transactional migration: an existing v3 finding retains its reviewed state, while legacy v1/v2 review invalidation remains. Reported recognition, raster and exact TSV are immutable derivative files with validated SQLite references. No observations or reviewed word anchors are inferred from OCR output.

The exact clean agent source `b6a702c0f7e65a7806f8f389f44bf73eb5af6f4c` passed **14 native retained-region checks** on Apple Silicon macOS 26.6.2, using the actual confined PNG/JPEG decoder and English OCR workers. Report SHA-256: `8165cb9abd6ee6c1545cb884d9a5d86b31b97f2858964f9336992835d119096c`. The tests cover canonical publication, retained raster/TSV/result identities, backup/restore, corrupt/missing/conflicting derivatives, cancellation, retry history, concurrent publication during a backup, failed migrations and incomplete manifests. An earlier fixture assertion expected eight words instead of the observed seven; its failed record is retained. The clean final run asserts the exact expected sequence.

The integrated source also passes **172 ordinary core tests**, with 21 native/runtime tests explicitly excluded, and strict Clippy. Four portable failure/history checks are included in source CI. Independent bounded review found no remaining blocker in this storage increment. Hosted Windows source testing is still required for its new positive recovery and junction cases; this Mac has no MinGW C compiler.

[Native editable Figma frames](design/IMAGE-OCR-REGIONS.md) define the desktop/compact inspector and workflow states. Two native exports are retained. Figma reported a connection issue during the final states export, so remote synchronization and that export remain unverified. Production region display is separate work in progress. Aggregate disk quota, orphan garbage collection, OCR accuracy, accepted anchors, Windows/Intel specialist runtime execution, signed helpers and complete installation acceptance remain open. All twelve release gates remain unpassed.


## Explicit report retrieval — 25 September 2026

The desktop now requests a report catalogue and explicitly fetches the selected immutable snapshot for export. Command v11 adds an ID/expected-digest read; Rust validates the stored identity and actual UTF-8 HTML hash, with a 256 MiB in-memory response bound. Legacy full workspace reads remain available. All mutation responses share the same domain implementation, and both read modes use one SQLite snapshot. Creating a report no longer allocates previous report bodies unused by its renderer.

The [measured isolated-copy check](performance/REPORT-PRESENTATION.md) at clean `de49ba64c89e54872196864383a44f5b615d14b2` reduced the 100,000-row, one-report response from **198,449,390 to 83,986,848 JSON bytes**. All non-report fields, 100,000 transactions and 95,100 decisions were identical. Explicit export returned the exact saved snapshot, and the original benchmark database hash remained unchanged. Observation SHA-256: `e43d34a59dd6bdef4dac18055b80d4c017f3ab78c236a60d72ebaba42e78df96`. This is a byte verification, not a timing or memory benchmark. Pagination and smaller report exports remain needed.

Combined retained-region and presentation source passes **176 ordinary Rust tests**, with 21 native/runtime tests excluded, strict Clippy, production UI build and **51/51 real-core browser workflows**. The new browser regression corrupts retained HTML after the catalogue loads, verifies an integrity error without a download, restores the original bytes and checks exact downloaded content on retry. Independent bounded review found no remaining blocker in the presentation read/dispatch scope. Earlier source `20895ce8c56ef6998a884dd0609ad51911d4293b` passed [all four hosted source jobs](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36014847349); newer changes require their own checks.

The [integrated Windows foundation repeat](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36014847360) also passed. Its actual checkout was PR merge `c1eee5dfa53989b39f7d2555d05d1484b9d4aa0c`, parents `b12271cc0905c5d6fb6d58b715a7e554eb976db4` and `20895ce8c56ef6998a884dd0609ad51911d4293b`; report SHA-256 remained `47aafad44f9de606f4fcb2de541b5afe72b70c3201ad7c478f75d39ceb088b36`. The repeat remains Windows Server 2022 development evidence, not Windows 11 installation acceptance. No complete-release gate changes.


## Native exports, coordinator shutdown and transaction pages — 25 September 2026

The actual Mac export check found a defect missed by Chromium browser tests: without a native download handler, the WebKit window cancelled HTML downloads. The development desktop now explicitly creates its configured window with a handler that accepts only local bundled-interface blob exports with known HTML/JSON names in the OS Downloads directory. The UI reports a saved filename only after native completion; rejection and missing completion are visible errors. Only event listen/unlisten permissions were added. Network downloads, arbitrary paths and event emission are not granted. Pending completion records expire after two minutes, so lost callbacks cannot permanently exhaust the eight-record tracker; an expired late event cannot confirm a different export. The tracker is not an aggregate download/disk quota.

The rebuilt native application with application source `c0c8285a2d31c1693acc498e82724c5d8840cf73` migrated the existing synthetic workspace from schema 3/revision 27 to schema 4/revision 28. Its migration backup has identical canonical records and all seven original checksums verify. HTML export and a collision-named second file both contain the original 15,026-byte snapshot with SHA-256 `790fc5ac4296af67af85a5ef5e71e0881aaf85208bcb8d63b6733a1f8780c10e`. Native JSON export contains all 13 exact canonical transactions; its SHA-256 is `b9710a51553cccda7beb0d07d941ce0496116eb01c706b6a6158ecac441aa221`. Both saved messages were observed. The operator evidence binds the executable/build log and discloses the test-only configuration change during compilation, SHA-256 `fdf60708ad527dd32231723374c3477bbe075342d08985fdc47fc5f59928424a`. Accessibility control timed out after HTML export; a fresh control session recovered the saved message. The application had no crash report and a read-only process sample showed its event loop waiting normally.

Three native policy/tracker unit tests and strict desktop Clippy pass. Five affected real-core browser workflows pass with an owned server. An earlier concurrent root run accidentally reused another worktree's development server and ended with connection refusals; that failed log is retained. The Playwright configuration now refuses an already occupied server instead of silently using a different source/workspace. Native export policy tests are included in source CI. Installed Windows/Intel export behavior still needs actual platform evidence.

A separate deterministic regression reproduced workspace restart contention: closing the coordinator's lock descriptor did not release a Unix lock still inherited by an unrelated fork. The coordinator now explicitly unlocks once **after all worker threads join**, serialized with shutdown. A real child holds the inherited descriptor across shutdown in the regression; no sleep/retry conceals contention. A second regression protects the replacement owner's lock from repeated shutdown. The original failure, before-fix full log and after-fix full log are separately retained. The repair is `fa5c704`; [the job contract](processing/JOBS.md) documents the ownership boundary.

The [transaction pagination backend](transactions/PAGINATION.md) is integrated at `d71313d`. Command v12 adds bounded revision/query-bound pages, stable date/sequence ordering, exact currency/account/date/review filters, all four review denominators and original/anchor-preserving rows. Pages contain at most 200 rows and 2 MiB of retained row bodies; oversized single rows fail explicitly. A correction invalidates continuation. This backend does not yet replace the UI's full transaction/review arrays, and SQLite still scans/sorts JSON records. No large-case responsiveness or release gate is claimed from adding this API.

Root integration at `d71313d` passes **186 ordinary core tests**, with 21 native/runtime tests excluded, and strict core Clippy. The previous published `f869892` source run passed the browser and both Mac jobs, but Windows strict Clippy rejected two read-only corruption-fixture resets. Narrow Windows-test-only lint allowances now document those resets at `2686e64`; the fresh hosted run remains pending. The native tests preceding that lint step passed.


## Native image-region review integration — 25 September 2026

Integrated source `3536859` passes **188 ordinary core tests**, with 21 native/runtime tests excluded, three desktop export-policy tests, strict core/desktop Clippy, the production UI build and **60/60 real-core browser workflows**. Current schema generation emits 24 schemas with no tracked byte changes. The final development-bridge disconnect guard is included in this combined browser run. Earlier failed runs remain retained separately.

The clean native application built at `0b4125e` completed the actual PNG decoder and English word-region OCR from the existing retained synthetic original. Its binary raster command displayed the verified 1200 × 230 raster and seven words. Selecting `0042` showed the exact rectangle (left 292, top 120, width 98, height 32) and raw score 15.542927; fit/100% viewing and the text-copy completion message were observed. The original SHA-256 is `410a92d6da102c53cac25fd450923057f717342df68d0a09dfc61d9f8296ef92`.

Normal quit and restart preserved all 55 canonical records byte-for-byte, the completed attempt and all three derivative objects. The 52 records present before the new job were unchanged. The executable SHA-256 is `af6055b38efd0cace9503657a533f30a53496dde35486fdc2d8b4bc5d4d72516`; the retained operator record binds the build log, original, job, derivative bytes and selected geometry, SHA-256 `420677505ed4226efd5720a3a7e1d1a35f8ae15f2bf208570d87f38167266221`. This establishes the development Tauri/WebKit binary-display path on the current Apple Silicon Mac. It is not OCR accuracy, accepted-source-anchor, application-wide network/memory or clean packaged-installation acceptance.

The first two hosted Windows Java engine attempts both failed at the initial parser operation. The second bounded post-termination diagnostic identifies a large dump-like scratch artifact; the underlying runtime cause is still under investigation. Engine adapters remain disabled, and resource/confinement limits have not been increased. The existing foundation predicates continue to pass; their success does not pass the engine or Windows 11 release gate.


## Bounded source review and measured transaction pages — 25 September 2026

[Command v13 source reads](transactions/SOURCE-READS.md) return at most 25 exact selected rows, in requested order, at the originating revision and recorded versions. The complete batch must fit 2 MiB of retained JSON bodies before any row body is copied into Rust. Canonical identity/value checks and each distinct original's integrity check precede publication. Pattern and period-comparison source dialogs now use these reads. A page change or return to an earlier page requires fresh verification; stale, corrupt and late results cannot enable source inspection.

Combined core passes **195 ordinary tests** with 21 native/runtime exclusions and strict Clippy. The production UI build and **63/63 real-core browser workflows** pass. Added browser cases verify actual altered-source rejection/restoration, exact 25/2-row calls and the page-return race found during peer review. Historical schemas remain unchanged; command v13 preserves all 46 prior variants and definitions. Five portable benchmark-evidence tests pass.

The [retained 100,000-row page benchmark](performance/TRANSACTION-PAGES.md) found an unnecessary second scan for empty selections. The guarded shortcut keeps cursor validation and every response byte unchanged. On this Mac, the two 20-sample campaigns measured empty-result warm p50/p95 at 158.190/164.865 ms before and 78.585/83.660 ms after. Nonempty query paths are unchanged. A first 200-row page serializes to 94,696 bytes versus 83,986,629 for full presentation; the default response and remaining client consumers are not yet fully paginated. The paired observation binds exact executables, source and unchanged original/copy hashes, SHA-256 `6066d5f6c2b73c74ebe5b75ead77340664e77bd5f45cabd136fc15a7b4fc28e6`. These short backend samples exclude IPC/rendering and do not pass the full performance gate.

Published `fc8c773` passed Windows source CI, the browser job and Windows confinement foundation repeat. Both Mac source jobs failed before desktop tests because the new unit-test step did not receive the existing CI resource override. A fresh checkout without staged runtimes reproduced that failure; the same three policy tests pass with the narrow step-local override, now integrated at `bf8b6f2`. Production bundle resources remain unchanged. Fresh hosted checks must verify the repaired head.


The native Mac app built at clean `e9f5f98` also verifies the new source-read path. Pattern calculation displayed the full 13-row denominator, with 12 AUD and one USD pending row. Opening AUD pending sources returned all 12 exact rows, and source handoff displayed `Opening credit`, `2400.00 AUD` and the matching preserved CSV row 2 amount excerpt. Normal quit left all 55 canonical records and revision 31 unchanged. The retained operator record binds executable/build hashes and both referenced originals, SHA-256 `93e6acdd6b0d534d01cf0bf4ca2cd3566ecf2e2749f48bf116030bab7f67be24`. This verifies the current development Tauri path, not clean installation or large-case desktop responsiveness. Period comparison shares the reader and has separate browser coverage.


## Paged finding history and transaction selector metadata — 25 September 2026

Command v14 adds [target-bound review-history pages](assessment/DECISION-HISTORY.md), with one revision snapshot, canonical insertion order, verbatim historical reasons/timestamps, up to 50 decisions and a complete 1 MiB body preflight. Unsupported, ambiguous, stale and oversized reads fail explicitly. The finding-review UI now uses this reader instead of filtering the default decision array. It preserves drafts across refresh while refusing stale review mutations, distinguishes unavailable from empty history, and discards late results after selection changes.

Command v15 adds [whole-ledger account/currency facets](transactions/FACETS.md), independent of the current ledger page and review state. Exact values and counts are paged in binary text order, with bounded copies and revision-bound representative cursors. It does not analyse amounts or reverify originals. The selector UI still awaits migration; the default transaction and decision arrays remain present. Literal whole-ledger search and citation readers are further dependencies of that migration.

Combined clean source `04d056724c536da16e34b7f695d9ec8603168ddc` passes **209 ordinary core tests**, with 21 native/runtime exclusions, strict core Clippy, production interface build and **69/69 real-core browser workflows**. All 51 historical schema files remain unchanged; v15 retains all 48 v14 variants and 26 definitions. The history browser tests cover real canonical writers, stale continuations, source handoff, empty/failure states, delayed responses and keyboard behavior. Two initial targeted failures exposed disabled-button focus loss; the final repair and no-focus-steal regression pass. Facet peer review found an account-control/filter mismatch; explicit rejection and a real quoted-CSV regression fix it. No release gate changes.

The native Mac app at the same clean source displayed all three saved decisions for the synthetic finding, with exact IDs, reasons and raw timestamps at revision 31. Refresh preserved an unsaved draft; nested inspection displayed the retained row 10 amount `-180.00`; Escape restored the source and finding controls. The draft was never submitted. Normal quit left all 55 canonical records byte-identical. Operator record SHA-256: `1c00051299c9a78de660d2deeb5b8c0b275044436b09fc7d1b2c1ac28d1f1e2d`. Multi-page and concurrent-writer behavior have browser coverage; this native check used three decisions and is not clean-installation acceptance.

Published `e9f5f9881bd49055cb07810fb5aed4c633d2e83b` passed [all four source jobs](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36024719341) and the [Windows confinement foundation repeat](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36024718994). The previous Mac CI resource-override failure is therefore resolved at that published source. Newer source still requires its own hosted checks.

The sixth Windows Java development probe passed its bounded benign FileWorker control but failed its first confined parser operation. A sanitized JVM source fingerprint identifies bootstrap image canonicalization; investigation found that the JDK rejects the extended Windows path prefix used by the confined runtime copy. The separate Windows engine PR is preparing an identity-checked Java launch-path spelling repair; its adapters remain disabled pending actual successful native probes. No ACL, capability or resource boundary was relaxed.

The original Figma document remains listed, but a safe control read timed out and native menu interaction did not recover its document. Its unsynced original was never reloaded or closed. Existing editable design references and implementation screenshots are retained; new pagination frames and remote-save verification remain open. Complete runtimes, remaining analytical workflows, clean Windows/Intel installation, signing and human review remain delivery dependencies. All twelve complete-release gates remain unpassed.


## Literal ledger search and original identity repair — 25 September 2026

[Command v16](transactions/LITERAL-SEARCH.md) adds bounded literal text search over the complete transaction scope, sharing the existing page/row/original-verification implementation. Text joins original description, account and date with single spaces; lowercase matching is whitespace-preserving and neither normalizes Unicode nor interprets wildcards. Text is a scope dimension before review selection. Empty search retains the older page behavior with a distinct search cursor family. Nonempty search explicitly fails malformed or oversized fields in scope instead of hiding them as nonmatches.

The matching profile records the compiled Unicode version. The shared 32-case fixture passes both actual canonical Rust search and real Node JavaScript checks; Rust's Unicode 16.0.0 and this Node runtime's Unicode 17.0 are reported separately. This is a named regression set, not universal engine-version equivalence. The browser CI job now runs the same JavaScript fixture and records its actual runtime versions. No new dependency package was added; the existing SQLite binding enables its fixed-function feature.

[Direct source inspection](processing/SOURCE-INSPECTION.md) now rehashes the original before returning an anchored quote. Bounded peer review also found that verifying a substituted digest alone could incorrectly attach another intact original to an evidence record. Original verification now binds canonical evidence ID to its digest; excerpt and page readers additionally bind the requested source key. The two-original retargeting regression protects that distinction. Missing, same-length altered and substituted originals fail explicitly, with exact restoration supported. Existing schema shapes and valid source behavior remain unchanged.

Combined source `daf16dfbe109cf7a6b50b0cf8d415a39ebc83ca1`. It passes **222 ordinary core tests**, with 21 native/runtime exclusions, strict core Clippy, production UI build, the 32 JavaScript cases and **70/70 real-core browser workflows**. The fresh browser binary includes the final shared digest check. Prior failed fixtures, peer findings and narrower verification records remain retained. Default arrays, selector/search UI migration, citation readers and compact summary projection remain outstanding. The published `80b8646` also passed all five hosted source/foundation checks; this new source requires its own run. All complete-release gates remain unpassed.


## Citation metadata and opt-in desktop summary — 25 September 2026

Command v17 adds a bounded citation catalogue and complete selected-ID metadata reader. Observation, transaction and whole-source metadata share literal matching, explicit revision/identity checks and a 2 MiB projection bound; failures never shrink the selected set. Original verification remains an independent source-inspection/review operation. The assessment UI migration is in progress. See [citation contract](assessment/CITATION-CATALOGUE.md).

The separate typed [desktop summary](performance/DESKTOP-SUMMARY.md) preserves exact legacy calculations and retained workspace fields while omitting full transaction/decision arrays, balance-check vectors and total-source ID vectors. It is opt-in only: desktop and browser transports still use the existing presentation response until every consumer migrates. This reduces a future response payload; the implementation still loads full canonical rows and runs the existing calculator. Evidence text and statement-import ID lists remain, so no bounded process-memory claim is made.

Integrated source `ba58cf9` plus a cross-mode citation regression passes **241 ordinary core tests**, with 21 native-engine tests explicitly ignored, strict all-target Clippy and unchanged regeneration of all 64 saved schemas. The last complete browser campaign remains 70/70 at `daf16df`; no UI changed in this backend integration. Windows Java probe PR53 has passed its first confined TXT parser but still times out on PDF; its complete recipe remains unpassed. All twelve release gates remain open.


## Transaction consumer migration — 25 September 2026

Commands v18–v20 add [selected balance states](transactions/BALANCE-STATES.md), [transfer counterpart pages](transactions/TRANSFER-CANDIDATES.md) and [complete filtered JSON export](transactions/COMPLETE-EXPORT.md). Reconciliation still uses the complete canonical source sequence, transfer selection retains the broad accepted-other-account denominator, and full export never substitutes a visible page. Export validation now checks borrowed filter strings before copying. The integrated core passes **265 ordinary tests**, strict all-target Clippy and unchanged regeneration of all 70 prior schemas; 21 native-engine cases remain explicitly excluded from the ordinary suite.

The assessment [citation UI](design/CITATION-PICKER.md) passes **79/79 real-core browser workflows** on its tested backend, including nine citation cases and bounded client request scheduling. Native build `4efa177` resolves the saved citation, retains the exact `-180.00` and mapped `12,30` source excerpts, preserves two unsaved citation roles through a zero-result search and refresh, and leaves all 55 canonical records unchanged on quit. Native refresh exposed a focus loss to the HTML root when its button was disabled. The retained negative observation is followed by the repair below; this remains development verification.

A clean isolated 100,000-row [payload comparison](performance/observations/2026-09-25-desktop-summary-payload.json) measured presentation **83,986,629 bytes** versus opt-in summary **5,111,287 bytes**, with exact defined projection equality and unchanged original/comparison databases and evidence. This is a 93.91% serialized-byte reduction for that fixture. Desktop transport still uses presentation until ledger, review, facet and export consumers migrate; no UI-latency or peak-memory claim is made.

Published `9a681fc` passed all five hosted source/foundation checks. The Windows Java eleventh probe separately passed 53 native Rust tests and five Java tests, then timed out in the benign PDF control. A bounded sample showed font-provider file I/O; the confined PDF transport returned output but its full fixture assertion was not reached. A deterministic bundled-font extraction fallback is being implemented in PR53, without additional worker rights or longer limits. Windows engine activation and all twelve release gates remain unpassed.


## Native focus repair and bounded analysis selectors — 25 September 2026

The citation refresh repair (`ad74c03`) passed 21 targeted real-core browser workflows and a rebuilt native WebKit check. Refresh restores its button focus while retaining the unsaved reason and selected roles; deliberate movement followed by blur is covered by a delayed browser response. During native verification, an operator keyboard action submitted a temporary synthetic citation. Normal UI editing restored its original set, retaining both audit decisions. All 55 preexisting records and the saved report are byte-identical after restoration, all seven originals rehash correctly, and revision 33 contains 57 records. The [verification record](VERIFICATION.md) discloses this test effect.

The [bounded facet selector](design/TRANSACTION-FACETS.md), integrated as `b0214bf`, replaces whole transaction arrays in pattern/comparison account and currency controls. It preserves exact selections outside a page, including leading zeros, and displays complete all-state denominators. Its seven new workflows and 17 existing analytical workflows pass; desktop/compact/empty/error axe checks have no violations. New Figma pagination states remain unverified. The additive development `--summary` mode passes an actual subprocess integration case; the combined core now passes 266 ordinary tests with 21 native exclusions. Desktop summary activation and the remaining ledger/review/export migration continue independently.

Combined pre-summary source `af67c16` now passes all **89/89 browser workflows** with the v20 core. This closes the backend/UI compatibility uncertainty for the integrated citation/facet changes; it does not qualify the forthcoming summary transport or installed releases.

## Desktop summary activation and report foundation — 25 September 2026

Both desktop and development transports now use the [typed summary and bounded ledger](design/DESKTOP-SUMMARY.md), integrated as `f2b1e9a`. Transactions, source-order balance states, transfer candidates, citations and history use their dedicated readers. Applied filters and complete export scope remain explicit; stale rows cannot mutate newer records, and late summaries cannot replace a newer workspace revision. The full real-core browser suite passes **104/104**, with three desktop Rust tests, strict desktop Clippy and the production build passing. Subsequent test-only readiness refinements pass four and three focused cases. The historical CLI modes and schemas remain compatible.

The rebuilt native Mac app displays the isolated **100,000-transaction** case at revision 95102, exact currency totals and review counts, and first/second 100-row pages. The regular 13-row case displays its retained source-order balance discrepancy. Complete JSON export fails in both cases: the main thread stalls in WebKit's macOS download-destination sandbox-extension issuance, with no completed file. The test processes were stopped after retaining traces; every canonical table and original remained unchanged. Native export repair is active. These observations do not establish required performance, clean installation or release acceptance.

The [editable DOCX foundation](reports/DOCX.md), integrated as `8681a42`, freezes typed report inputs and shared calculations, validates references and renders a bounded, deterministic, inert OOXML package. Seven integration tests and one archive-bound test pass; the combined core passes **274 ordinary tests**, with 21 native exclusions, and strict Clippy. Repeated generation yields identical bytes and all eight rendered pages pass inspection using the recorded alpha LibreOffice executable. Canonical DOCX publication, historical snapshot storage/recovery, assembly, exhibits, approved fonts and native Word compatibility remain unfinished.

Published `4fe5647` passed [all four source jobs](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36037666297) and the [Windows confinement foundation check](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36037666286). New integrated source requires fresh hosted verification. The separate Windows Java thirteenth campaign passes its confined parser corpus but fails Lucene indexing; its diagnostic follow-up preserves resource and permission boundaries. Durable collection checkpoint work has begun independently. New Figma states, complete packaging, clean Windows/Intel acceptance, signing and human review remain open, and all twelve release gates remain unpassed.

## Durable collection and workspace onboarding — 25 September 2026

The private [durable collection foundation](discovery/DURABLE-JOBS.md), integrated as `06acbb8`, saves charged request reservations, response evidence and replayable checkpoints. Recovery preserves unknown interrupted outcomes, cancellation and the original deadline. It remains synthetic-only and unreachable from application dispatch; live transport activation is separate. Its shared original reader bounds and verifies a single no-follow read. The foundation's own 288-test and old-reader recovery evidence is retained; the combined DOCX/foundation/onboarding source passes 297 ordinary core tests and strict Clippy, with 22 specialized cases excluded.

The native large-ledger account/currency filter returns the expected 6,700 rows and source-linked amount at CSV row 5602. Normal quit and unchanged canonical tables/originals are verified. An observed onboarding defect is repaired: fictional demo seeding is restricted to a new empty workspace, and overview priorities display actual entity/observation/location counts. A statement-only workspace no longer offers to load the demo. The production build and focused real-core browser regression pass; no layout redesign or new Figma acceptance is claimed. Native export repair and Windows search compatibility remain active; all twelve release gates remain open.

## Verified native export repair — 25 September 2026

The [typed native save lifecycle](exports/NATIVE-SAVES.md), integrated as `86fb0cc`, replaces native WebKit downloads for complete transaction JSON and saved assessment HTML. Rust prepares the exact requested artifact, the interface rechecks its scope/lifetime, and commit publishes verified bytes to the application-managed exports directory without overwriting existing files. File identity, bounded stages, expiry, shutdown, cleanup failure and repeated acknowledgements are covered. The browser development download path is retained.

The actual rebuilt Mac apps save both 13 rows and all 100,000 rows with exact reference bytes. The large 60,613,204-byte export remains unchanged on a second save, and a historical revision-12 HTML report exports unchanged while its workspace is revision 33. Both interfaces remain responsive, quit normally and preserve every canonical table and original. Combined verification passes **319 ordinary core tests**, strict Clippy and **110/110 browser workflows**; 22 specialized tests remain excluded. These checks resolve the retained development Mac export failure, without establishing clean installation, other native platforms, performance or signed-release acceptance.

Processing, backup/restore and collection export also now consume verified original buffers rather than reopening source paths. [The consumer record](security/VERIFIED-ORIGINALS.md) covers exact copy bytes, source replacement and rollback. The public staged-file scanner now handles deletions while continuing to scan surviving staged blobs, with six real-Git regressions. All twelve release gates remain open.


## Canonical source keys and retained file identity — 25 September 2026

Evidence lookups now require the canonical SQLite key, Evidence ID and original digest to agree. This prevents a valid source body and intact original from being substituted beneath another source key. Review, analytical provenance, processing, collection, report capture and recovery use the bound metadata readers; specialized bounded readers retain their existing explicit checks. [The source-identity contract](security/EVIDENCE-IDENTITY.md) distinguishes metadata validation from original-byte verification. Historical reports, public commands and schema shapes remain unchanged.

Immutable derivative reads now verify one bounded no-follow handle and compare its final named identity. The shared private file verifier also covers original reads and native export files. On Windows it uses the full 128-bit file ID and expected hard-link count, with write/delete sharing denied; unsupported identity queries fail closed. Regression cases cover replacement, growth, hard links, FIFO replacement and preserved exact bytes. These protections do not establish an application-wide race-free filesystem sandbox.

Combined core verification passes **333 ordinary tests**, with 22 specialized/native cases excluded, plus strict all-target Clippy. The last complete browser/native UI observation remains the 110-case campaign and actual Mac exports described above; this source-only follow-up does not relabel that evidence. Native Windows identity/sharing cases await the new hosted source run. Canonical DOCX publication/recovery, cancellable live collection and confined Windows Lucene remain active; all twelve complete-release gates remain open.


## DOCX recovery, Java compatibility and collection transport — 25 September 2026

Canonical DOCX publication retains a bounded frozen JSON model and exact editable document before one revision-guarded SQLite publication. Stable request IDs recover lost acknowledgements; inspection and export bind both artifact digests. Storage schema 5 adds report artifacts to the existing content store and consistent recovery inventory. An actual schema-4 OCR workspace upgrades with a complete backup and restores with both OCR and report objects. The existing HTML snapshots remain immutable. [DOCX storage](reports/DOCX-SNAPSHOTS.md) records this backend scope; catalogue commands and interface saving are being integrated separately.

The [Windows Java recipe campaign](security/WINDOWS-JAVA-WORKERS.md) passed on Windows Server at its recorded merge source, including 60 native Rust tests, 30 Java tests, bounded Lucene queries and exact before/confined/after permission predicates. That engine source is integrated locally. Root rebuilt the shared JARs with 30 actual Maven/JUnit tests and passed all 13 intended Mac parser/search and hostile-boundary cases at clean `8c4f8ab`. The first combined campaign incorrectly selected a separate native OCR test without an OCR runtime; that negative result is retained and the harness filter is repaired. Windows core activation, Windows 11 installation and signed distribution remain open.

The private [cancellable transport foundation](discovery/CANCELLABLE-TRANSPORT.md) is integrated as `c6a04ca`. It preserves selected-host/public-address checks, pinned verified HTTPS, complete response bytes, exact checked clock observations and typed unverified resolver shutdown. Fourteen synthetic TLS/socket cases are included in the combined **360 ordinary core tests**, with strict Clippy passing. It has no live coordinator caller or new public command. Native resolver execution, durable integration and migration of the existing synchronous collection path remain required. No hosted search provider is introduced.

[Ledger scope frame 57:2](design/LEDGER-SCOPE.md) has editable native Figma layers and an inspected native PNG. Independent remote readback timed out; compact/state extensions and final design approval remain open. No complete-release gate changes.

Command v21 now exposes canonical DOCX capture, revision-bound metadata pages and explicit verified inspection. Typed native DOCX save reuses the existing no-clobber file lifecycle and exact binary bytes. The UI is being connected separately. Integrated `229ea20` passes **372 ordinary core tests**, strict Clippy and all **five existing native-export browser compatibility cases**; the preceding 110-case full browser run remains recorded above. A deterministic regression repairs false export-busy refusal caused solely by janitor registry contention.


## DOCX interaction, extraction compatibility and Windows routing — 25 September 2026

The [DOCX interface](design/DOCX-SNAPSHOTS-UI.md) now captures immutable snapshots, pages their metadata and saves verified binary documents through the native lifecycle. Typed recovery checks a retained request at one canonical revision; only proven absence at a later revision enables a separate new-capture action. Same-revision uncertainty keeps the original request. Native WebKit saving, Word interaction and the remaining editable-design comparison are separate checks.

New [extraction v2](processing/EXTRACTION-V2.md) records expose local-font substitution/coverage limitations and validate original, owner, attempt and result identity. Historical v1 bytes remain readable after retry and restore. Signed-clean Mac compatibility passes 14 parser/search cases. The [Windows canonical parser route](processing/WINDOWS-PARSER.md) is integrated with typed failure outcomes, raw strict decoding and cancellation; its actual Windows coordinator campaign is being prepared. Missing trusted-runtime assets remain blocked prerequisites; scratch/execution I/O remains a worker failure.

Combined source `3ce8000` passes **390 ordinary core tests**, strict Clippy, production UI build and **125/125 real-core browser workflows**. All 80 prior JSON schemas remain unchanged; only three new schemas were added. The first 119-case browser run exposed a late transaction acknowledgement closing a later review. [Two deterministic regressions](transactions/REVIEW-LIFECYCLE.md) reproduced it; the repair preserves the new draft and its stale revision. The failed run remains retained. All twelve complete-release gates remain open.

## Windows campaign test repairs — 25 September 2026

The first hosted runs of `ed4ab2c` failed before canonical coordinator execution. Source verification encountered a Python module-name collision between the campaign runner and its receipt tests; the receipt tests now have a distinct filename. The AppContainer job passed 65 worker tests and failed an assertion that a Windows exclusive-open refusal maps to Unix `PermissionDenied`. The repaired fixture independently checks the exact Windows sharing violation and compares its native error classification, retaining the blocked-runtime, cancellation and successful-after-release assertions. Runtime policy and confinement are unchanged.

Local verification passes 52 host worker tests, strict worker Clippy and the full 140-case Python discovery suite (three platform skips). A fresh hosted run is required to establish Windows execution; the failed runs remain evidence. The canonical campaign, Windows 11 installation and all complete-release gates remain unpassed.

## Browser prerequisites and durable collection — 25 September 2026

The fresh hosted browser campaign exposed a cached-helper dependency: 122 workflows passed, while three DOCX save tests ran before another spec built their Rust helper. Shared Playwright setup now builds both required executables before any selected suite. All three failures pass locally with the helper initially absent; a new hosted campaign remains required.

Private durable collection settlement is integrated as `2fe5f7d`. It charges request reservations before transport, preserves exact bounded receipts across publication retries, releases the workspace during network I/O and refuses stale ownership. Recovery-required states block further requests across reopen. The integrated source passes 404 ordinary core tests with 22 specialized tests excluded. The production collection route is not switched to this private transport; coordinator shutdown integration and live activation remain open. No complete-release gate changes.

## Observed native reporting and runtime inventory — 25 September 2026

The actual isolated development Mac app at `3ce8000` passed DOCX capture/save, stale-request recovery, immutable re-export, normal quit/reopen and persisted catalogue checks. Two exports matched canonical bytes. Word 16.113.2 opened, edited and saved a disposable copy without changing the retained export. That observation exposed ordinary words breaking across lines. [Generator 2](reports/DOCX-WORD-WRAPPING.md), integrated as `4939659`, repairs the style setting while preserving generator 1's exact historical rendering and restore/export behavior. All seven managed-render pages were inspected. Actual Word layout for generator 2 remains pending because native controls stopped responding; the source patch and baseline native proof are separate evidence.

The [offline inventory producer](../packaging/INVENTORY-PRODUCER.md), integrated as `57fe451`, checks explicit reviewed ownership and locked file identities without executing or downloading dependencies. The actual staged Mac engine tree contains 425 verified files and 250,551,479 bytes, with 18 required components absent and zero invalid present components. It exits incomplete as required. This is a partial engine-tree observation, not a complete application bundle. Full integrated Python discovery runs 156 tests with four platform skips. Complete installers, signed distributions and all twelve release gates remain open.


## Shared coordinator and offline Python staging — 25 September 2026

The private synthetic collection lane now shares coordinator ownership with document jobs. Both lanes join before ownership can be released. Unknown termination quarantines execution immediately, including when publishing the collection receipt is blocked; active document work is cancelled and queued work cannot start. The already-owned exact receipt remains eligible for bounded settlement retries under the same retained lock. [Coordinator contract and regressions](discovery/COLLECTION-COORDINATOR.md) distinguish this test-only integration from the existing synchronous production collection route. Native resolver proof and live durable activation remain open.

Integrated application source `0e6d390` passes **423 ordinary core tests**, strict all-target Clippy and **125/125 real-core browser workflows**; 22 specialized cases are explicitly excluded from ordinary tests. The native desktop test target compiles but contains zero tests, so it adds no native lifecycle evidence. Source campaign [36132159822](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36132159822) passed all four hosted jobs at the earlier `ea154db` head. Fresh hosted source and AppContainer campaigns are running for `0e6d390`.

The canonical Windows campaign's first-fixture failure is now reproduced and explained: its test probe read evidence at the response root rather than inside the actual workspace envelope, before queuing a parse job. [The repaired probe](security/WINDOWS-PARSER-COORDINATOR.md) preserves production contracts and all required checks. Nine probe tests and 13 strict receipt tests pass. Actual Windows canonical success still requires the fresh native campaign; historical failures remain retained.

[Offline CPython staging](../packaging/PYTHON-STAGING.md), integrated as `98b395f`, accepts one exact reviewed upstream archive, enforces bounded no-follow extraction, retains available notices and materializes only ten explicitly reviewed links. Actual staging produced 3,333 regular files and 276,208,764 bytes without executing the interpreter. Independent inventory identifies 22 missing components and zero invalid present components. Full integrated Python discovery runs 177 tests: 173 pass and four platform-specific checks skip. Locked wheels, loader closure, relocation execution, missing referenced notice, confinement and complete application packaging remain unpassed. No release gate changes.


## Native canonical result and recovered design view — 25 September 2026

The [fresh Windows Server canonical campaign](security/WINDOWS-PARSER-COORDINATOR.md#native-canonical-campaign--25-september-2026) passes all 25 checks, ten synthetic fixtures/expected derivatives and four joined coordinators at merge `025ed706`, whose tree matches `0e6d390`. It proves the exercised import, job, extraction, restart, runtime-refusal and complete recovery paths. All four hosted source jobs also passed. In-flight canonical cancellation, Windows 11 installed artifacts and complete-release acceptance remain separate.

The first [opt-in native Mac DNS campaign](discovery/NATIVE-TRANSPORT-PROOF.md) observed four passing cases and one failed negative-callback criterion. All three created subscriptions were deallocated; no HTTP or repeat ran. The installed API contract explains the absent negative callback under current flags. That retained failure motivates a separately reviewed resolver correction; it is not relabelled as a passing campaign.

[Design recovery](design/DESIGN-WORKING-COPY.md) now verifies remote readback of the saved industrial ledger frame and identifies a new editable Drafts copy. Further authoring waits on restored browser controls. Word opened the unchanged generator-2 ordinary DOCX and displayed the intended first-page word wrapping; whole-page/long-literal inspection remains pending after another control timeout. These observations narrow the earlier blockers without declaring design or report acceptance complete.


## Reviewed wheel archives and corrected native DNS — 25 September 2026

The [offline wheelhouse verifier](../packaging/PYTHON-WHEELHOUSE.md), integrated as `cb9bbf5`, retains the exact 58 locked production archives and source-linked metadata/notices without installing or executing package code. Actual verification checks 94,887,529 compressed bytes and 319,470,877 expanded bytes. The resulting 339 files total 97,089,012 bytes, including 174 primary metadata files and 105 notice files. All outputs explicitly remain archives-only and satisfy zero runnable runtime components. Installation layout, hooks, native loader closure, confinement and actual offline imports remain open.

The [Mac negative-DNS correction](discovery/MACOS-DNS-NEGATIVES.md), integrated as `32eab45`, now requests intermediate negative callbacks and treats them independently of valid address candidates. Successful add/removal notifications remain bounded and public-address checked; caller cancellation/deadline/clock changes take precedence after owned cleanup. One new signed-clean native campaign passed all five cases with three created/deallocated subscriptions, zero HTTP and no retry. The preceding failed campaign remains unchanged. Windows resolver execution and live durable collection remain unproved.

The [clock-rollback settlement repair](discovery/COLLECTION-COORDINATOR.md), integrated as `8a0a93f`, preserves internal stop intent at the exact reserved request's validated checkpoint when wall time moves backwards. It accepts no supplied timestamp, retains raw response clocks and keeps ordinary timestamp validation strict. Exact-response retry and uncertainty precedence remain intact. This remains a private synthetic execution path; no public command or schema changed.

Combined application source `5c9ad52` passes **435 ordinary core tests**, strict all-target Clippy and formatting, with 23 specialized cases excluded. Python discovery runs 196 tests: 192 pass and four platform checks skip. Nine native-DNS evidence tests also pass normally and under optimization without network requests. The prior 125-browser and hosted platform results retain their exact `0e6d390` source; later publication requires fresh hosted checks.
## Current continuation — 25 September 2026

The signed-in Figma development copy now contains saved, native editable
[DOCX catalogue and capture-outcome frames](design/DOCX-SNAPSHOTS-DESIGN.md).
Both survived reload with byte-identical Figma re-exports. An application
comparison is in progress. The ordinary generator-2 report has now been visually
inspected across all three Word pages; long-literal layout and generator-2
edit/save remain unverified after the native picker stalled.

Published `bdc15be` passed all four
[source jobs](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36137528865)
and the separate
[Windows AppContainer campaign](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36137528676).
Root validated the new source-bound canonical receipt. These remain development
observations, with no Windows 11 clean-install or in-flight cancellation claim.

The integrated [Python static preflight](../packaging/PYTHON-INSTALL-PREFLIGHT.md)
records the proposed installation, all native references and package hooks.
A finite offline installer is being implemented from those exact inputs; the
58 wheels remain uninstalled in this observation. The opt-in Windows DNS harness
is under source review. Complete workflow coverage, runtime confinement,
packaging, clean-platform testing, signing and human review remain open. All
twelve complete-release gates are unpassed.

## Saved report design, installed Python files and native DNS — 25 September 2026

The [DOCX application comparison](design/DOCX-SNAPSHOTS-COMPARISON.md) now
connects the editable Figma catalogue and capture-outcome frames to the actual
industrial report interface. Nine captured states have zero reported automated
accessibility violations; keyboard pagination/focus and exact 20/20/3 catalogue
pages were checked. Corrected frame text makes workspace refresh and the native
filename explicit. Compact editable frames, zoom/manual accessibility and final
design acceptance remain separate work.

The [offline Python installer](../packaging/PYTHON-OFFLINE-INSTALL.md) assembles
the pinned runtime and all 58 wheels into 11,320 files / 601,821,300 bytes.
Root's independent reader verifies every file and all installed RECORDs against
manifest `4dc6fd171e842d1f9254be7fc5cb16e2e01203896403dcd9839a8aec69dad822`.
No candidate interpreter or package code has run. The first confined compatibility
probe is being implemented; runtime functionality, relocation and canonical
integration remain unproved. The missing zlib-ng notice reference is
[explained at the upstream metadata-generation level](../packaging/PYTHON-ZLIB-NOTICE-REVIEW.md),
with original metadata and unresolved final notice review preserved.

The single [native Windows DNS campaign](discovery/WINDOWS-NATIVE-DNS-PROOF.md)
passes all five cases on Windows Server 2022. It made three fixed native
lookups, no HTTP requests and no retries. Pending cancellation released caller
resources after completion while retaining unverified provider quiescence;
the transport refused further work. This establishes neither Windows 11
acceptance nor live durable collection activation.

Integrated application `c2635c3` passes the production UI build and all
**125 real-core browser workflows**. The full Python suite runs **238 cases:
234 pass and four platform checks skip**. Unchanged Rust sources retain the
`bef9f30` result of **438 ordinary core tests**, 23 specialised exclusions and
strict Clippy. The [integration record](verification/integration-c2635c3.json)
binds retained logs and source distinctions. Complete-release gates remain false.

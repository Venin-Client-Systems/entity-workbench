# Development verification — 2026-09-23

These observations retain the implementation history, not completion of the full plan. Later dated entries supersede earlier limitations only within their stated source and platform scope. The [native HTTPS, CSV and account-flow checks](#native-https-recovery-csv-application-and-account-flows--26-september-2026) are separate bounded development observations. All twelve complete-release gates remain unpassed.

Latest combined source [3a48786](verification/integration-3a48786.json) passes 633 ordinary Rust tests (31 specialized exclusions), strict all-target Clippy and formatting, 341 Python tests with four skips, the production UI build and 47 affected collection/document/OCR browser workflows. It integrates exclusive graph scheduling, the fixed app-local Python adapter, private capability composition and inert durable collection exports. Normal desktop graph execution remains unavailable; no native candidate or public-network campaign ran for this increment. Earlier full browser and native observations remain bound to their own revisions.

| Check | Observed result | Scope |
|---|---|---|
| Rust domain/workspace suite | 49 tests passed (42 integration, 7 unit) | Exact decimals/dates; atomic import; duplicate retention; review conflicts; correction propagation; transfer exclusion; currencies; reversible merges; backup/restore; corrupted originals; newer schemas; symlink paths; escaped reports; uncertainty; public-IP policy; worker-message limits; discovery budget; static HTML extraction; missing balances; acquisition provenance; general identity authoring; source-anchor validation/excerpts; identity decision recovery; assertion invalidation; robots/transport/rate-limit outcomes; redirect scope; bounded source expansion |
| Rust static analysis | Clippy passed with warnings denied | Workspace and test targets |
| UI production build | TypeScript and Vite passed | Bundled React/Cytoscape/ECharts/MapLibre assets, map worker and licensed Inter font |
| Browser workflows | Six passed against real Rust commands | Seed, transaction correction/acceptance, merge/reversal, local map, report snapshot and persistence; no external browser requests; no uncaught page errors. Empty-workspace authoring adds two namesakes, reviews cited observations, records separate/deferred decisions, corrects an observation and checks persistence. Statement workflows verify delimiter/mapping preview, saved profiles, original cells, duplicate rejection, invalid rows and delayed-response invalidation |
| Accessibility and keyboard | Eight tested section states and both transaction review layouts have zero axe violations; keyboard and minimum-width checks passed | Persistent desktop review, compact native modal, draft retention through resize, out-of-filter selection, nested source anchors, Escape, return to opener, 960×640 layout; remaining manual checks in `design/accessibility-results.json` |
| Native desktop build | Windows x64, macOS Apple Silicon and macOS Intel source builds passed in GitHub CI at `b6f80f0`; local Apple Silicon development app bundle built | Not signed/notarized product distributions; clean offline installation remains unverified |
| Java engines | Tika synthetic text parse and five Lucene queries passed on Java 21.0.12.1 | Boolean, phrase, proximity, fuzzy, fielded queries; revision returned |
| Rust → confined Lucene | Phrase and fuzzy query returned expected evidence and revision; native app phrase search returned the expected synthetic source | Staged app-local Java, local index, macOS development profile |
| Hostile Java development probe | Passed after dyld profile repair | Control worker could access sentinels/network; confined worker could not; assigned job I/O still worked |
| Python engines | 3 tests passed | Exact DuckDB/Parquet totals with record IDs; reviewed NetworkX paths; spaCy phrase candidates |
| Direct website collection | One public documentation page retained with original SHA-256 and static text | `https://example.com/`, 2 requests including robots policy; no search provider; not broad-web coverage |
| Complete release gate | Unpassed | Explicit machine-readable gates in `release-gates.json` |

The Java hostile probe exercises disposable synthetic sentinels only. It does not establish confinement for Python, Tesseract, Chromium, all JVM/native libraries, descendant processes or every supported OS version.

The source CI matrix targets Windows x64 and both Mac architectures. The native jobs initially passed in [the initial source run](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/35717611878). Its Ubuntu browser job could not launch Chromium under the hosted AppArmor policy. Browser verification now uses macOS with Chromium sandboxing still enabled. A later browser run exposed an intermittent identity-history assertion: tests now require the saved-history region, and decisions require a comparison at the current workspace revision. A deliberately delayed real comparison response verifies the stale-state guard.

CI source compilation is separate from clean offline installation, Windows 11 runtime acceptance, signed helper validation and downloadable artifact testing. Consult [the current pull request checks](https://github.com/Venin-Client-Systems/entity-workbench/pull/1/checks) for verification of the latest changes; successful earlier builds do not pre-claim current CI success.

The five collector unit tests use a synthetic transport script. They verify state-machine outcomes and bounded link expansion without any live network access. They are not evidence of live broad-web coverage, DNS behaviour or end-to-end source discovery.

No performance claim is made. The required 16 GB large-corpus benchmark has not been run. A locally fast synthetic test is not a substitute.

## Editable design pass

Real Figma Design frames, styles and component sets were created and exported. The first implementation applies the native foundations to the working UI; [handoff](design/HANDOFF.md) records frame links, comparisons, measured contrast pairs and remaining discrepancies. Final visual approval remains open.

The rebuilt Apple Silicon development application was launched with the new palette, bundled font and persistent transaction panel. Native source inspection returned the preserved CSV amount and exact row anchor. WebKit initially returned focus to the document when the source dialog closed; after the explicit focus fix, native accessibility readback showed focus returning to the source-inspection button.

The [complete pre-design CI run](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/35719487969) passed all four jobs at `b6f80f0`. Subsequent design changes have passed the local UI workflows, build and native inspection; their hosted checks remain independently visible on the pull request.

## Industrial design revision

The owner-selected industrial direction is implemented from native Figma frame `6:2`, with a graphite/amber palette, joined metric strips, square controls, compact ledger spacing and locally bundled JetBrains Mono. The revised UI production build, both browser workflows, all 34 Rust tests, strict Clippy and formatting checks pass locally. The eight section states and both review layouts retain zero automated axe violations. Ten declared text/background pairs meet 4.5:1; the smallest measured ratio is 5.30:1. The workflow tests retain their no-external-request and no-page-error assertions.

The Apple Silicon development bundle was rebuilt and launched. The revised native ledger and inspector display the original CSV amount and anchor; closing source inspection with Escape returns focus to the source button. Native inspection identified default WebKit select sizing, which is corrected with explicit control appearance while retaining the system option menu. These are development checks, not signed distribution or clean-install approval. Updated synthetic exports, image hashes and the exact remaining design scope are in the [current handoff](design/HANDOFF.md). Latest hosted verification remains visible on the pull request.

## Reusable statement mappings

Nine new Rust integration tests cover exact signed and separate debit/credit amounts, comma/dot decimals and strict grouping, explicit date formats, leading-zero accounts, multiline logical CSV anchors, profile reuse, overlapping imports, unchanged original bytes, whole-file rejection including errors beyond the displayed sample, stale/modified preview binding, and tab-separated BOM input with source-linked observations. The full suite is 43 tests. Strict Clippy, formatting and the production UI build pass. Java and Python adapters were unchanged.

Four browser workflows pass against real Rust commands. The new statement workflows add zero-violation axe checks for mapping and preview at 1440×1000 and 960×640, reachable import actions without horizontal page overflow, focus on the preview heading, preserved source inspection, saved-profile persistence, duplicate-original rejection, invalid-row blocking and a deliberately delayed real preview discarded after the analyst edits the mapping. No external requests or uncaught page errors occurred in the successful mapped-import flow. Screenshots and checksums are in [the statement design handoff](design/STATEMENT-IMPORT.md).

The Apple Silicon development bundle was rebuilt and launched. The existing synthetic workspace advanced from revision 7 to 8 through the guarded schema upgrade. A native semicolon statement with day-first dates, comma decimals, newest-first rows and fixed textual account/currency values imported three pending transactions at revision 9. The preview showed zero invalid rows and zero balance mismatches. Native source inspection returned `12,30` at logical CSV row 3, column `Paid out`, while the retained text correctly displayed the multiline description on separate physical lines. Escape restored focus to the source-inspection button. This is local development evidence, not clean-install or signed-distribution approval.

The v1→v2 tests verify that a backup contains both the original schema database and byte-identical referenced evidence. An injected SQLite event failure rolls back the version and revision while retaining a usable backup; restoring the v1 backup preserves evidence and then upgrades the restored workspace. Abrupt process termination and future migration paths remain unverified.

## Assessment authoring and explicit review

Six new Rust integration tests exercise question creation and revision, finding/question links, supporting and contradictory citations, explicit review decisions, pending-record rejection, stale writes, citation and field validation, altered-original rejection, correction propagation and preserved HTML snapshots. Version 2 migration tests retain the legacy database and originals, reopen legacy findings, preserve prior snapshots, test failed-migration rollback of finding status, and restore a usable workspace. The full core suite is 49 tests on this Unix development host; the existing Unix-only symlink test is omitted on Windows. Strict Clippy and formatting checks pass.

Six browser workflows pass against real Rust commands. The assessment flows author questions and findings, inspect both opposing source anchors, mark a finding reviewed, download a report, correct an observation, reject re-review of the pending correction, edit the finding and question, inspect decision history and verify that the prior report is unchanged. A concurrent import makes a stale question save fail without losing the draft or overwriting newer records. Four assessment states at 1440×1000 and 960×1000 have zero automated axe violations, with no horizontal modal overflow, external requests or uncaught page errors. The edit-flow check exposed ambiguous wrapped textarea labels when editing existing values; explicit accessible field names now make those controls stable. No test timeout increase was needed.

The editable Figma finding-review specimen, source export, application screenshots and remaining design scope are recorded in [the assessment handoff](design/ASSESSMENT-REVIEW.md). Java/Python engines and confinement policies were unchanged. Full workflow, packaging, sandbox, broad-web coverage and signed-release gates remain open. Hosted checks for this increment must be assessed on its pull request; earlier successful source builds are not evidence for a new commit.

The Apple Silicon development app was rebuilt and launched against its existing synthetic workspace. Opening advanced revision 9 to 10 through the guarded schema upgrade. Native finding review inspected `statement.csv`, row 10, column `amount`, returning the preserved `-180.00` value; Escape restored focus to the source button. Reviewing the pending transaction citation failed visibly while retaining the reason. Editing the finding then saved its revised limitations and reason at revision 11. Native development validation does not establish clean offline installation or a signed release.

The final rebuilt native app retained the revised finding after restart. Saving another recorded edit advanced to revision 12 and returned keyboard focus to **Review finding**; the fix defers focus restoration until the form's DOM changes have completed. Saving a native report snapshot then advanced the workspace to revision 13 while the snapshot correctly identifies source revision 12. The final six browser workflows also verify this editor-to-register focus path.

## Delivery tooling — EW-01

The offline runtime inventory verifier has 25 synthetic test methods. Local Apple Silicon: 24 passed, one Windows-junction check skipped as platform-specific. Tests exercise all three target policies, missing OCR/region assets, tampering, unsafe paths, links, malformed declarations, bounded errors and fail-closed behaviour under `python -O`. Native CI runs the same suite; source CI is distinct from installed-artifact acceptance.

The actual Java/Lucene staging inventory hashes 269 files / 168,205,077 bytes and fails completeness with 21 missing required components and the unlisted development marker. See the [sanitized negative report](delivery/evidence/runtime-inventory-2026-09-23.json) and [interpretation/limitations](../packaging/README.md). The tested directory is only the engine staging area, not the whole application. No release gate has changed.

The first Windows inventory run exposed zero identity/link fields from `DirEntry.stat`, which rejected ordinary files. The verifier now uses `os.stat(..., follow_symlinks=False)` to obtain real metadata without following name-surrogate reparse points. The existing positive inventories and hardlink/junction tests cover this regression on Windows. This follows the [Python filesystem API contract](https://docs.python.org/3/library/os.html#os.DirEntry.stat); the failed CI run remains available in the PR history.

The Windows rerun also exposed inconsistent path-versus-handle timestamp observations. Identity/size/link checks now bind the pathname to its opened handle; concurrent-change detection compares descriptor metadata before and after hashing. Regression tests cover differing path timestamps and a real file mutation during hashing. Exact manifest byte hashes remain mandatory.

## Acceptance evidence tooling — EW-02 — 2026-09-24

The ledger now maps twelve gates and ten scenarios to owner issues, required platforms and versioned procedures. Ten synthetic fixture files are hashed and tied to scenario assertions. Three published schemas describe the ledger, observations and review receipts. The checker verifies retained attachment/receipt bytes and exact source, artifact, policy and fixture bindings; it never executes attached data or procedure strings.

Local verification ran 51 Python tooling tests: 50 passed and the Windows-only runtime-junction test skipped. Twenty-six new release-evidence tests cover missing/stale/corrupt evidence, review binding and chronology, unsupported schemas, source-CI substitution, invalid OS versions, required minimum-version coverage, separate outcomes across OS versions, negative outcomes across policy changes and fail-closed behavior under Python optimization. All 26 also passed in a separate reviewer run. The actual `--check-policy` command reports a valid incomplete ledger, no candidate bytes verified, no accepted gates and twelve unpassed gates.

Cross-review identified and corrected vacuous platform policies, unbound review declarations, stale procedure/policy/fixture acceptance, hidden rejected failures and cross-OS failure suppression. Required OS support minima remain unknown and therefore block qualification. Review receipts prove consistency of retained declarations, not reviewer identity or the truth of an execution report. The [evidence guide](release-evidence/README.md) documents that trust boundary and the proposed 7/30-day freshness policy. Exact native CI results belong to this change's PR; earlier runs do not establish its portability.

The source run at `a819953355de25e06225e0fb23f3514ae91c81e4` passed [all four hosted jobs](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/35979007393): Windows Server source compilation, both Mac architectures and browser workflows. Separately, the discovery head `a0c414e3` passed [its four jobs](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/35978782990), and readiness head `12ff09f3` passed [its four jobs](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/35978875447). A local combined tree at `a3ebf29f80b9b0eb164ca230049592424966128e` ran 107 tooling tests: 104 passed and three Windows-only junction checks skipped. These are exact-revision source observations; later commits require their own checks. No installed-artifact release acceptance is inferred.

## Acquisition receipts — EW-05, 2026-09-24

The core suite now passes 67 tests on the current Mac: 25 unit and 42 integration tests. Nineteen new synthetic tests replace the old single acquisition test, covering complete empty/unsupported/error bodies, truncated streams without false hashes, redirect/link ancestry, charged failures versus refused reservations, exact budget accounting, duplicate bodies in both import orders, failed storage and final-transaction rollback, legacy/interrupted receipt absence, restored response-only originals, immutable export snapshots, malformed/future receipts, explicit elapsed-time overruns, and unsafe/dangling paths. A Windows-specific export-junction regression is included for hosted Windows execution; its result is not inferred from Mac tests. Strict Clippy, formatting and schema generation passed. Historical command v3 and workspace v3 schemas are byte-unchanged; command v4 and receipt v1 are additive.

The production UI build and all six real-Rust browser workflows passed. No visual controls changed. Collection receipt inspection/export currently use Rust dispatcher commands; designed review controls remain outstanding. The existing Vite large-bundle warning remains; this is not a performance benchmark.

A separate live smoke check on 24 September used the production broker with the explicit seed `https://example.com/`, zero expansion hops, two requests and 30 seconds. The robots response returned HTTP 404, and the page returned HTTP 200. Both complete responses contained identical 559-byte bodies: storage retained one SHA-256 original and both request/acquisition records, then accepted the page's static text derivative. The receipt recorded `live`, `successful`, two attempts and 1,180 elapsed milliseconds. Local export inspection verified its manifest hash and each exported original's hash and size. Raw bodies, receipts and export artifacts remain in ignored local evidence storage; no third-party page body is published here. This is not a benchmark campaign, terms approval, independent relevance label, in-app discovery-chain demonstration or broad-web coverage evidence.

The [receipt contract and limitations](discovery/ACQUISITION-RECEIPTS.md) distinguish received-original retention from successful content extraction, and application version from verified build identity. All twelve complete-release gates remain unpassed. Windows native-source verification and current PR checks must be assessed on the published head; this local result does not pre-claim those checks.


## Mac worker supervision — EW-04, 2026-09-24

The actual Rust Java/Lucene launcher now assigns private jobs, gives search workers read-only index access, clears inherited environment/descriptors, and supervises process groups, CPU/wall/file/descriptor limits and bounded outputs. Strict index acknowledgement validation precedes revision publication. Invalid derivatives trigger safe rebuilding, and explicit scratch cleanup rejects results when removal cannot be verified. A separate code peer review found and drove fixes to acknowledgement validation, damaged-index recovery and discarded cleanup errors; that review is not independent release/security sign-off.

At source `b8f31acec8823da78fb69908d60d09d7997f877e`, 55 ordinary core tests passed and two runtime-dependent tests were explicitly ignored in the ordinary suite. All seven native supervisor tests passed separately on macOS 26.6.2 arm64 with Java 21.0.12.1 and Lucene 10.5.1. The clean-source report binds source/runtime hashes and records `complete_release: false`; its SHA-256 is `22537fcb5db299620fba19a8a852537590d3412b4c9ebfe0833d6390ef0e04a4`. Raw diagnostics and exact runtime evidence remain local. Strict Clippy, formatting, Python compilation and staged public audits passed.

An unconstrained synthetic control proved the hostile actions work. The confined worker denied other-workspace/sibling reads, original/input writes, direct networking, child spawning, inherited environment and profile access; assigned work succeeded. Native Lucene checks cover Boolean, phrase, proximity, fuzzy and fielded queries, failed-query cleanup, failed-rebuild invalidation, and recovery from symlink/oversized/hardlinked index poison. Other probes exercise inherited descriptors, group timeout cleanup, oversized output and permission-restricted scratch while preserving outside targets.

The [development supervisor review](security/MACOS-WORKERS.md) documents access, unsafe-code invariants, reproducible probes and limitations. This uses deprecated `sandbox-exec`, not the signed-helper release design. The tree budget is monitored rather than a hard filesystem quota, and Java heap size is not an OS RSS limit. Intel runtime execution, signed helpers, other worker families, clean installation, crash cleanup and downloaded artifacts remain unverified. No release gate passed. Current hosted source checks belong to this PR's actual head.


The first Windows receipt run passed the new junction-rejection check but failed the positive export test with access denied. Export publication now closes the synced manifest handle before renaming its containing staging directory. The same positive snapshot test remains mandatory on Windows; a passing negative path test alone does not establish working export. The failed run is retained in PR history, and the corrected head requires a fresh hosted result.

## Integrated collection review, parser and durable jobs — 24 September 2026

Combined source `aacee9435b97d45e57bf4512a92de14bd8674a8f` passes 89 ordinary Rust tests on the current Mac (47 unit and 42 integration; five native-dependent cases excluded from this run), TypeScript/Vite build, and the 107-test delivery-tool suite (104 passed, three Windows-only skips). The collection increment passed 13 browser workflows against real Rust commands. Its two native editable Figma frames, compact/desktop axe results, screenshots and hashes are recorded in [the collection handoff](design/COLLECTION-REVIEW.md).

All twelve parser/search native tests passed separately at that clean combined revision on macOS 26.6.2 arm64 with staged Java 21. The actual confined PDF coordinator also completed, published an immutable derivative and restored its job, derivative and original from backup. The extraction remained unreviewed and did not replace the original's accepted text. Exact runtime/source hashes and raw synthetic diagnostics are retained locally; no case data or third-party source bodies are included in public fixtures.

Thirteen new durable-job tests cover idempotent queue keys, a two-worker limit, cancellation/success and shutdown/registration races, stale cancellation before a retry is claimed, bounded retries, database rollback after derivative insertion, replay without duplication, altered originals, exclusive coordinator ownership, interruption, cleanup-error precedence and backup/restore. The native desktop uses an explicit exit callback because the runtime may terminate the process without Rust destructors. Failed recovery does not claim that an orphaned worker was reaped. The [job contract](processing/JOBS.md) records remaining execution and presentation scope.

The cumulative changes are reviewed in [PR #1](https://github.com/Venin-Client-Systems/entity-workbench/pull/1); earlier source commits and checks remain in history. Its [combined hosted run](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/35994479583) must be assessed for the exact head. Main-branch review remains required. Source compilation, hosted browser checks and development confinement do not qualify installed artifacts or any of the twelve complete-release gates.


## Designed document review and app-local OCR — 24 September 2026

Combined clean source `304b127182563abf451e8a1f7d163644b938a70e` passes 98 ordinary Mac core tests (56 unit and 42 integration), strict Clippy, native-host compilation and the UI production build. Eight native-dependent cases are excluded from that ordinary run. The document UI increment independently passed all 23 real-core browser workflows at `daddd6bb76d563feb0e360ff060c86663a889642`, including ten new document flows; combined hosted results require the newly published head's own checks. The [design handoff](design/DOCUMENT-JOBS.md) retains three editable Figma frames, fourteen design/application images and five zero-violation axe observations.

At the clean combined revision, all thirteen parser/search boundary checks passed, all eight OCR contract/native checks passed and the native coordinator PDF publication/backup-restore case passed separately. OCR used a relocated 13 MB development Tesseract bundle with its exact native-library inventory, pinned English model and retained third-party notices. Hostile native probes denied other-worker reads, original/input mutation, direct networking, process creation and inherited state while assigned recognition succeeded. A blank image was reported separately from recognized text. Three portable tests verify that inventory rejection, compiler failure and timeouts replace stale success with retained failure observations; source CI now runs these explicitly. General image/PDF decoding, word regions, original-page mapping and recognition accuracy remain unverified.

The fresh Apple Silicon `.app` was built with the local Java/parser/Lucene runtime and English OCR assets. Through its native file chooser and document controls, the 650-byte synthetic PDF was imported, queued, shown running and published as one partial derivative by PDFBox 3.0.8. The review showed the expected synthetic text, matching original digest, distinct result digest and explicit no-source-anchors, no-OCR and embedded-document limitations. A normal quit and restart preserved the same job ID, attempt, derivative, text and hashes. The operator observation binds the executable, fixture and native-test reports; its retained SHA-256 is `101fdc1dee6f2c683c4ae22e5b8bee3eb99177914d89f2ec7782da159451cb22`. This confirms one native development workflow, not a clean-machine installation, application-wide network-traffic measurement or signed/notarized release.

Peer review found and corrected cancellation acknowledgement wording when the canonical outcome is failed/unverified, and request-key loss when leaving Evidence after an uncertain acknowledgement. Regression tests use actual canonical jobs, including a terminal transition before remount. No failed or unverified worker is labelled safely cancelled, and the open application reuses the original uncertain request key. Recovery across a full application restart still requires checking the durable ledger. Unknown worker termination now suspends new processing instead of silently continuing; verified OS-process recovery remains outstanding.

The [main review](https://github.com/Venin-Client-Systems/entity-workbench/pull/1) remains subject to its required human review. All twelve complete-release gates remain unpassed. The separate Windows development branch retains its actual failed startup observations and does not enable core worker execution until compatibility and confinement have been demonstrated.


The first combined Windows source run at `827e8606ef90f0583255ff45ba735d9345467381` exposed a verifier defect: timestamp-only OCR history names collided under the host clock, so a failed observation overwrote its predecessor. History files now use UUID suffixes with exclusive creation. A deterministic fixed-clock regression requires both success and failure records to survive and the latest projection to show failure. The portable failure-evidence suite now has four passing local tests; Windows rerun remains required. The failure was in evidence retention and is not evidence of Windows OCR runtime execution.


Report snapshot publication now revalidates the export directory at save time, creates a private staging file, closes and publishes the complete bytes without overwriting an existing name, and removes only this operation's files if canonical publication fails. Three local regression cases pass: failed database insertion preserves the previous export and revision; canonical/export hashes survive backup/restore; and a replaced symlink directory receives no writes or permission changes. Existing assessment and escaped-report integration tests and strict Clippy pass. A Windows junction counterpart is prepared for hosted execution. Two bounded code reviews found no further actionable defect. This does not claim crash-atomic filesystem/database publication, protection from a malicious concurrent same-user process, or complete Windows workspace ACL enforcement.


A subsequent recovery review found that an orphaned running claim was labelled interrupted and could be manually retried without proof that its prior OS process had exited. Coordinator startup now maps such claims to `worker_exit_unverified` and atomically suspends pending work. Earlier development orphan records with a cleared lease are treated the same way. A joined, confirmed shutdown remains distinct and retryable. Thirteen processing tests pass, including new legacy-state and transactional-suspension regressions; coordinator tests and strict Clippy pass. Recovery does not infer termination from a CPU limit or from the disappearance of the parent coordinator. Verified process recovery and releasing a suspension remain unimplemented.


## Integrated transaction patterns and image OCR review — 24 September 2026

Clean source `8819d8fad6496aa00b4ec6342e1ea1145b729f4d` integrates the transaction-pattern domain/review, separate bounded PNG/JPEG worker and durable image OCR queue. The combined local suite passes **125 ordinary Rust tests**, strict Clippy, native-host compilation, production UI build and **35 browser workflows**. Twelve runtime-dependent tests remain excluded from the ordinary Rust run. The browser tests use real Rust commands and fixed synthetic fixtures; they do not claim those fixture results were produced by an OS worker.

Separately at that clean revision, all **13 coordinator/publication checks** and **six image engine/boundary checks** pass using the combined application staging directory. Actual PNG and JPEG recognition publishes immutable original/raster/result provenance and survives backup/restore; the legacy PDF parser coordinator still passes. Unsupported formats, animation/multiple-image containers, malformed images, excessive dimensions/pixels and worker output limits remain distinct rejection cases. Image and document work share the bounded executor pool. A caught adapter panic now yields unverified worker exit and suspends the queue instead of claiming that destructor cleanup proved termination.

Transaction review supports exact description totals, cash/refund candidates and explained weekly/fortnightly/calendar-month recurring debits, preserving repeated purchases, separate currencies, review denominators and source IDs/versions. Optional transfer exclusion requires reciprocal accepted equal/opposite same-currency counterparts; out-of-scope counterpart originals are checked. Peer review reproduced silent decimal rounding in parsing and arithmetic plus noncanonical date acceptance. Shared exact-money helpers now refuse unrepresentable results, and canonical dates are required before lexical filtering. Eleven transaction integration cases cover these and the remaining calculation boundaries. Period comparison, account-flow exploration and large-workspace benchmarks remain outside this increment.

The [transaction design handoff](design/TRANSACTION-PATTERNS.md) and [image OCR design handoff](design/IMAGE-OCR-REVIEW.md) bind editable Figma frames to application comparisons and scoped axe observations. OCR review shows separate original/raster/result hashes, explicit unsupported/failed/quota/empty/recognized states and inert unreviewed text. Whitespace-only empty output is preserved canonically but disables Copy. Previous attempts remain immutable. The normalized raster is validated in memory and discarded; retained provenance is not a page exhibit or verified source anchor.

The previous published source revision `58c4d0dcaa01a3c2fcd33bd5ca6d0d50ee181b05` passed [all four hosted source jobs](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/35999229379), including the Windows report-export junction and positive-publication checks and UUID evidence-history correction. Those results apply to that revision; this larger increment requires its own hosted checks. Main review and all twelve complete-release gates remain open.


The Apple Silicon development app was rebuilt at that revision with the combined Java/parser/Lucene/image runtime and English OCR assets. A native chooser import retained the 14,671-byte synthetic PNG; the queue visibly progressed through Running to Completed and published one immutable OCR derivative. Recognition matched the fixture's synthetic reference and amount. The review showed the expected 1200×230 raster, distinct original/raster/result digests and explicit unreviewed/no-anchor/discarded-raster limitations. A normal quit/restart retained the same job, attempt, extraction, text and hashes at workspace revision 23, and Escape restored focus through the job to the ledger.

Native transaction analysis at revision 23 kept 12 pending AUD rows and one pending USD row out of reviewed totals, while preserving both currency denominators. Source drillthrough reached the original statement row 8 amount `-100.00`. No review decision was created during this inspection. The retained operator report binds the executable, fixture and two native test reports; SHA-256 `63edbd44f58d68167c7cd1cb605a6ad2a0ba451e3bcfedc104859f5ef6c20237`. This is one native development workflow, not a clean install, traffic audit, OCR accuracy benchmark or signed release.


The first hosted Windows run of the integrated image/transaction branch passed its ordinary Rust tests but failed strict Clippy in a Windows-only corruption-fixture helper. The helper intentionally clears the DOS read-only attribute on an owned synthetic original before testing tamper detection; Unix already uses explicit owner-only permissions. A narrowly scoped Windows-only lint allowance now documents that invariant in both transaction test helpers. Production permissions are unchanged. The failed run remains retained, and the revised source head requires a new Windows check.


## Exact period comparison and Python precision repair — 24 September 2026

Source `18610796a6becc3756f10c12ce3eb09018f972f8` passed [all four hosted source jobs](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36004263460), including the repaired Windows corruption-fixture lint and both native Mac architectures. These are source builds, not installed-artifact acceptance.

The next increment adds the [editable period-comparison design and working review](design/TRANSACTION-COMPARISON.md). Rust reads two explicitly selected nonoverlapping inclusive periods at one workspace revision, preserving account/currency groups, exact deltas and rational relative changes. Unequal durations, reversed chronological selection, zero/negative baselines, review partitions, transfer treatment and missing coverage remain explicit. The public command is v8; historical schemas remain unchanged. The implementation branch passed 135 ordinary Rust tests, strict Clippy, production build and all 42 real-core browser workflows, including seven new comparison cases and four zero-violation scoped axe checks.

Integration initially exposed a stale local development executable: the seven comparison tests received an unknown-command error while the seven pattern tests passed. The current Rust harness was then rebuilt and all 14 targeted workflows passed. The npm browser-test command now builds the locked current harness automatically before launching tests, preventing that local source/executable mismatch. The failed observation is retained in ignored integration artifacts; it is not relabelled as a passing run.

The native Apple Silicon development app built from clean source `b78ff38a025c1feab9966b955e18d0ab60a3b4b8` passed manual accessibility interaction at workspace revision 23. Baseline 1–5 March 2025 and comparison 6–31 March showed 9 and 4 rows over 5 and 26 days, four separate account/currency groups, and the unequal-duration warning. All 13 synthetic rows remained pending and accepted totals were correctly zero; empty periods had a different explanation. Pending drillthrough reached statement row 8 and exact original amount `-100.00` AUD. Handoff focused the review close button; Escape returned to the ledger row. Refresh made the result stale and disabled source actions; explicit recalculation restored current status without changing the workspace revision. The retained operator report binds the executable and observations. This does not establish clean installation, live traffic auditing, screen-reader or native behavior on the other targets.

A separate review reproduced silent rounding in the prototype Python adapter's fixed `DECIMAL(38,8)` conversion: `1.123456789` became `1.12345679`. The adapter now constructs exact Arrow Decimal128 values at a per-currency scale of 8–28, rejects incompatible magnitudes and checks aggregate precision as well as the engine's integer overflow. An unverified transfer pointer now blocks the prototype operation instead of silently removing money. Rust's canonical transfer rules remain authoritative; this Python adapter is not enabled in the application.

All 22 local Python tests pass at that clean revision with DuckDB 1.5.0 and PyArrow 23.0.1. Independent review also compared five scales with integer reference sums. Peer review demonstrated that declared Parquet page size does not bound dictionary-expanded Arrow/Python memory. The reader now preserves dictionary encoding while checking per-field lengths and converts bounded batches into Python objects; a new repeated-dictionary regression passes. Native memory/time enforcement, packaged execution and canonical snapshot/protocol integration remain required. The [adapter contract](../workers/python/README.md) states these limits. Local report SHA-256: `415501556203ce134da2819ac2df24148d6e8fa96ef2683c57498f5ef4a4d9a1`.


## Selected-page PDF rendering foundation — 24 September 2026

Source `bc9d9716ffa78c8b3bb6a4fe562dffffc4499199` passed [all four hosted source jobs](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36007428086), including the comparison UI and exact Python adapter repair. Those checks do not cover the following renderer increment.

The integrated [scan-focused PDF renderer](security/PDF-RENDER-WORKERS.md) runs in its own disposable Java process and classpath. One explicitly chosen page and integer DPI produce a bounded grayscale raster; a separate OCR process receives the exact raster hash. Original, selected page, available page count, crop, rotation, actual affine, dimensions and raster identity are validated together. Encrypted, unsupported, failed and exhausted-limit outcomes remain distinct. No canonical PDF job, page-selection interface, accepted fact or word-region anchor is claimed by this engine increment.

Peer review reproduced three PDFBox fallback behaviors that could silently omit or reinterpret content: PostScript XObjects, malformed drawing operands and invalid PNG predictor selectors. The adapter now rejects those cases and unfinished operand lists, with explicit shared operand validation and bounded operator counting. Pixel tests also exposed a transform error for fractional boxes crossing zero. Capturing the renderer's actual float-rounded transform and enabling exact double JSON round trips preserves the correct geometry. Regression fixtures retain these findings rather than accepting partial renderings.

Clean integrated source `97ff1b7a74645b8f63454024093fbeff05e89424` passed 138 ordinary Rust tests (17 platform/runtime cases explicitly ignored in that command), strict Clippy, all 42 real-core browser workflows, four portable PDF evidence tests, and all eight separately invoked PDF native/contract tests with the combined staged Java and English OCR runtime. The native report records macOS 26.6.2 arm64 and exact source, runtime and 35 synthetic fixture hashes; report SHA-256 is `4280b873b50c3a4a4663ccc43e425d36578b18050378ce06cb1614f56734c034`. Its failure recorder is included in the three-target source CI matrix.

This is the experimental Mac confinement mechanism, not the signed-helper release design. General PDF fonts/graphics, canonical PDF queue/review, word regions, Intel/Windows native runtime tests, hard aggregate disk/RSS bounds, supervisor-crash recovery and clean installed packages remain outstanding. Hosted verification of the new source head is separate. All twelve complete-release gates remain unpassed.


## HTML calculation drillthrough — 24 September 2026

The PDF foundation source `def357bfa34da534d410b023dad56a3335a9711a` passed [all four hosted source jobs](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36008765721). The following narrow reporting change adds direct links from each currency calculation to its included transactions and excluded matched-transfer records. Complete accepted/pending/rejected/deferred partitions retain excluded denominators. Exhibit rows expose the saved transaction version, readable source locations and expandable exact anchor JSON. The existing calculation rules and immutable report storage are unchanged.

Two real-workspace regression tests independently parse the exported HTML and verify exact mixed-currency totals, repeated purchases, transfer partitions, all review states, unique/resolving fragment links, inert hostile descriptions, lossless anchor JSON, row versions, empty/pending-only cases, unchanged originals and preserved prior snapshots after correction. Both pass. The broader core suite passed 140 ordinary tests with 17 native/runtime tests explicitly ignored, strict Clippy passed, and four affected real-core assessment/investigation browser workflows passed. Subsequent source-label formatting changes passed the focused two tests and strict Clippy again.

Manual browser inspection of a generated synthetic snapshot followed the excluded-transfer link to its version-3 transaction. Long anchor JSON initially compressed adjacent table columns; readable location links and bounded expandable anchor data corrected that observed layout. The final rendered sample retains HTML SHA-256 `7e375bc59ce1b5453db55169c0e957aceca24ddcba05eddad81ba0da761182d0`. This is a correctness/readability improvement to the existing report prototype, not final report design or DOCX acceptance. Expanded record lists increase output size; 100,000-row browser memory and rendering remain unbenchmarked. Independent code review found no actionable blocker in the calculation-link change.


## Durable PDF page OCR and native review — 24 September 2026

The canonical queue now accepts an explicitly selected PDF page and bounded integer DPI, using command v9, document-job v3 and a separate PDF-extraction v1 contract. A request key binds original, method, page and DPI; document/image/PDF work shares the two-worker limit. Separate renderer and OCR processes publish one validated immutable derivative with original/raster/result identities, exact geometry, worker identities and explicit incomplete/blocked outcomes. Text remains unreviewed and is not silently accepted or indexed. Cancellation, stale attempts, interrupted ownership and unknown process exit retain the existing fail-closed queue behavior. The [job contract](processing/PDF-JOBS.md) documents this scope.

Clean core integration `4608ece605c4fcddfc80051c92474386112c8005` passed all **nine native PDF job/publication tests** using the combined app staging on macOS 26.6.2 arm64. The exact source/runtime/fixture report SHA-256 is `1eb308c50c1f8ecfe1437a5b19d9c0ae5b20c9d3e605af3127f4f9e58bb98490`. Publication, retry/attempt identity, invalid returned results, originals-aware backup/restore and separate worker outcomes are exercised. Four portable failure-evidence cases run in source CI.

The integrated UI at `07ee9cf` passes **148 ordinary Rust tests**, strict Clippy and the production/native development build; 18 native/runtime cases are explicitly excluded from the ordinary suite. The real-core browser suite passed **50/50** after correcting a collection-export test that focused Close before the real workspace refresh re-enabled it. The original combined run passed 49/50 and is retained locally. The repaired test deliberately delays that real refresh, checks disabled then enabled state, and verifies focus wrapping without fixed sleeps or fabricated responses. All eight PDF workflows passed in both combined runs. Five new PDF accessibility observations have zero axe violations; editable Figma frames, desktop/compact comparisons and checksummed images are in [the design handoff](design/PDF-PAGE-OCR.md).

The freshly built native app imported the 16,314-byte synthetic `scan.pdf` through its file chooser, queued page 1 of 2 at 144 DPI, and visibly moved from Running to Completed with one derivative. Review showed the expected synthetic text, a 1200×230 raster, CropBox `[0, 0, 600, 115]`, zero rotation and actual affine `[2, 0, 0, -2, 0, 230]`. A normal quit and restart preserved the same job, attempt, extraction, text and all three hashes without another attempt; workspace revision remained 27. The operator record binds the executable, source, fixture, runtime identities and native suite, SHA-256 `07b5a72a50f0fe2b60f09d6c3372091bfbcd2e8ef844d776e613ec0702b3b9e4`.

This establishes a native development workflow on the available Mac. General PDF features, word regions/accepted anchors, cross-platform specialist runtime execution, signed helper confinement, complete offline installation and release artifacts remain unfinished. These observations do not measure application-wide network traffic or qualify clean installed packages. All twelve complete-release gates remain unpassed.


## OCR word-region engine — 24 September 2026

The separate opt-in [word-region engine](security/OCR-WORD-REGIONS.md) returns bounded page/block/paragraph/line/word boxes relative to the assigned raster, exact word text and explicitly uncalibrated Tesseract confidence. A fixed recipe produces paired text/TSV outputs using the existing inventoried English runtime. Rust checks hierarchy, order, bounds, unique identities, finite scores, complete rows, text agreement and exact raster/output bindings. Existing text-only OCR, job and extraction schemas remain unchanged. This engine creates no accepted facts or original-document anchors and does not yet persist regions in canonical jobs.

Clean integrated source `2fa8737c318ff7538d3e1c06e9f0fad55b1b143a` passed **154 ordinary Rust tests** (20 native/runtime cases explicitly excluded), strict Clippy, five portable failure/history evidence cases and **eight native region/contract tests** on macOS 26.6.2 arm64. The native report binds source, runtime and synthetic fixtures, SHA-256 `5c99c38e80de1f613f57b9b047b00d917a5dd260e5ee4bc7f5512bbc57513af9`. It includes actual recognition with word boxes covering raster ink, blank input, malformed/oversized/truncated output, hostile access, direct networking, cancellation/reaping and output limits. The separately signed increment also passed all eight legacy native OCR checks, release/debug Clippy, unchanged schema generation and bounded independent code review.

Paired output validation cannot establish recognition accuracy or detect a worker that fabricates mutually consistent text and regions. Canonical retention, original-page mapping, reviewed anchors, UI and installed cross-platform execution remain subsequent work. The portable failure recorder is included in all three source-CI targets; source compilation does not qualify runtime confinement. No complete-release gate passes from this increment.


## Windows foundation, scale baseline and snapshot consistency — 25 September 2026

The designed PDF/region source `e62077dabc3c30d5ef8e3ba5369f90698b4e147b` passed [all four hosted source jobs](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36013001638). Later changes require their own hosted checks.

The Windows foundation is now integrated as a separate development crate. [Native run 36013049020](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36013049020) passed 29 native tests, all 18 combined permission/delivery predicates, exact worker ACL-rewrite denial with a successful unconfined positive control, separately identified host-injected empty-DACL inspection/cleanup, the remaining resource/hostile modes, and copied Java 21 assigned input/output. The actual tested source was PR merge `3fb76818687a127ae277ec3ed8ee320aea4f10a1`, with parents `e62077d` and `ad03ea0`; its tree matched the local merge before the evidence note. The sanitized report SHA-256 is `47aafad44f9de606f4fcb2de541b5afe72b70c3201ad7c478f75d39ceb088b36`. [Windows review details](security/WINDOWS-WORKERS.md) preserve all fifteen earlier failures and distinguish precise prevention from host-created recovery. The observed host is Windows Server 2022, not Windows 11 installation acceptance. Core Windows parser/search/OCR adapters remain disabled; no release gate passes.

The [100,000-transaction baseline](performance/OBSERVATION-2026-09-24.md) completed all six canonical operations with four samples each on the available 16 GiB Apple M1. All accepted/pending/rejected/deferred partitions and transfer source lists remained explainable. First calls ranged from about 270 ms for an empty analytical scope to 1,660 ms for HTML publication; the largest repeated HTML call was 1,752 ms. These small, cache-uncontrolled core samples exclude native IPC, JavaScript filtering/rendering, documents/OCR and concurrent maps/graphs. They do not qualify p95 or application responsiveness. The observation is retained unchanged at its measured source.

The separate [payload diagnostic](performance/PAYLOAD-DIAGNOSTIC.md) measured an actual View response of 83,986,629 bytes, rising to 198,449,390 after one saved HTML snapshot. Transactions consume 47,110,803 serialized bytes, review decisions 25,568,901 and the one-report array 114,462,763. This motivates narrower revision-bound reads and explicit detail retrieval; no transaction, decision or old report is deleted. The generated HTML itself is 113,592,530 bytes, and its measurement child's peak RSS includes restore/readback/oracle allocations. Browser performance at that size remains unmeasured.

Review also reproduced a correctness defect in whole-workspace reads: revision and record tables were read in separate implicit transactions. A second connection could commit a source between those reads, placing later content under an older revision in both UI responses and saved reports. `Workspace::view` now uses one SQLite read snapshot. Three deterministic tests failed before the fix; four pass afterward, covering a committed concurrent writer in WAL mode, blocked-then-resumed writing with the normal DELETE journal, immutable report revision/content and transaction release after a failed decode. The full core suite passes **158 ordinary tests**, with 20 native/runtime tests explicitly excluded, plus strict Clippy. Independent bounded review found no further actionable issue. The fix may hold the existing read lock longer; large-workspace contention remains part of performance work.

After integration, all four affected real-core assessment/investigation browser workflows pass. The performance example passes four Rust checks and eight portable failure-evidence checks; strict Clippy includes the new example. The 100,000-row timing observation remains bound to its original source rather than being relabelled after the snapshot correction.


## Retained image regions — 25 September 2026

The opt-in image-region coordinator and private derivative store are integrated at `896df06`. [The contract](processing/IMAGE-REGIONS.md) adds command v10 and processing-job v4 without changing the historical schemas. Storage schema 4 has a backed-up transactional migration: an existing v3 finding retains its reviewed state, while legacy v1/v2 review invalidation remains. Reported recognition, raster and exact TSV are immutable derivative files with validated SQLite references. No observations or reviewed word anchors are inferred from OCR output.

The exact clean agent source `b6a702c0f7e65a7806f8f389f44bf73eb5af6f4c` passed **14 native retained-region checks** on Apple Silicon macOS 26.6.2, using the actual confined PNG/JPEG decoder and English OCR workers. Report SHA-256: `8165cb9abd6ee6c1545cb884d9a5d86b31b97f2858964f9336992835d119096c`. The tests cover canonical publication, retained raster/TSV/result identities, backup/restore, corrupt/missing/conflicting derivatives, cancellation, retry history, concurrent publication during a backup, failed migrations and incomplete manifests. An earlier fixture assertion expected eight words instead of the observed seven; its failed record is retained. The clean final run asserts the exact expected sequence.

The integrated source also passes **172 ordinary core tests**, with 21 native/runtime tests explicitly excluded, and strict Clippy. Four portable failure/history checks are included in source CI. Independent bounded review found no remaining blocker in this storage increment. Hosted Windows source testing is still required for its new positive recovery and junction cases; this Mac has no MinGW C compiler.

[Native editable Figma frames](design/IMAGE-OCR-REGIONS.md) define the desktop/compact inspector and workflow states. Two native exports are retained. Figma reported a connection issue during the final states export, so remote synchronization and that export remain unverified. Production region display is separate work in progress. Aggregate disk quota, orphan garbage collection, OCR accuracy, accepted anchors, Windows/Intel specialist runtime execution, signed helpers and complete installation acceptance remain open. All twelve release gates remain unpassed.


## Explicit report retrieval — 25 September 2026

The desktop now requests a report catalogue and explicitly fetches the selected immutable snapshot for export. Command v11 adds an ID/expected-digest read; Rust validates the stored identity and actual UTF-8 HTML hash, with a 256 MiB in-memory response bound. Legacy full workspace reads remain available. All mutation responses share the same domain implementation, and both read modes use one SQLite snapshot. Creating a report no longer allocates previous report bodies unused by its renderer.

The [measured isolated-copy check](performance/REPORT-PRESENTATION.md) at clean `de49ba64c89e54872196864383a44f5b615d14b2` reduced the 100,000-row, one-report response from **198,449,390 to 83,986,848 JSON bytes**. All non-report fields, 100,000 transactions and 95,100 decisions were identical. Explicit export returned the exact saved snapshot, and the original benchmark database hash remained unchanged. Observation SHA-256: `e43d34a59dd6bdef4dac18055b80d4c017f3ab78c236a60d72ebaba42e78df96`. This is a byte verification, not a timing or memory benchmark. Pagination and smaller report exports remain needed.

Combined retained-region and presentation source passes **176 ordinary Rust tests**, with 21 native/runtime tests excluded, strict Clippy, production UI build and **51/51 real-core browser workflows**. The new browser regression corrupts retained HTML after the catalogue loads, verifies an integrity error without a download, restores the original bytes and checks exact downloaded content on retry. Independent bounded review found no remaining blocker in the presentation read/dispatch scope. Earlier source `20895ce8c56ef6998a884dd0609ad51911d4293b` passed [all four hosted source jobs](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36014847349); newer changes require their own checks.

The [integrated Windows foundation repeat](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36014847360) also passed. Its actual checkout was PR merge `c1eee5dfa53989b39f7d2555d05d1484b9d4aa0c`, parents `b12271cc0905c5d6fb6d58b715a7e554eb976db4` and `20895ce8c56ef6998a884dd0609ad51911d4293b`; report SHA-256 remained `47aafad44f9de606f4fcb2de541b5afe72b70c3201ad7c478f75d39ceb088b36`. The repeat remains Windows Server 2022 development evidence, not Windows 11 installation acceptance. No complete-release gate changes.


## Native exports, coordinator shutdown and transaction pages — 25 September 2026

The actual Mac export check found a defect missed by Chromium browser tests: without a native download handler, the WebKit window cancelled HTML downloads. The development desktop now explicitly creates its configured window with a handler that accepts only local bundled-interface blob exports with known HTML/JSON names in the OS Downloads directory. The UI reports a saved filename only after native completion; rejection and missing completion are visible errors. Only event listen/unlisten permissions were added. Network downloads, arbitrary paths and event emission are not granted. Pending completion records expire after two minutes, so lost callbacks cannot permanently exhaust the eight-record tracker; an expired late event cannot confirm a different export. The tracker is not an aggregate download/disk quota.

The rebuilt native application with application source `c0c8285a2d31c1693acc498e82724c5d8840cf73` migrated the existing synthetic workspace from schema 3/revision 27 to schema 4/revision 28. Its migration backup has identical canonical records and all seven original checksums verify. HTML export and a collision-named second file both contain the original 15,026-byte snapshot with SHA-256 `790fc5ac4296af67af85a5ef5e71e0881aaf85208bcb8d63b6733a1f8780c10e`. Native JSON export contains all 13 exact canonical transactions; its SHA-256 is `b9710a51553cccda7beb0d07d941ce0496116eb01c706b6a6158ecac441aa221`. Both saved messages were observed. The operator evidence binds the executable/build log and discloses the test-only configuration change during compilation, SHA-256 `fdf60708ad527dd32231723374c3477bbe075342d08985fdc47fc5f59928424a`. Accessibility control timed out after HTML export; a fresh control session recovered the saved message. The application had no crash report and a read-only process sample showed its event loop waiting normally.

Three native policy/tracker unit tests and strict desktop Clippy pass. Five affected real-core browser workflows pass with an owned server. An earlier concurrent root run accidentally reused another worktree's development server and ended with connection refusals; that failed log is retained. The Playwright configuration now refuses an already occupied server instead of silently using a different source/workspace. Native export policy tests are included in source CI. Installed Windows/Intel export behavior still needs actual platform evidence.

A separate deterministic regression reproduced workspace restart contention: closing the coordinator's lock descriptor did not release a Unix lock still inherited by an unrelated fork. The coordinator now explicitly unlocks once **after all worker threads join**, serialized with shutdown. A real child holds the inherited descriptor across shutdown in the regression; no sleep/retry conceals contention. A second regression protects the replacement owner's lock from repeated shutdown. The original failure, before-fix full log and after-fix full log are separately retained. The repair is `fa5c704`; [the job contract](processing/JOBS.md) documents the ownership boundary.

The [transaction pagination backend](transactions/PAGINATION.md) is integrated at `d71313d`. Command v12 adds bounded revision/query-bound pages, stable date/sequence ordering, exact currency/account/date/review filters, all four review denominators and original/anchor-preserving rows. Pages contain at most 200 rows and 2 MiB of retained row bodies; oversized single rows fail explicitly. A correction invalidates continuation. This backend does not yet replace the UI's full transaction/review arrays, and SQLite still scans/sorts JSON records. No large-case responsiveness or release gate is claimed from adding this API.

Root integration at `d71313d` passes **186 ordinary core tests**, with 21 native/runtime tests excluded, and strict core Clippy. The previous published `f869892` source run passed the browser and both Mac jobs, but Windows strict Clippy rejected two read-only corruption-fixture resets. Narrow Windows-test-only lint allowances now document those resets at `2686e64`; the fresh hosted run remains pending. The native tests preceding that lint step passed.


## Native image-region review integration — 25 September 2026

Integrated source `3536859` passes **188 ordinary core tests**, with 21 native/runtime tests excluded, three desktop export-policy tests, strict core/desktop Clippy, the production UI build and **60/60 real-core browser workflows**. Current schema generation emits 24 schemas with no tracked byte changes. The final development-bridge disconnect guard is included in this combined browser run. Earlier failed runs remain retained separately.

The clean native application built at `0b4125e` completed the actual PNG decoder and English word-region OCR from the existing retained synthetic original. Its binary raster command displayed the verified 1200 × 230 raster and seven words. Selecting `0042` showed the exact rectangle (left 292, top 120, width 98, height 32) and raw score 15.542927; fit/100% viewing and the text-copy completion message were observed. The original SHA-256 is `410a92d6da102c53cac25fd450923057f717342df68d0a09dfc61d9f8296ef92`.

Normal quit and restart preserved all 55 canonical records byte-for-byte, the completed attempt and all three derivative objects. The 52 records present before the new job were unchanged. The executable SHA-256 is `af6055b38efd0cace9503657a533f30a53496dde35486fdc2d8b4bc5d4d72516`; the retained operator record binds the build log, original, job, derivative bytes and selected geometry, SHA-256 `420677505ed4226efd5720a3a7e1d1a35f8ae15f2bf208570d87f38167266221`. This establishes the development Tauri/WebKit binary-display path on the current Apple Silicon Mac. It is not OCR accuracy, accepted-source-anchor, application-wide network/memory or clean packaged-installation acceptance.

The first two hosted Windows Java engine attempts both failed at the initial parser operation. The second bounded post-termination diagnostic identifies a large dump-like scratch artifact; the underlying runtime cause is still under investigation. Engine adapters remain disabled, and resource/confinement limits have not been increased. The existing foundation predicates continue to pass; their success does not pass the engine or Windows 11 release gate.


## Bounded source review and measured transaction pages — 25 September 2026

[Command v13 source reads](transactions/SOURCE-READS.md) return at most 25 exact selected rows, in requested order, at the originating revision and recorded versions. The complete batch must fit 2 MiB of retained JSON bodies before any row body is copied into Rust. Canonical identity/value checks and each distinct original's integrity check precede publication. Pattern and period-comparison source dialogs now use these reads. A page change or return to an earlier page requires fresh verification; stale, corrupt and late results cannot enable source inspection.

Combined core passes **195 ordinary tests** with 21 native/runtime exclusions and strict Clippy. The production UI build and **63/63 real-core browser workflows** pass. Added browser cases verify actual altered-source rejection/restoration, exact 25/2-row calls and the page-return race found during peer review. Historical schemas remain unchanged; command v13 preserves all 46 prior variants and definitions. Five portable benchmark-evidence tests pass.

The [retained 100,000-row page benchmark](performance/TRANSACTION-PAGES.md) found an unnecessary second scan for empty selections. The guarded shortcut keeps cursor validation and every response byte unchanged. On this Mac, the two 20-sample campaigns measured empty-result warm p50/p95 at 158.190/164.865 ms before and 78.585/83.660 ms after. Nonempty query paths are unchanged. A first 200-row page serializes to 94,696 bytes versus 83,986,629 for full presentation; the default response and remaining client consumers are not yet fully paginated. The paired observation binds exact executables, source and unchanged original/copy hashes, SHA-256 `6066d5f6c2b73c74ebe5b75ead77340664e77bd5f45cabd136fc15a7b4fc28e6`. These short backend samples exclude IPC/rendering and do not pass the full performance gate.

Published `fc8c773` passed Windows source CI, the browser job and Windows confinement foundation repeat. Both Mac source jobs failed before desktop tests because the new unit-test step did not receive the existing CI resource override. A fresh checkout without staged runtimes reproduced that failure; the same three policy tests pass with the narrow step-local override, now integrated at `bf8b6f2`. Production bundle resources remain unchanged. Fresh hosted checks must verify the repaired head.


The native Mac app built at clean `e9f5f98` also verifies the new source-read path. Pattern calculation displayed the full 13-row denominator, with 12 AUD and one USD pending row. Opening AUD pending sources returned all 12 exact rows, and source handoff displayed `Opening credit`, `2400.00 AUD` and the matching preserved CSV row 2 amount excerpt. Normal quit left all 55 canonical records and revision 31 unchanged. The retained operator record binds executable/build hashes and both referenced originals, SHA-256 `93e6acdd6b0d534d01cf0bf4ca2cd3566ecf2e2749f48bf116030bab7f67be24`. This verifies the current development Tauri path, not clean installation or large-case desktop responsiveness. Period comparison shares the reader and has separate browser coverage.


## Paged finding history and transaction selector metadata — 25 September 2026

Command v14 adds [target-bound review-history pages](assessment/DECISION-HISTORY.md), with one revision snapshot, canonical insertion order, verbatim historical reasons/timestamps, up to 50 decisions and a complete 1 MiB body preflight. Unsupported, ambiguous, stale and oversized reads fail explicitly. The finding-review UI now uses this reader instead of filtering the default decision array. It preserves drafts across refresh while refusing stale review mutations, distinguishes unavailable from empty history, and discards late results after selection changes.

Command v15 adds [whole-ledger account/currency facets](transactions/FACETS.md), independent of the current ledger page and review state. Exact values and counts are paged in binary text order, with bounded copies and revision-bound representative cursors. It does not analyse amounts or reverify originals. The selector UI still awaits migration; the default transaction and decision arrays remain present. Literal whole-ledger search and citation readers are further dependencies of that migration.

Combined clean source `04d056724c536da16e34b7f695d9ec8603168ddc` passes **209 ordinary core tests**, with 21 native/runtime exclusions, strict core Clippy, production interface build and **69/69 real-core browser workflows**. All 51 historical schema files remain unchanged; v15 retains all 48 v14 variants and 26 definitions. The history browser tests cover real canonical writers, stale continuations, source handoff, empty/failure states, delayed responses and keyboard behavior. Two initial targeted failures exposed disabled-button focus loss; the final repair and no-focus-steal regression pass. Facet peer review found an account-control/filter mismatch; explicit rejection and a real quoted-CSV regression fix it. No release gate changes.

The native Mac app at the same clean source displayed all three saved decisions for the synthetic finding, with exact IDs, reasons and raw timestamps at revision 31. Refresh preserved an unsaved draft; nested inspection displayed the retained row 10 amount `-180.00`; Escape restored the source and finding controls. The draft was never submitted. Normal quit left all 55 canonical records byte-identical. Operator record SHA-256: `1c00051299c9a78de660d2deeb5b8c0b275044436b09fc7d1b2c1ac28d1f1e2d`. Multi-page and concurrent-writer behavior have browser coverage; this native check used three decisions and is not clean-installation acceptance.

Published `e9f5f9881bd49055cb07810fb5aed4c633d2e83b` passed [all four source jobs](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36024719341) and the [Windows confinement foundation repeat](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36024718994). The previous Mac CI resource-override failure is therefore resolved at that published source. Newer source still requires its own hosted checks.

The sixth Windows Java development probe passed its bounded benign FileWorker control but failed its first confined parser operation. A sanitized JVM source fingerprint identifies bootstrap image canonicalization; investigation found that the JDK rejects the extended Windows path prefix used by the confined runtime copy. The separate Windows engine PR is preparing an identity-checked Java launch-path spelling repair; its adapters remain disabled pending actual successful native probes. No ACL, capability or resource boundary was relaxed.

The original Figma document remains listed, but a safe control read timed out and native menu interaction did not recover its document. Its unsynced original was never reloaded or closed. Existing editable design references and implementation screenshots are retained; new pagination frames and remote-save verification remain open. Complete runtimes, remaining analytical workflows, clean Windows/Intel installation, signing and human review remain delivery dependencies. All twelve complete-release gates remain unpassed.


## Literal ledger search and original identity repair — 25 September 2026

[Command v16](transactions/LITERAL-SEARCH.md) adds bounded literal text search over the complete transaction scope, sharing the existing page/row/original-verification implementation. Text joins original description, account and date with single spaces; lowercase matching is whitespace-preserving and neither normalizes Unicode nor interprets wildcards. Text is a scope dimension before review selection. Empty search retains the older page behavior with a distinct search cursor family. Nonempty search explicitly fails malformed or oversized fields in scope instead of hiding them as nonmatches.

The matching profile records the compiled Unicode version. The shared 32-case fixture passes both actual canonical Rust search and real Node JavaScript checks; Rust's Unicode 16.0.0 and this Node runtime's Unicode 17.0 are reported separately. This is a named regression set, not universal engine-version equivalence. The browser CI job now runs the same JavaScript fixture and records its actual runtime versions. No new dependency package was added; the existing SQLite binding enables its fixed-function feature.

[Direct source inspection](processing/SOURCE-INSPECTION.md) now rehashes the original before returning an anchored quote. Bounded peer review also found that verifying a substituted digest alone could incorrectly attach another intact original to an evidence record. Original verification now binds canonical evidence ID to its digest; excerpt and page readers additionally bind the requested source key. The two-original retargeting regression protects that distinction. Missing, same-length altered and substituted originals fail explicitly, with exact restoration supported. Existing schema shapes and valid source behavior remain unchanged.

Combined source `daf16dfbe109cf7a6b50b0cf8d415a39ebc83ca1`. It passes **222 ordinary core tests**, with 21 native/runtime exclusions, strict core Clippy, production UI build, the 32 JavaScript cases and **70/70 real-core browser workflows**. The fresh browser binary includes the final shared digest check. Prior failed fixtures, peer findings and narrower verification records remain retained. Default arrays, selector/search UI migration, citation readers and compact summary projection remain outstanding. The published `80b8646` also passed all five hosted source/foundation checks; this new source requires its own run. All complete-release gates remain unpassed.


## Integrated citation and desktop-summary readers — 25 September 2026

The command v17 citation integration passed 233 ordinary core tests, strict Clippy and preservation of all 57 earlier schemas (50 v16 command variants and 29 definitions unchanged). The combined opt-in summary integration at `ba58cf9`, with an additional assertion that citation responses are identical in full, presentation and summary modes, passes **241 ordinary core tests**; 21 native-engine cases remain ignored in that ordinary run. Strict all-target Clippy passes, and schema regeneration changes none of the 64 saved files. Logs are retained as `artifacts/summary-citation-integrated-{core,clippy,schemas}.log`.

Summary tests compare all retained fields and difficult money/reconciliation outputs against the existing calculator, including exact scale, multiple currencies, pending/rejected/deferred rows, source-separated balance windows and explicit transfers. They exercise a real concurrent writer, committed mutation followed by failed refresh, stale replay rejection, queue wakeup, single execution and shared cancellation ownership. Citation tests exercise complete selected sets, source identity, atomic size preflight, short pages, literal matching, stale/corrupt records and read snapshot consistency. No desktop transport has switched modes. No new installed-package, UI-latency or process-memory claim follows from these backend tests.


## Balance, transfer, complete export and citation integration — 25 September 2026

The v18 balance slice passed 247 ordinary core tests and a separate read-only peer review. The transfer slice passed 258 on its independent base, including 11 new snapshot/identity/paging cases. The complete export slice passed six tests plus its public mode/bounds regression; combined v20 source passes **265 ordinary core tests**, strict all-target Clippy and formatting, with 21 native cases explicitly ignored. All 70 previously saved schemas are unchanged; v20 retains 54 v19 variants and 35 definitions. Logs: `artifacts/transfer-export-integrated-{core,clippy,schemas}.log`.

The exact opt-in summary observation SHA-256 is `a67fec13910c7fbf88eb9066bb0fe20900b1de9494745330b120d8c804d4c841`. Actual serialized presentation/summary responses were 83,986,629 / 5,111,287 bytes; every retained field and exact calculation matched the declared projection. The prepared/comparison database remained `d920f4d0256a6b92e59ea1de85abeb3855b6ec2962e754b69a30166d9f5cf3dd` before and after closed handles. The original frozen corpus remained unchanged. Root also reran the diagnostic's serializer test and two failure-history tests, without rerunning this measurement. See [scope and retained identities](performance/DESKTOP-SUMMARY.md).

The UI-only citation source `ec16358`, integrated as `4efa177`, passed 79/79 real-core browser workflows, type/build and zero axe violations at 1440/720 px. Its [verification readout](design/review/citation-picker/verification.json) retains the initial compact-test Escape assumption and the later fixed client request scheduling. Root inspected desktop and compact rendered evidence. Browser evidence used the v17 binary; no later backend compatibility is inferred solely from that earlier campaign.

Actual native build `4efa177` showed 22 uncited records at revision 31; literal account `000017` matched three. An unsaved contradictory selection changed selected count to two and remaining matches to two, and a zero-result search plus workspace refresh preserved the draft reason and both roles. Source inspection returned the original `12,30` at mapped CSV row 3, `Paid out`, digest `54e40dcc8bc61c2c4b8b231533c2039abf176844fac014671d4d411c1d459691`. Both nested source returns and editor closure restored their openers. All 55 records remained identical. CmdQ was followed by an AX timeout; independent process inspection confirmed the named executable had exited. The operator record is `artifacts/citations-native-initial.json`, SHA-256 `90d01522bdbad3b02ddd5763e603807d0245fad4b043cfa09ac1dba0a551080b`.

Native refresh lost focus when its opener was disabled; the following repair and native retest retain this earlier negative result. The native corpus fit one citation page, so multipage/race cases remain browser observations. No Figma extension, supported clean installation, release artifact or performance gate is claimed.


## Focus repair, development summary entry point and facets — 25 September 2026

The additive CLI flag at `27d3de2` has an actual subprocess integration test: import and view return the strict summary, full/presentation modes retain their legacy arrays and complete filtered export returns identical dedicated responses in all modes. Combined clean source `ad74c03` passes **266 ordinary core tests**, with 21 native cases excluded, and strict Clippy. Logs: `artifacts/summary-cli-integration.log`, `artifacts/summary-cli-clippy.log` and `artifacts/summary-cli-integrated-core.log`. Desktop and browser defaults are still presentation mode at this source.

The focus repair, integrated as `ad74c03`, passes **21/21 targeted citation/assessment/history browser tests** after the original failure was reproduced. Its [retained observation](design/review/citation-picker/refresh-focus-verification.json) distinguishes the earlier 79-test full suite from this narrower run. Native build `ad74c037e7369bc11d278552e4c65c48e7fffdb2` restores focus to Refresh workspace and retains the draft reason and both citation roles. The executable SHA-256 is `87079d9d828e6e112a3781c05fb0e28385fd910d20b112b7a4f8111e3542da5d`. Native selection handling also exposed an operator test effect: Return after an AX popup selection submitted the temporary contradictory citation. The normal editor restored the original selection with a reason; neither decision was erased. Before/after checks find every prior record identical, two additional decision records, revision 31→33 and 55→57 records. All seven originals rehash correctly and the immutable report is unchanged. Normal quit was independently confirmed by the named process being absent. The operator record `artifacts/citations-focus-native.json` has SHA-256 `1218ea725764add2f34b3784d397f65d6252f7217597389f2d4efd55f58068cf`. Native multipage, clean installation and release signing are not established by this check.

Facet UI source `903ac02`, integrated as `b0214bf`, passes **24/24 real-core browser tests**: seven new cases and 17 existing pattern/comparison cases. Cases cover more than 100 account/currency values, exact retained selection, stale and failed continuations, two revision changes behind one held real response, one-active/latest-pending scheduling and focus movement followed by blur. Production build and new-state axe checks pass. Root inspected the desktop pattern and compact comparison renders. [The handoff](design/TRANSACTION-FACETS.md) retains screenshots and distinguishes the reused editable design foundations from new pagination states awaiting Figma review. No whole-application test, default-summary activation, native selector, IPC latency or release claim follows from this isolated handoff.

The combined pre-summary source `af67c16` passes **89/89 real-core browser workflows** in the full suite, including the citation focus repair and shared selectors alongside every existing workflow. Log: `artifacts/pre-summary-integrated-ui.log`, SHA-256 `7f511213f38fa4c6cea93f7c2d3ac6736f4f1118cb740c44a674fcdae8aa06cf`. This run uses the v20 backend and presentation transport; summary activation must receive its own combined verification.

## Summary transport, native export failure and DOCX — 25 September 2026

Cutover source `a102c445918b0988257d8b73c588c8685d74296d`, integrated as `f2b1e9a5704bac43dcf6577d1f6abd4b24deee17`, passes **104/104 real-core browser cases**, the production type/build, three desktop Rust tests and strict desktop Clippy. The [retained readout](design/review/desktop-summary/verification.json) includes failed attempts, exact log hashes and all repairs. Closed balance-state validation, monotonic summary publication, explicit empty/unavailable states, byte-short pages, continuation beyond 100 cursor-history entries and stale complete-export rejection are covered. Four focus/visual and three final citation-readiness cases pass after test-only refinements; production code is unchanged from the full run. Desktop and compact rendered evidence has zero automated axe violations. New Figma extension frames remain unverified.

Actual native builds at clean `f2b1e9a` use separate standard and benchmark application identities. Their executable SHA-256 values are `96194b1b501ef87b45641430442ebb556b9c79e01a984293b4424115552ed025` and `3a662d8811330a3a501efc626400198de90692231955c3d0887d4fdba62ad4f3`. The isolated case displays revision 95102, 100,000 rows, 85,000 accepted and 5,000 in each other review state. AUD net is `-460966.00` with 56,400 included and 200 excluded transfer rows; USD net is `-235291.00` with 28,400 included. Native page continuation displays rows 101–200 after 1–100. The regular case displays all 13 pending rows at revision 33 and the `162.00` source-order balance mismatch.

Both the 100,000-row and 13-row JSON exports stall without a new completed download. Actual process samples place the main thread in WebKit download destination handling, ending at `_sandbox_extension_issue` / `__mac_syscall`. They do not identify the underlying OS cause. CUA state/screenshot/normal-exit attempts fail or time out. Each exact test app is stopped with SIGTERM after preserving the trace. Read-only comparison of every canonical table and all retained originals confirms both workspaces unchanged; the previous Downloads file is byte-identical. This is a retained native failure, not an export pass or evidence that the whole large-case performance target is met. Operator record `artifacts/native-summary-benchmark/native-initial.json`, SHA-256 `2ef22742ed27c7a99cbead80f9363b0743968437c3dc968869782653bd540a9b`, binds the builds, traces, pre/post state and observations. Repair remains active.

DOCX foundation source `d816db40702af0b11ad176de8745f13a755e59d3`, integrated as `8681a42`, passes seven report integration tests, one private archive bound test, four portable evidence-harness tests and debug/release strict all-target Clippy. At exact clean source, repeated generation yields frozen JSON SHA-256 `dbfeebdd7240f2b572994f508bfebbcfd81066c9b12cd0da2903426fe33cd6fa` and 67,943-byte DOCX SHA-256 `3a062eecd97c43d69237a84c4109eae1e40011ef2aab65ca852d1c7e23e80979`. All eight final clean-render PNGs are byte-identical to the eight individually inspected pages. The renderer is explicitly **LibreOfficeDev 26.8.0.0.alpha0**, not the older version asserted by its runtime manifest. Exact report SHA-256 is `e266e6c6b63ec34021e63c1360ca586454e420b7a8c79ee0fb367ca20b8a4f54`; it makes no native Word or stable LibreOffice claim. The pure model does not rehash originals or publish canonical reports. Existing HTML-only snapshots remain unchanged.

Root's combined source `8681a42` passes **274 ordinary core tests**, with 21 native cases explicitly ignored, and strict all-target Clippy. Core log SHA-256 is `535c063ed6d3725326f01002df879c48763e68482f923e4c2cd6abc2c6f55c31`. This integration adds no UI change after the 104-case cutover campaign. No complete-release gate is passed by these source or development observations.

## Durable checkpoint integration and factual onboarding — 25 September 2026

Signed foundation `0800d29380a93001b99772c307028209ea2a813b`, integrated as `06acbb8`, passes its 288 ordinary tests, debug/release strict Clippy and source-pinned previous-reader backup/restore case. [The retained record](discovery/verification/durable-foundation.json) distinguishes private synthetic checkpoint semantics from unimplemented live activation and native Windows sharing verification. Historical schemas and dependency versions remain unchanged.

At native source `f2b1e9a`, query `MERCHANT`, account `0000`, currency `AUD`, all review states and date-ascending order produce 6,700 matching rows: 5,100 accepted and 1,600 pending. The first row is `SYNTHETIC MERCHANT 0056`, `2016-01-01`, `-15.56 AUD`; its review resolves the preserved `amount` at CSV row 5602 and revision 95102. These values match the retained core oracle. The app quits normally, its process is independently absent, and all five canonical tables plus originals remain identical in both test workspaces. Record `artifacts/native-summary-benchmark/native-filtered-source.json` has SHA-256 `56321b5a1b006db71c9b57694c23ed03fd0351e1da45b32d49343e7815898fa2`. The first readback script used a nonexistent database filename and failed before writing; the corrected read-only check is recorded. This remains a correctness observation, not a performance benchmark.

The demo guard regression attempts seeding into existing evidence-only, statement-only and report-only workspaces with no entities; each rejects without changing the view or originals. The previous mixed collection fixture now creates its existing observation through normal domain actions rather than loading a second demonstration. Combined core: **297 passed, zero failed, 22 specialized ignored**; strict Clippy and UI production build pass. The new real-core statement-only overview case passes **1/1** and verifies absent demo onboarding and factual zero entity/location counts. Logs remain under `artifacts/native-summary-benchmark/`: `integrated-durable-demo-core.log`, `integrated-durable-demo-clippy.log`, `demo-ui-build.log`, `demo-ui-regression.log`. Native export verification and combined full-browser rerun follow the independent export repair.

## Native JSON and historical HTML saves — 25 September 2026

Signed native repair `a4b9389ae7820a8feef7d1ec2baead290869e812`, integrated as `86fb0cccdbc940a8fdf12ef4325f72ebf35a1ad3`, passes its 11 file/lifecycle regressions and real-core native-IPC browser cases. Root's combined source passes **319 ordinary core tests, zero failures, 22 specialized ignored**, strict all-target Clippy and the complete **110/110 browser suite**. Native Windows file-identity execution is pending hosted source verification; a local isolated metadata type-check is not runtime acceptance.

Both actual native apps were rebuilt from clean `86fb0cc`. The standard executable SHA-256 is `dbb7a2c7bca37ef8967d89c444be4321c06da20b09e75fcdae4b7ff7c3cc77f5`; the isolated benchmark executable is `cf048e9910400e851bd809034977645832a4fbd13ccb2be1c4664876df92ec7d`. Native save receipts and responsive controls are observed for:

- 13 rows at revision 33: 8,089 bytes, SHA-256 `9fa710bfc3be17f853896d898045dfdb3368ad6cba594116ae8d194578912275`.
- 100,000 rows at revision 95102: 60,613,204 bytes, SHA-256 `4d0e48c5621e067afad8c43e099ff4e5dd1f68c73eda50cefc58888be33cb5d6`. A second native save reuses the same inode and unchanged modification time, with exactly one exported file.
- Saved revision-12 HTML while current workspace is revision 33: 15,026 bytes, SHA-256 `790fc5ac4296af67af85a5ef5e71e0881aaf85208bcb8d63b6733a1f8780c10e`, exactly equal to the canonical historical bytes.

JSON files are byte-identical to independently retained core responses; their parsed arrays also match every canonical transaction in stable date/source order. Completed files are ordinary, singly linked, mode 0600, and staging is empty. Both apps quit through their normal UI, their processes are absent, and read-only hashes of all five canonical tables and every original match the pre-test baseline. The saved-report native render is inspected. Operator record `artifacts/native-summary-benchmark/native-export-repair-verified.json`, SHA-256 `4c3f12d294b32f0cd0cf2ddbc77a1de36a0d3cab8533b7b35ef35aff7fa15608`, binds both builds, the oracle, test logs and file/canonical verification. Earlier native failures remain retained. This is development-Mac correctness evidence, not a clean-installation, Figma-extension, performance, Windows/Intel or signed-release pass.

The original-consumer patch `8b9613c`, integrated as `e41ac3a`, passes its 307-test isolated core suite, debug/release strict Clippy and [retained regressions](security/verification/original-consumers.json). The combined count above includes it. Audit repair `a779771`, integrated as `8bcffac`, passes six temporary-Git regressions, including deletion plus a prohibited staged addition, staged/working-tree mismatch, rename and type change. The scanner still reads index blobs; it does not substitute working files.


## Canonical source keys and retained file identity — 25 September 2026

Evidence lookups now require the canonical SQLite key, Evidence ID and original digest to agree. This prevents a valid source body and intact original from being substituted beneath another source key. Review, analytical provenance, processing, collection, report capture and recovery use the bound metadata readers; specialized bounded readers retain their existing explicit checks. [The source-identity contract](security/EVIDENCE-IDENTITY.md) distinguishes metadata validation from original-byte verification. Historical reports, public commands and schema shapes remain unchanged.

Immutable derivative reads now verify one bounded no-follow handle and compare its final named identity. The shared private file verifier also covers original reads and native export files. On Windows it uses the full 128-bit file ID and expected hard-link count, with write/delete sharing denied; unsupported identity queries fail closed. Regression cases cover replacement, growth, hard links, FIFO replacement and preserved exact bytes. These protections do not establish an application-wide race-free filesystem sandbox.

Evidence-key source `ad0adeaf`, integrated as `5fdc86f`, passes 318 ordinary tests on its isolated base and 329 after integration. The [retained evidence](security/verification/evidence-identity.json) preserves before-fix false-success cases, the refined restore adversary and exact source/log identities. Shared retained-file source `276ec4e`, integrated as `cfc1415`, adds four ordinary derivative-reader cases and a platform-specific Windows sharing case. Root's original-reader follow-up uses the same handle verifier and adds a Windows hard-link regression.

The resulting integrated source passes **333 ordinary core tests**, zero failures and 22 explicitly ignored specialized/native tests; strict all-target Clippy passes. The retained logs are `artifacts/native-summary-benchmark/retained-file-integrated-core.log` (SHA-256 `cf039b0f43991339af94942e64acee84846dfc4c12d6310d0bcdf52a3f626717`) and `retained-file-integrated-clippy.log` (SHA-256 `c7061d495a519e163b373607abba1b64da086afb15db39700a3699e8bacf6ef9`). Native Windows-only cases are compiled/executed by the next hosted Windows source run, not by this Mac result. No dependency version, public schema, storage schema, UI or live activation changed. No full browser repeat was needed for this source-only integrity increment; the prior 110-case campaign remains bound to its tested source. Complete-release gates remain unpassed.


## Canonical DOCX, Windows recipes and integrated transport — 25 September 2026

DOCX source `c1e7ead`, integrated as `d0ec2d4`, adds immutable canonical snapshots and schema-five restore. Its handoff SHA-256 is `842c207ec749e163c1287cf52a63323dc2642eca50e175ad5c82a7dfc3fe4454`. The frozen example retains 24,362 DOCX bytes (SHA-256 `4ca9f1fca4e42f104292daf5b09747bf35291ed55c33616841e6c5919aef3b90`) and 3,777 JSON bytes (SHA-256 `251f86cce8bde1d752237fb3a29087df66b3e8f22e9817f52393f671e55c4a0e`). Three rendered pages were inspected using the previously disclosed LibreOfficeDev build; this is not native Word acceptance. Root's combined pre-transport result is 346 ordinary tests and 22 ignored specialized tests, with strict Clippy.

Windows source `4387168`, native run `36047073221`, was actually tested at merge `34c238886a249b5799e8e5cac90d828d325f127e`; source CI `36047073106` also passed all four jobs. The full campaign, environment, 15 earlier failures, predicate maps and artifact hashes are retained in [the Windows Java record](security/WINDOWS-JAVA-WORKERS.md). Integration `7202488` retains byte-identical engine behavior and adds Mac source fingerprint coverage for both directory helpers. It does not activate a canonical Windows processing route.

Root's Maven build compiled with `--release 21` on the available JDK 25.0.2 and ran all **30 JUnit tests**, zero failures/errors/skips. The initial shell call lacked Maven on PATH; the subsequent offline build lacked a cached Surefire JUnit provider. Both failures are retained; the actual successful Maven build used the existing local Maven and official Central dependencies. A fresh isolated staged Java-21 runtime then passed **13/13 actual Mac parser/search boundary tests** at clean `8c4f8ab`, report SHA-256 `1976e079673ca1636a2e11df90dc0113eedc199fde5f7fdf274b8ed282743219`. The first campaign selected 19 tests because its exclusion list omitted the separately staged image-region OCR suite: all 13 intended Java checks passed, five ordinary region checks passed and the unstaged native OCR case failed. The corrected exact module exclusion is committed; both initial and final reports remain retained. This is not OCR, complete packaging or cross-platform installed release evidence.

Private transport source `e180b238`, integrated as `c6a04ca`, retains [its source and negative-case records](discovery/verification/cancellable-transport.json). Its schema compatibility count refers to **73 historical schema-directory files**, including **71 JSON schemas** plus the README and runtime inventory, not 73 schema definitions. The combined root source passes **360 ordinary tests** (285 library plus 75 integration), zero failures and 22 specialized ignored, plus strict all-target Clippy. The source is still unactivated. Native Windows resolver cancellation and actual Mac DNS execution are not established by the local synthetic TLS fixtures or narrow Windows FFI type check.

The earlier published integration head `c585712` passed all hosted source checks (browser, Windows Server, Apple Silicon, Intel Mac and AppContainer foundation). Those successful checks are bound to that head and do not stand in for a new-source run. Required human review remains in place.

The full production UI build and **110/110 real-core browser workflows** pass against the integrated `c6a04ca` production source. Only design documentation commit `2eec055` was created while the campaign ran; tested application source was unchanged. Combined verification record `artifacts/windows-docx-integration/integrated-verification.json` has SHA-256 `dc738aba8e5bee71a49f2df730b68a25b0b9ff36c98212b8e2a792ff2a6ae57a`.


## DOCX commands and exact binary native export — 25 September 2026

Signed source `ac0607243682d58a635342b50f02ace7343b4e91`, integrated as `229ea20`, adds three direct Command-v21 results: stable-ID DOCX capture, bounded revision-bound catalogue pages and explicit both-digest inspection. New metadata readers never load originals or render documents; the explicit inspection/save path verifies the frozen artifacts. Nine additive JSON schemas retain all 73 historical schema-directory files, including every Command-v20 variant and definition, byte-for-byte.

Native `docx_report` preparation retains report ID, captured source revision, document digest, DOCX digest and byte count. Exact binary output uses the existing single-stage, 120-second expiry, no-clobber publication, acknowledgement recovery and shutdown cleanup. Old HTML and JSON byte conversions are unchanged. A real test exposed janitor registry contention falsely rejecting the first preparation as busy; a separate preparation gate now serializes generators while allowing short housekeeping waits. The original failure and initial test-instrumentation contention are retained; the final regression uses explicit channels rather than timing retries.

The isolated handoff passes 358 ordinary tests, 7 catalogue cases and 16 native lifecycle cases, plus core and desktop strict all-target Clippy. Source-only desktop checks use the existing empty-resource override and do not establish native installed behavior. The handoff SHA-256 is `75f500224de9e7fa8e2e8cb943fdff30c9182f1f10fc1823a4e9fe6d2d241f78`. Root's integrated result is **372 ordinary core tests**, zero failures, 22 specialized ignored and strict all-target Clippy. All **five native-export browser compatibility cases** pass after integration, retaining JSON exact bytes, lost-ack replay, abandoned stage cleanup, mismatched metadata refusal and immutable historical HTML. DOCX catalogue UI, actual native DOCX saving and Word compatibility are separate follow-up checks.

Root logs: core SHA-256 `30c503979d70de71efaae8cc2318f96064dbf00f8274c1a713b21d5282ec532b`; Clippy `bd5d7eaae1316e6d13a2cc0d3cb78df4dcb3cffba153c799ee3ea43bc97c2266`; native-export compatibility `0d3ffa6ef55202e5634602352ab388bc91f73ecf91ef982d2e45bf1117ccbe71`.


## Integrated DOCX recovery and extraction v2 — 25 September 2026

Clean source `3ce80007de315c588157cfee319529dec407b75d` combines the DOCX catalogue/native-save UI, Command-v22 recovery, extraction v2, the Windows canonical parser route and the transaction review completion repair. Root verification passes **390 ordinary core tests** (315 library plus 75 integration), zero failures and 22 specialized/native exclusions; strict all-target core Clippy, the production interface build and **125/125 real-core browser workflows** also pass. Regenerating current schemas changes no tracked file. All **80 historical JSON schemas** at `523bcfb` are byte-identical; the additions are command v22, DOCX capture resolution v1 and extraction v2. The schema README was updated separately because its previous current-version statements were stale.

The initial integrated browser campaign at `d839085` passed 118/119 and exposed a genuine late-close race. Two held-response regressions then failed before the repair for same-row and different-row reopenings. Source `f645ce7` binds close to the submitting selection; the later draft remains stale and visible. All 15 targeted cases and the final 125-case campaign pass. Original logs and failure context remain private retained evidence; no timeout was increased to hide the failure.

DOCX UI source `1929391` passes 17 isolated compatibility cases; typed recovery source `4066fcc` passes 377 isolated ordinary Rust tests and 21 browser cases, including pinned-snapshot concurrent-writer and uncertain-acknowledgement tests. Their handoff hashes are `c04c087644b4dddc9b044dfadf2214ff05f925bcace1b7a5ff274c3ec571e9c1` and `54a3c59ee3f41753c74e53522765a0e579ba7c09e987176414d54b4103409def`. Real Rust binary-saving bridges do not establish actual WebKit/Word interaction. The [14-case native Mac extraction compatibility report](processing/verification/extraction-v2-native-mac.json) remains bound to its signed source and unchanged staged Java runtime.

Windows runtime classification source `395215be` passes 52 host tests, host strict Clippy and Windows-target all-target check/Clippy. Its two before-fix failures are retained. Windows-specific branches were compiled locally, not executed on this Mac. The canonical adapter passes host binding and both-dispatch publication checks; actual Windows execution remains the next campaign. The earlier published `523bcfb` passed all five hosted source/AppContainer checks. Those results do not substitute for verification of this new source.

## Windows campaign preflight regressions — 25 September 2026

At `ed4ab2c`, [source run 36057371444](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36057371444) failed full Python discovery because `test_windows_parser_coordinator` resolved to the runner rather than the same-named test module. The unit file is now `test_windows_coordinator_receipts.py`; full discovery runs 140 cases with three platform skips on the development Mac. The campaign runner and its strict receipt validation are unchanged.

[AppContainer run 36057371509](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36057371509) passed 65 worker tests and failed the trusted-runtime access fixture's raw `PermissionDenied` assertion. That fixture uses a Windows exclusive file handle, which must be checked as an exact `ERROR_SHARING_VIOLATION` using an independent native read. It now compares the runtime reader's error kind to that control, then still requires blocked verification/execution, cancellation precedence and successful verification after releasing the handle. No production code or worker access rules changed. All 52 host worker tests and strict host Clippy pass; Windows runtime verification remains a new hosted-run dependency.

Combined local verification record: `artifacts/integration-3ce8000/verification.json`, SHA-256 `a85c6b84004c8dbba89a7e7413c15979148ad35addb6773dbf4030fd64ed4b76`. No release gate is changed.

## Clean browser prerequisite repair — 25 September 2026

[Source run 36130338402](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36130338402), at head `64722b9`, passed 122 browser workflows and failed three DOCX native-save cases because `native_export_session` did not exist. Its build was inside a later spec file, so successful local runs had depended on a cached executable. Playwright's shared global setup now builds both `ew-dev` and `native_export_session` from current locked Rust source before any test runs, including direct or individually selected Playwright invocations. The redundant npm prehook and spec-local helper build are removed.

The development-Mac verification physically moved the prior helper away, confirmed it was absent, and invoked the three failed cases directly with `npx playwright test`. All three passed and the executable was rebuilt. The production interface build also passes. This narrow repair does not relabel the failed hosted campaign as passing; combined and fresh hosted checks remain required. The original hosted failure log and clean-helper regression log are retained privately.

## Canonical Windows campaign failure — 25 September 2026

[AppContainer run 36130338414](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36130338414), source merge `6f1b388e4906430757bac7466735878fc1e058b6` (tree `a0587c774cd7b6a302efded23f8e6e57adc13633`), passes the worker and Java engine campaigns, then fails the canonical coordinator campaign on `notice.txt` at the publication assertion. Queued cancellation passed; the coordinator joined and retained its failed synthetic workspace. No extraction derivative was recorded. The receipt does not distinguish an input/runtime refusal from result publication failure, so the underlying cause remains unresolved.

The development probe's receipt version 2 now records only the last fixture job's typed state/failure, attempt, result count and whether it started. A closed Python validator rejects arbitrary text, extra fields, invalid enum values and out-of-range counts. The field is cleared at each phase change; it does not weaken the 25 required checks or supply a success claim. Six probe source tests and 13 receipt/runner tests pass locally. The retained source-bound failed receipt remains version 1; no historical receipt or public application schema is altered.

## Native DOCX baseline and versioned wrapping repair — 25 September 2026

The isolated actual Mac app `org.entityworkbench.docxreview20260925`, built at clean `3ce8000`, was exercised through native controls. Two canonical captures and native saves, stale-revision typed recovery, an explicit new capture, immutable re-export, normal quit/reopen and two persisted catalogue records passed. The workspace advanced from revision 6 to 9 through the two report publications and one deliberate finding correction. Original evidence, saved HTML and unrelated records remained unchanged. Word 16.113.2 opened, edited and saved a disposable copy; the canonical export remained byte-identical. The retained baseline handoff has SHA-256 `5f12e28ad6c561bbf84ec6f309cc13ef2dce67e9e17e30ea6e809a399416e499`.

Word exposed character-level wrapping in ordinary prose. Signed source `d8d5b8a`, integrated as `4939659`, adds known generator 2 while retaining exact generator 1 output and rejecting unknown versions. Root's integrated check passes 406 ordinary core tests, zero failures and 22 specialized exclusions, plus strict all-target Clippy. Golden historical rendering, old/new snapshot inspection, UUID retry, native export and complete backup/restore pass. All 85 prior schema-directory files remain byte-identical in the isolated repair. The [wrapping record](reports/DOCX-WORD-WRAPPING.md) records all seven inspected managed-render pages and the renderer's actual identity discrepancy. Repair handoff SHA-256: `0b1620e7893b14378502ae25ec3b1a357ba527dfd869021ecdac654b352fc6e9`.

Generator 2's actual Word layout is still unverified. Both agent and root observed the native picker failing to open the new files; root subsequently received unavailable-window errors from Word and Finder. No process was force-quit and no OS permission was changed. This is a native-control blocker, not observed evidence of DOCX rejection. Neither the baseline nor the managed render establishes clean installation, other native platforms or complete report assembly.

The complete **125/125 real-core browser suite** passes at integrated `4939659`, including the repaired shared helper setup and all DOCX native-save workflows. Application source stayed unchanged throughout this run; only these status/verification notes were edited. This is browser/development-bridge evidence, separate from native Word behavior.

## Runtime inventory producer integration — 25 September 2026

Signed inventory source `0964b1d`, integrated as `57fe451`, supplies deterministic offline inventory generation from explicit component ownership. Shared bounded traversal/hashing retains the existing verifier's rejection behavior; the generated inventory is independently verified before no-clobber publication. Sixteen producer tests and 25 existing verifier tests pass under normal and optimized Python, with one native Windows junction skip in each suite on this Mac. Full integrated Python discovery runs 156 tests with four platform skips. Strict all-target core Clippy passes after the durable settlement integration.

The [actual partial-stage record](../packaging/evidence/development-inventory-2026-09-25.json) binds the executed scripts, reviewed plan, requirements and inventory. It verifies 425 files / 250,551,479 bytes, identifies 18 missing components and zero invalid present components, and exits 1 with `generated: true`, `complete: false`. Inventory SHA-256: `2d1cc4cf9620e6e46b56d6186d817e0188eb78196fc9bfa42a092bfd99301629`. No runtime was launched or dependency downloaded by that observation. It does not establish binary version provenance, loader compatibility, confinement, final signed-artifact bytes or installed behavior.


## Integrated ownership, quarantine and Python staging — 25 September 2026

Application source `0e6d390745d23c4e48ead4f73d96c0bd3979f236` passes 423 ordinary core tests, zero failures and 22 specialized exclusions, strict all-target Clippy and all 125 real-core browser workflows. The desktop test target compiles and runs zero tests; it does not prove a native startup/shutdown lifecycle. The retained integration record binds these logs and the subsequent packaging-only source `98b395f`; SHA-256 `944f02cc2de9d0f3d677720604462167bd140a772a9239fb12dd44e939c710de`.

The collection lane shares document coordinator ownership and joins before release. A deterministic blocked-publication regression failed before the follow-up: collection uncertainty left document execution admitted until the receipt write succeeded. Immediate quarantine now cancels active documents, refuses queued execution and preserves stronger termination/cleanup failures while permitting only the exact already-owned receipt to settle under the retained lock. The [source-bound regression record](discovery/verification/collection-quarantine-order.json) retains both outcomes. All 83 JSON schema files remain unchanged. This path is synthetic/test-only; the normal collection command is not migrated or enabled by these checks.

[Source campaign 36132159822](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36132159822), head `ea154db`, passed the browser, Apple Silicon, Intel Mac and Windows Server source jobs. [AppContainer campaign 36132159827](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36132159827), actual merge `fe75f83f6acbe4f5b8253bf22c758457012d5876`, passed engine checks but failed the first canonical fixture before a job existed. Receipt-v2 SHA-256 `0cc7c4eb889742c28a39e3a06427874df467490b617d086b5736ce1013854611` records a null observed job. The original v1 receipt's ambiguity is preserved; the new receipt and a real-coordinator host regression identify the probe's incorrect Import/View response-envelope assumption. Integrated fix `18a0e1d` passes nine probe tests and 13 strict receipt tests without changing production responses or permissions. Fresh campaigns [36134296848](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36134296848) and [36134296949](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36134296949) remain in progress at this observation.

The signed offline CPython stager `9742657`, integrated as `98b395f`, has 20 synthetic tests passing normally and under optimization on Python 3.13.11 and 3.14.2. Root's combined Python discovery runs 177 tests, with 173 passes and four platform skips. [The actual staging record](../packaging/evidence/python-staging-2026-09-25.json) binds exact executed-file digests rather than claiming a clean-commit release run. It retains the initial absent-parent failure and final successful 3,333-file / 276,208,764-byte stage. Output manifest SHA-256: `6b4edce6755094b4f8393d5553da9a6968ac18b761035616430aaed7dbb0be95`. Independent inventory SHA-256: `dc5ea1f8b7527ba1c7ea7e53d2750045d3bbb095e70a926a6b4cc9b8d5ac4fee`, with 22 missing and zero invalid components, expected exit 1.

No interpreter or downloaded package code was executed. The full install tree, raw upstream metadata and all 19 available licence files are retained; metadata references an absent zlib-ng notice, which remains unresolved. This is file-staging evidence only. Wheel installation, native loader closure, relocation execution, minimum OS support, worker confinement and complete notices remain required. All twelve complete-release gates remain unpassed.


## Windows canonical success and partial native observations — 25 September 2026

[Run 36134296949](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36134296949) passes the Windows Server worker, Java-engine and canonical coordinator campaigns. Root downloaded the [exact canonical receipt](security/verification/windows-parser-coordinator-2026-09-25.json), SHA-256 `0e7aab01efcd4cb81ed1690e4251f1e1981d9cc62f3920700b967ae84a3fcd22`, and validated all 25 checks, ten bound fixtures/derivatives and four joins. GitHub commit readback binds actual merge `025ed706fd8144f346f5348a24135b051a23134c` to parents `b12271c`/`0e6d390` and tree `29ffb34ec1b01c3fd9d5c8e4eeaba8aa58d8968c`, identical to local `0e6d390`. Shallow-receipt parent absence is preserved. The receipt explicitly does not prove in-flight cancellation. All four [source jobs](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36134296848) also passed at that published head. These are development observations, not Windows 11 clean-install or signed-release evidence.

Signed-clean Mac DNS source `2a1d8d9`, integrated as `22f0798`, made one fixed campaign with exactly three subscriptions and no HTTP. Four cases passed; the negative-name lookup delivered no callback before its five-second deadline, so the overall campaign failed. All three subscriptions were deallocated. The [immutable partial record](discovery/verification/native-transport-macos.json) binds report SHA-256 `516d1339ee7bace2e3eef92c80f95a124f6fddf08e7220ae57c7397ef3252149`; it records the API flag/negative-result mismatch and leaves later correction/native verification separate. Its nine offline evidence tests pass normally and under optimization and are now included in source CI without enabling native networking.

Native Word opened the unchanged generator-2 ordinary document (SHA-256 `0b9f69392b57dd1190dea7d45c7fcc16bad97ab9fcf2b9dec3b8350b6fc74fd9`) and showed three pages/481 words. The visible first-page prose wraps at word boundaries and retains exact decimal text. Full-page/long-literal inspection remains pending after native zoom/read timeouts; no content was edited or saved. Retained operator observation SHA-256: `5a9e73099b1f682066154fe3e35d1c451ad33c72d730a2842bb1e4f65099c4e8`.

Figma's recovered signed-in view independently showed original frame 57:2 and its expected dimensions/content. A Drafts duplicate exposes editing controls, but browser control dropped before further authoring. [The recovery record](design/DESIGN-WORKING-COPY.md) separates saved-frame readback, working-copy creation and unverified old unsynced edits. No all-frames, complete-layout or release approval is inferred.


## Combined resolver, settlement and wheelhouse verification — 25 September 2026

Integrated application/script source `5c9ad52` passes **435 ordinary debug-profile core tests** (359 library and 76 integration), zero failures and 23 specialized exclusions, strict all-target Clippy and formatting. The new nine-case DNS evidence suite passes normally and under optimization without network. Full Python discovery runs 196 cases: 192 pass and four platform-specific checks skip. Retained combined log record SHA-256: `0650d868f0871974e2841535dc9489c2b439433fbd7ea74f29cd677317a68997`. Only status documentation changed during these checks. The existing 125-browser result remains bound to `0e6d390`; no new native UI result is inferred.

[Mac negative-DNS source `07311ce9`](discovery/MACOS-DNS-NEGATIVES.md), integrated as `32eab45`, passed one signed-clean v2 native campaign. All five cases pass, with three created/deallocated subscriptions, four observed public candidates in the success case, two actual NoSuchRecord callbacks followed by Network after the internal five-second stage, and no HTTP/retry. Report SHA-256: `4c9e33cc353751be6166ccc4958ddf19e68c3c010390b1398df5c3014c5e450a`. Both source and binary identities remained unchanged. The earlier failed campaign remains byte-identical. Candidate sets are partial observations; caller stop conditions take precedence after cleanup. Actual Windows resolver execution and live durable activation remain separate.

The private clock-rollback repair `185b043`, integrated as `8a0a93f`, retained a deterministic pre-fix failure and passed exact-ticket, owner, replay, raw-clock, unknown-state and failed-publication retry cases. Its isolated release-profile suite passed 419 ordinary tests with 22 exclusions and strict release Clippy; the combined debug totals above are distinct. No public timestamps, schemas or commands are relaxed. [The coordinator contract](discovery/COLLECTION-COORDINATOR.md) explains the timestamp-free internal cancellation boundary. Handoff SHA-256: `597a2b72834fbe99fa5d066db07015cee47334606b6aa247d5b7a8959c3a01eb`.

[Wheelhouse evidence](../packaging/evidence/python-wheelhouse-2026-09-25.json), integrated as `cb9bbf5`, binds the exact 58 selected official archive downloads and executed-file identities. Nineteen synthetic tests pass normally and under optimization on both installed Python versions. Actual verification streams 319,470,877 expanded bytes and retains 339 files / 97,089,012 bytes; all 58 archives, 174 primary metadata files and 105 notice files independently match their sources. Manifest SHA-256: `171739e9a2d0ae7eb58cb52e29d0d5c35d5e97a4e694482cc91d9f29a4065ea0`. These are uninstalled archives and satisfy zero runnable runtime components. Package-code execution, installation, loader closure, relocation, confinement and complete notices remain unpassed. All twelve release gates remain false.
## Figma persistence, static Python layout and current hosted checks — 25 September 2026

[Source campaign 36137528865](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36137528865)
passed all four browser, Apple Silicon, Intel Mac and Windows Server jobs at
published `bdc15be`. The separate
[Windows AppContainer campaign 36137528676](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36137528676)
passed its worker, Java engine and canonical coordinator stages. Root downloaded
the new receipt, checked its 25 canonical predicates, ten derivatives and four
joins, and confirmed its actual merge tree equals `bdc15be`. Exact receipt
SHA-256: `3e9702d8410eec7a4310b6f876ed6251b14624ed75d74e93190b5000f53fbd6c`.
The [campaign interpretation](security/WINDOWS-PARSER-COORDINATOR.md) preserves
the platform and in-flight cancellation limitations.

Signed Figma-design handoff `4175252`, integrated as `ece7284`, retains two
[native editable DOCX frames](design/DOCX-SNAPSHOTS-DESIGN.md), their dimensions,
native text-control observation and exact Figma exports. Both frame re-exports
were byte-identical after a normal reload. The historical file's sharing was not
changed. This establishes those saved specimens, not owner approval, compact
behavior, complete report design or equality with the application.

The unchanged generator-2 ordinary specimen was inspected across all three
native Word pages. Exact decimal text, source identities, original CSV text and
limitations remain visible. A repeated table header sits alone at the bottom of
page 1, leaving a pagination refinement. The long-literal picker still did not
open its selected file; generator-2 edit/save remains unverified. The
[wrapping record](reports/DOCX-WORD-WRAPPING.md) retains the scope and failed
controls; operator-record SHA-256:
`c5fdaaac420afdf105dd0c4bdf71d8ae05a4482754a2ec2a4e7a3ae3a2d4b2cf`.

Signed static Python preflight `c3800a4`, integrated as `2de5148`, proposes
11,037 file locations and analyzes 588 arm64 load-command references at 196
native paths: 235 local and 353 OS-library references, with none unresolved or
ambiguous under its documented model. Eleven synthetic tests passed normally
and under optimization on Python 3.13.11 and 3.14.2. The
[complete static receipt](../packaging/evidence/python-static-preflight-2026-09-25.json)
has SHA-256 `2ec8e805fc0b0c815d274ab81f35837bb2f5c689617b3d9e48a5199ac778ef30`.
No candidate interpreter, package or startup hook was executed, and no package
was installed. Actual native loads, imports, relocation and confinement remain
required. All twelve complete-release gates remain false.

## Offline installation and report design integration — 25 September 2026

Application source `c2635c3` passes the production build and **125/125 real-core
Chromium workflows** (4.7 minutes). The full Python discovery suite runs 238
tests: 234 pass, four platform-specific checks skip and none fail. The earlier
`bef9f30` Rust run passes 438 ordinary cases, with 23 specialised exclusions,
and strict all-target Clippy; the relevant Rust source files are unchanged
between those revisions. The [integration record](verification/integration-c2635c3.json)
binds exact retained logs. The build's large-chunk advisory remains; these tests
are neither native WebKit acceptance nor a performance qualification.

Signed offline-installer handoff `0654a29`, integrated as `c2635c3`, preserves
the reviewed CPython input and wheel provenance while regenerating only the
58 primary installed RECORDs. Root's independent verification confirms
11,320 files / 601,821,300 bytes and the exact installed manifest. All 21
installer cases pass under normal/optimized development Python 3.13.11 and
3.14.2. The [receipt](../packaging/evidence/python-offline-install-2026-09-25.json)
records an assembled, unexecuted development prefix, not a runnable accepted
release component. Static loader review, actual confined execution and hostile
boundary verification remain distinct evidence categories.

Signed design/application handoff `a43ec4b`, integrated as `bbffc53`, includes
two corrected native editable Figma frames and their fresh reload observations.
The corrected catalogue's before/after exports are byte-identical; only the
final post-reload outcome export was retained. The [comparison](design/DOCX-SNAPSHOTS-COMPARISON.md)
records desktop/compact application captures, 13 affected workflow cases and
one final native-session protocol case, exact pagination, focus and nine axe
checks. Root visually reviewed the desktop, compact and saved-receipt captures,
then passed the full browser suite. No owner approval or complete manual
accessibility acceptance is inferred.

[Native Windows run 36141003773](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36141003773)
executed the exact signed source `b064c724` once and passed all five DNS cases.
Root independently validated the five raw events, equality with the native log,
report SHA-256 `ce0ee3b2dba5611b5052931f1e9e75f6b1925e6c3957e0577d29d47efccf2266`
and downloaded archive SHA-256
`1d054916d015a6b88939441a972c15e75dc41ff612f9320c12e2b9bac24876b8`.
The [source-bound record](discovery/verification/windows-native-dns.json) preserves
the Windows Server host, exactly three native launches, no HTTP/retry,
released-after-completion observation, quarantine refusal and unverified
provider quiescence. Retained-pending-completion was not exercised natively.
No historical Mac evidence changed and no release gate passed.


## Report pagination and actual confined Python compatibility — 25 September 2026

The [source-bound integration record](verification/integration-db55dc7.json)
retains full `e10e2e6` verification: **442 ordinary core tests**, 24 specialised
exclusions, strict all-target Clippy, 251 Python cases (247 passes/four skips)
and **13/13 affected DOCX Chromium workflows**. The subsequent test-only
import/quota diagnostics at `db55dc7` pass six focused Rust checks, strict
all-target Clippy and 252 Python cases (248 passes/four skips). No later full
core run is inferred from those focused checks. The prior complete 125-case
browser campaign remains bound to `c2635c3`.

Generator-3 [header pagination](reports/DOCX-HEADER-PAGINATION.md) preserves
exact historical generator-1/2 bytes. Eight renderer cases and canonical
publication/retry/export/restore for both historical versions pass. All seven
managed-rendered pages were visually inspected. The ordinary and long-literal
headers share page 2 with their first rows. Native Word controls timed out twice;
[the operator record](reports/verification/native-word-header-attempt.json)
records no generator-3 open, edit, save or native pagination proof.

The [compact design record](design/DOCX-COMPACT-ACCESSIBILITY.md) binds native
editable Figma frames `2006:53` and `2006:104`, exact post-reload export equality,
actual Chrome 200% zoom and keyboard recovery. Canonical readback proves all 21
prior snapshots unchanged plus one new capture. Root checked every retained
artifact hash and visually reviewed the catalogue and recovery-focus captures.
Manifest SHA-256: `328b1edaa27b5b173722e8f5e53eb2274ab35f7559bca7a58428c826f6115625`.
The record preserves its limited route and absence of final owner approval.

Signed Python source `fdf7265` first failed with generic quota exhaustion at the
imports checkpoint. Its [original receipt](../packaging/evidence/python-compatibility-first-2026-09-25.json)
remains byte-identical, SHA-256
`128fdb98c944e6b129ac10c94d34caba0e0baa03d924c06cec4b51d446a507c2`.
No individual import or particular exhausted limit is inferred from that run.
Signed diagnostic source `6e357b1` then passed exactly one second campaign,
with all fixed assertions, confirmed termination and cleanup. The
[successful receipt](../packaging/evidence/python-compatibility-second-2026-09-25.json)
has SHA-256 `12f3fb253e994fa5a903e87dd5337b544ca8a5546c5d138d1fd392836d9149ee`.
Preparation took 46,657 ms; spawn through reaping took 22,844 ms under unchanged
limits. Both diagnostic streams were empty. Independent verification retained
11,320 files / 601,821,300 bytes / 58 RECORDs unchanged. Root separately
validated exact native/outer receipt equality, expected results and source-file
hashes. This result establishes no cause for the first failure and no hostile
boundary, relocation, calibrated Splink or release claim.

[Source run 36142865319](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36142865319)
passes browser, Apple Silicon, Intel Mac and Windows Server jobs at published
`dbec31f`. [Windows run 36142865325](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36142865325)
passes worker, Java and canonical coordinator stages. The
[retained receipt](security/verification/windows-parser-coordinator-dbec31f-2026-09-25.json)
has SHA-256 `2592e4ae377b0f951f84f408cca9352445c3d37c6547066fbf21d5836ca9aef7`.
Root verified all 25 predicates, ten derivatives and four joins, and the actual
merge tree's equality with the published head. In-flight canonical cancellation
remains explicitly unproved. All twelve complete-release gates remain false.


## Combined durable collection, CSV and Python probe source — 26 September 2026

Signed combined source `090a350` passes **473 ordinary Rust tests** (397 library plus 76 integration), with 26 specialised/native cases excluded, strict all-target Clippy, **280 Python cases** (276 pass/four platform skips), production UI build and **138/138 real-core browser workflows**. The desktop test target compiled and ran zero unit tests; it is not a native-app interaction observation. The existing large-bundle warning remains. The [combined source record](verification/integration-090a350.json) retains log hashes and exact source/tree identities; SHA-256 `cb4f73a02e168bd1012edef6c03d39afd68bbe9a161a5f22d63dacf3f4935af7`. The release checker has valid policy, zero passed gates and twelve unpassed gates (expected exit 1).

The [durable collection design record](design/DURABLE-COLLECTION-UI.md) binds source `0248351`, native editable Figma frames and 20 focused browser workflows. Root verified all 27 frozen source digests and 17 retained artifact digests and inspected the preview design/application pair. Manifest SHA-256 `222ef02333576eca83bb456fbe918dd47a4ee97a019a68d870e7268733ef02e3`. Post-reload Figma equality remains unverified. The matching v23 API and interface are integrated together; old synchronous live collection now refuses, and normal native collection remains disabled. Exact-key recovery, preview and retained-source review do not establish a live campaign or enabled native controls.

The [CSV source observation](transactions/verification/typed-csv.json), SHA-256 `9bafb0fc9294f4223a7fcb58ba0dd0da66f4b6b18452ad122f43d4eb5089e8f2`, binds signed `55b9a45`: 13 independent checks across 35 actual Rust CLI commands, eight scope round-trips through Python's CSV reader, and exact old/new JSON artifact plus entire response bytes. Earlier harness/hash/cache mistakes are retained. The [native CSV save evidence](exports/verification/native-csv.json), SHA-256 `1e92d88581fb9f5221688642e128b10f1770ceb757d85597deeb16dfe4f8c59d`, covers 19 lifecycle cases including three new CSV cases, exact preparation/publication bytes, correction after preparation, ticket retry and no-clobber refusal. CSV receipts use version 2; older artifact receipts remain version 1. All 93 historical schema files were unchanged at that increment, with four new lifecycle schemas. These backend checks do not claim CSV controls, native UI or spreadsheet-consumer acceptance.

At signed `b502d98`, trusted release-mode prefix hashing passed in 7,432 ms without executing candidate Python or listeners. At signed `1abcc5e`, one hostile campaign passed with host before/after controls, assigned work, EPERM denials for sibling read and original/runtime write-open, and zero confined TCP/UDP listener deliveries. Confirmed termination and cleanup preceded independent unchanged-prefix verification. The [outer hostile receipt](../packaging/evidence/python-hostile-first-2026-09-26.json) SHA-256 is `697303830dab5bd5ced1b72c6bec4f00ad059465d03b0db3e2664a9f0cf709d3`.

The next relocated compatibility campaign at that same source failed at the unchanged whole-worker wall deadline (30,044 ms supervised), last checkpoint `spacy:before`. This is not a measurement of spaCy's individual elapsed time. Both copied and original prefixes remained independently valid after confirmed termination/cleanup. The [outer relocation receipt](../packaging/evidence/python-relocation-first-2026-09-26.json) SHA-256 is `3857dbcd9d3476ad9bc7ac6b7168bc07ce805883f50b1d447503606a06129788`. Root verified exact native receipt hashes, their outer projections (relocation omits null result) and all bound source hashes. There was no automatic retry, grant change or deadline increase. Earlier compatibility failures and successes remain immutable. Neither this loopback-only hostile observation nor prior source CI passes a release boundary gate.

The metadata-only [cross-target Python plans](../packaging/PYTHON-CROSS-TARGET-PLANS.md) select 58 Intel Mac and 60 Windows packages from the existing lock. Offline frozen resolver comparisons, exact lock/closure checks and five negative contract tests passed. No target archive was downloaded or executed by those checks. The broader root Python suite above includes these and the isolation harness tests.

Published `df4dbff` separately passed [source run 36146863047](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36146863047) (browser, Apple Silicon, Intel and Windows Server) and [Windows native development run 36146863028](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36146863028). Exact provider readbacks are retained privately; hashes `0e89fca6ec9d4fd4713ce3794f2467175f5e700a663bf5569e6c81e882caf928` and `c3f1a33098fe2c87d93555f646a6cc113cfa43acd1c9a90869a44ff9ac2939b4`. These results belong to that published source, not to a downloadable approved release or this newer integration. Required human review remains outstanding.

## Native HTTPS recovery, CSV application and account flows — 26 September 2026

One fixed native campaign at clean signed `8262223` made exactly two public HTTPS requests: the selected host's robots file and this project's immutable Apache-licensed synthetic fixture. It used the actual resolver, pinned TLS transport and durable coordinator; no redirect, retry or expansion occurred. Robots returned 404/14 bytes; the seed returned 200 and exactly the expected 863 bytes. The campaign recorded 5,006 ms and the owned process 5,932 ms, with confirmed shutdown and ownership release. The [source-bound native receipt](discovery/verification/native-durable-https-first-2026-09-26.json) SHA-256 is `10e2f6ad95f2b24b746739aea587066215b149315f4720e1f1bb059f3638895d`.

Root independently validated the receipt/event chain, every retained original hash and exact fixture bytes, SQLite integrity, and equality of all six canonical tables across source, backup and restored workspaces. The [independent checks](discovery/verification/native-durable-https-first-root-checks.json) have SHA-256 `975251c019f8e3748813d20151885d9ee05ade3a1b959831481a8ea19b3d02a0`. This was one owned synthetic source, not general publisher permission, useful cold-corpus coverage, HTTP cancellation, Windows execution or normal application activation. Normal native collection remains disabled. The harness's earlier source-only verification made no public requests; ordinary transport tests may use synthetic loopback TLS.

The [CSV design and application record](design/TYPED-CSV-EXPORT-UI.md) binds two native editable Figma frames, 38 browser compatibility cases and nine final CSV cases after wording corrections. Root verified all 17 retained source digests and 20 artifact digests and visually inspected both Figma specimens and wide/compact app controls. Post-reload export equality remains unverified. A separate actual Mac WebKit development app at clean `0f65c56` refused mixed-review CSV by default, saved all three synthetic records after explicit inclusion, reused the existing exact file on repetition, and retained its workspace through normal quit/reopen. Subsequent JSON save preserved the historical array contract. Independent readers matched every exported field to canonical data; all tables and originals remained unchanged and staging was empty. The [native check manifest](transactions/verification/native-csv-ui-first-2026-09-26.json) SHA-256 is `0ac07afe85922e2264a4396dbe1692f10125630361650ba3321e8b344f25b6cb`. No spreadsheet-consumer, signed installer or other-platform acceptance is inferred.

Summary/report calculations now refuse invalid transfer markers rather than dropping their rows. Source `f58c711` passed 476 ordinary core tests (26 exclusions), strict Clippy and three focused cases after a meaningful two-failure reproduction. The [summary validation record](verification/summary-transfer-validation.json) preserves the reproduction and historical report-byte checks; SHA-256 `3332e4a45ed5f209cd18a0e6160fd3b5b7cd599cb5f66c82ef388201f50029af` binds the retained observation. Pattern analysis still retains invalid marked rows with its explicit unverified annotation.

The [account-flow backend](transactions/ACCOUNT-FLOWS.md), source `e370c8d`, adds Command v25 and verified directed internal-transfer pairs without inferred external counterparties. One pinned canonical snapshot separates selected activity from out-of-scope support. It retains exact account/currency totals, every review partition, pair/source IDs and versions, with complete refusal on size limits. Fourteen focused cases and 493 ordinary core cases (26 exclusions) passed, along with debug/release strict Clippy. A real concurrent correction cannot mix revisions; a later correction removes the current pair while historical report bytes remain unchanged. All 97 historical schema files remained byte-identical. The [source record](transactions/verification/account-flows.json) SHA-256 is `1af53d71ac3271eff8654c3ed292d1f798d757c48cfde45386ec4b5a23512fb3`. The interface and native graph workflow are still separate work.

At source `4d76120`, a single relocated Python diagnostic again exhausted the unchanged 30-second whole-worker deadline: 30,020 ms supervised, last direct checkpoint `spacy:before`. Completed DuckDB and NetworkX import pairs measured 736/24 ms and 307/306 ms elapsed/process CPU. The later 21,035 ms language-to-CLI attempt interval contained 339 ms process CPU; attempt records are not import completions and do not establish the cause of the delay. Confirmed termination and cleanup preceded independent unchanged original/moved-prefix verification (11,320 files, 601,821,300 bytes, 58 RECORDs). The [retained negative campaign](../packaging/evidence/python-relocation-diagnostic-first-2026-09-26.json) SHA-256 is `111d063769297fe9fe44917a315becd5aa7cf423edef4c3adfd3da9786b2e5b5`. Earlier successful original-prefix compatibility and hostile observations remain separate evidence. No retries or deadline/profile/package changes occurred.

Published `3875aea` passed [all four hosted source jobs](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36151305884) and the [Windows AppContainer/Java/canonical development campaign](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36151306010). Exact retained provider readbacks have SHA-256 `7efb19685965f997b207b0404a36e29c4b7310aa61b18f325957151a1ea7e63c` and `8a7345e7b3453717095a2dabbae402b7f652b4a2b4face40f0c503de6562d42f`. Windows Server 2022 evidence does not establish Windows 11 installation. Required human review remains outstanding and newer combined source needs its own hosted checks.

## Combined source and separate relocated analytical engines — 26 September 2026

Clean combined source `a8f504dfc5f929df546a3363c265b2595f612ee1` passed 501 ordinary Rust tests (422 library/79 integration), with all 29 specialized/native cases excluded, strict all-target Clippy, formatting, 309 Python cases (305 pass/four skips), the production UI build and 147 real-core browser workflows. Desktop compilation passed with zero unit tests and no bundled resources; the separate native CSV application observation above remains the actual interaction evidence. The existing Vite chunk warning remains. Release policy is valid with zero of twelve gates passed. The [integration record](verification/integration-a8f504d.json) has SHA-256 `2c94eaa29ee4530fa0683acd2c698e7b0f82f72b2822da91d553e6e038979739`. Subsequent engine evidence changes documentation/receipts only.

At clean signed `46e6c1e`, exactly one fixed NetworkX worker and one fixed DuckDB/PyArrow worker passed in separately relocated verified prefixes. The second invocation proceeded only after the first's confirmed lifecycle, source/binary identity and unchanged true-original/moved inventories. NetworkX's exact path/assertion/unreachable checks took 1,192 ms supervised; the exact per-currency decimal totals/source IDs and unverified-transfer refusal took 20,272 ms. Both stayed within the unchanged 30-second whole-worker limit. No candidate warm-up, package/profile change or retry occurred. All 58 metadata versions matched, all prefixes remained unchanged and no job directory remained.

The [engine observation record](security/PYTHON-ENGINE-RECIPES.md) separates initialization and operation pairs from trusted preparation and preserves exact actual expanded profile/binary identities. Outer receipt hashes are `83bf13dd7e5d2b4cea40ae8eee340c048dc16556d75f158859e57af71ba9f97e` and `e8e62b5635589f8efbb68d4fb60bbc478a05fb8bbee47dec67ab3f029d763bdc`. Root independently revalidated each full native/outer receipt and exact result, all 24 source hashes and six retained log/artifact hashes per recipe, inventory-receipt equality and paired-clock arithmetic. The [root checks](verification/python-engine-root-checks.json) retain that scope and an initial verification-script shape correction. No further candidate invocation was used for those checks.

These successful fixed operations support the next canonical snapshot/protocol increment. They do not establish reliable startup, cold-cache performance, full ledger capacity, spaCy/plugin compatibility, calibrated Splink, production job activation, signed helpers or clean-platform acceptance. The combined relocated spaCy timeout remains an unresolved negative observation.

The Windows source job at published `ecf84fe` failed in five subcases of an offline timeout test: the fixture attempted to patch Unix-only `os.killpg` on Windows. No native campaign was invoked. The test now explicitly models its OS and signal boundaries while retaining all timeout, signal/reap failure, single-launch and no-retry assertions. Removing those host attributes reproduces all five earlier errors and now passes; nine focused tests pass normally and under optimization, and all 309 Python cases run with 305 passes/four skips on the development Mac. The [retained failure and correction record](verification/windows-timeout-fixture-fix.json) binds source, logs and the exact edited test. Application networking/confinement code is unchanged; corrected Windows hosted acceptance remains pending.


## Reviewed account flows, owned graph validation and cancellation — 26 September 2026

Combined production source `817d200` passed **520 ordinary Rust tests** (441 library/79 integration), with 30 specialised/native cases excluded, strict all-target Clippy, formatting, **317 Python cases** (313 pass/four skips), and the production UI build. The first browser run passed 157 cases and failed before one case entered: fixture removal reported `ENOTEMPTY` after the preceding navigation test released a held response without awaiting all real response handlers. Test-only correction `73d25e2` retains those handlers through fulfillment, awaits remounted facet readiness and uses [Playwright's documented waiting unroute](https://playwright.dev/docs/api/class-page#page-unroute-all). It adds no sleep, deletion retry or weakened product assertion. The two affected cases passed, followed by **158/158 complete real-core browser workflows**. All production source files are unchanged by that correction. The [integration record](verification/integration-817d200.json) retains exact source identities and all initial/corrected log hashes. The existing bundle warning remains, and all twelve release gates remain unpassed.

The [account-flow design record](design/ACCOUNT-FLOWS-UI.md) binds two native editable Figma frames, final exports after fresh navigation, and wide/compact/source-dialog comparisons. Root independently verified all 23 source digests, 99 historical JSON schema digests, 15 artifact digests and seven sanitized logs, then inspected all five final images. Manifest SHA-256 `459ce18fa06b15d8fd167a5ad4f8c400c2624421b497dfa7a922f37434b5b9dc`. Controls preserve exact separate currencies, both endpoints, selected/support denominators and revision-bound source access; stale or failed reads disable source actions. This source/browser observation does not establish native application, 200% zoom, full assistive-technology or owner acceptance.

The [owned graph snapshot](analysis/GRAPH-SNAPSHOT.md), signed source `98e4398`, reads one pinned canonical revision, retains every parallel accepted assertion and verified text source privately, and passes only bounded IDs/topology to a future worker. A consumed handle binds workspace instance, fresh nonce and actual record fingerprints. Rust independently checks shortest paths and unreachable claims; worker-supplied provenance is refused. A peer review found and repaired an internally tagged Serde unit variant that could ignore unknown fields. Positive disconnected controls and forged-field regressions now exercise the empty struct variant. Fifteen focused tests and the combined suite above pass, including a deterministic real concurrent writer during capture and validation. Results remain read-only interpretations at their captured revision, with explicit undirected/all-retained-time limitations. No native worker launch or publication authority is added.

At clean signed source `040f5cf0`, exactly one actual native cancellation campaign passed at verified seed response headers. The normal coordinator committed cancellation intent before signalling its token; ordinary body processing returned `Stopped(Cancelled)`. The run retained only its complete 14-byte robots original, with two charged/launched requests, no seed original or promoted page, confirmed joined shutdown, released ownership and terminal resume refusal. Root independently matched every canonical table and retained original across source, backup and restored workspaces. [Actual receipt](discovery/verification/native-cancellation-first-2026-09-26.json) SHA-256 `32303f54b87f26fd42226d821b08485bac0df7f4b0f3fe12cbaba47a42313a50`; [independent checks](discovery/verification/native-cancellation-first-root-checks.json) SHA-256 `ad6c3cedd029cd3250f67159d4018242fbe54a6809e5ec0dbbf3749ce9cf0b53`. Buffered body bytes may already have arrived; this proves owned-response cancellation at the stated point, not interrupted server delivery, every transport phase, Windows behavior or normal activation.

Published `9a53cda` passes [all four hosted source jobs](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36159431035) and the [Windows AppContainer/Java/canonical development campaign](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36159430886). Exact provider readback hashes are `2d7faed98d57354359279ae412f462c5d5ee90e0963e1d6057fcb5fef002a59a` and `55d47daaff1598fad14560d36fe12bdb8068b1fd054a0b3c8657ae065ff1d714`. The earlier `ecf84fe` Windows test failure remains retained; its other three source jobs and separate confinement campaign passed. These hosted results belong to those exact earlier revisions. Windows Server 2022 is not clean Windows 11 installation, and human PR review remains required.

The subsequent [fixed Python graph adapter](analysis/PYTHON-GRAPH-ADAPTER.md), source `3b51bee1`, passes 13 strict stdlib contract/stream tests, ten real NetworkX development tests and 16 Rust graph cases. Three fixtures match actual captured Rust requests and independently validated results, normalizing only the necessarily fresh test nonce. Root reran all 16 graph cases and 13 stdlib cases after integration and independently matched 22 entry-point files plus two upstream source hashes against the [static inspection](analysis/python-graph-backend-static.v1.json). No backend-info provider is present; the only backend entry is the ignored built-in loopback. NetworkX import can otherwise execute backend-info providers, so the future bootstrap must validate the complete prefix, explicit paths and environment before import; selecting the core algorithm backend alone is insufficient. The [root check record](verification/graph-adapter-root-checks.json) retains the independent scope. This adapter has no launcher, canonical write or publication authority, and no staged interpreter ran in these checks.

Actual isolated native account-flow interaction subsequently passed at `817d200`: six selected/two support rows and two pairs totalling 40.00 AUD; both source endpoints/originals; a native correction from `-20.00` to `-21.00` at revision 12→13; stale source actions disabled; recalculation to one 20.00 AUD pair and one support row; normal quit/reopen retaining all ten records. Root independently checked 41 raw artifact hashes, nine source hashes, nine frontend hashes and three bundle hashes, compared the current read-only database to the retained snapshots, verified the exact three existing record changes and one added decision, and recomputed both pair totals/IDs using decimal arithmetic. All original bytes were unchanged, and all five application tables plus database bytes were unchanged through the respective read-only and post-correction restart phases. The final app process was absent. [Native receipt](transactions/verification/native-account-flow-first-2026-09-26.json) SHA-256 `2602b5986a2c8ccfc82a34e0ded893dd28e3ad520af08774d3cedd91b940b1bf`; [root checks](transactions/verification/native-account-flow-root-checks.json) SHA-256 `5c7d53ed46b7116af89904999b7b1ec51c5b9bc7111c177ad26c3a8b2ea575d4`. This resolves the earlier browser-only limitation for this finite workflow on the available Mac; resources were excluded and no complete release gate passed.


## Bounded HTML interpretation and replay compatibility — 26 September 2026

The [v4 collection policy](discovery/BOUNDED-HTML-INTERPRETATION.md) replaces repeated ancestor scans with iterative extraction and adds explicit parser admission and output limits. A refusal retains the complete response, acquisition, truthful receipt and request charge without promoting partial text, links or a successful page. Historical v1–v3 records keep their policy and canonical bytes: admitted inputs replay exactly; over-limit interpretation refuses visibly without rewriting the journal. The fixed native proof profiles remain explicitly v3.

Source `b0971cc` passed the extraction/storage regressions, 530 ordinary Rust tests, debug/release strict Clippy and 13 collection browser workflows. Its [retained evidence](discovery/verification/bounded-html-interpretation.json) includes a meaningful failing-before regression: chunked parser feeds initially erased an interior U+FEFF; the correction preserves the pinned driver's initial and script-resumption behavior. Root matched all 21 source-file, 100 historical-file and 16 log identities; an independent read-only review found no remaining concrete blocker.

Integrated clean source `e7684c2` additionally includes the canonical graph adapter. It passes **531 ordinary Rust tests** (452 library/79 integration, 30 native/specialized exclusions), strict all-target Clippy, formatting, **330 Python cases** (326 pass/four platform skips), the production UI build and **13/13 affected collection browser workflows**, including compact axe checks. The [integration record](verification/integration-e7684c2.json) binds the retained logs. The prior full 158-browser result remains associated with its original source.

These counters do not establish a hard CPU, RSS, latency or cancellation bound: a parser call is not preemptible, node count is checked after a call, depth after construction, and parsing/replay still hold the workspace mutex. No native or public-network campaign ran for this change; native activation remains false. Release policy remains valid with zero of twelve gates passed.


## Actual captured graph worker and independent acceptance — 26 September 2026

Exactly one [canonical graph experiment](analysis/PYTHON-CANONICAL-GRAPH-PROBE.md) ran from clean signed `f76ef907`. Rust captured a real synthetic workspace at revision 2, the confined relocated packaged Python/NetworkX worker returned `a,b,c`, and the owned Rust validator restored both parallel first-hop assertions. The worker exited 0, termination and cleanup were confirmed, and stdout/stderr were empty. Its supervised interval was 1,185 ms; this single observation is not a benchmark or a cold-start guarantee. Another agent's brief compile ended during preparation/build, before candidate execution. No retry, warm-up or limit increase occurred.

Root independently accepted the raw receipt/request/result/wrapper bindings, all 33 executed-source hashes and actual binary identity. A read-only SQLite reader passed integrity checking, reproduced the exact six-table/schema/user-version logical-content hash, independently rebuilt the accepted graph and shortest path, checked parallel assertion IDs and verified the sole original. Full true-original and moved-prefix checks again matched all 11,320 files / 601,821,300 bytes / 58 RECORDs. No assignment directory or WAL remained. The [root verification record](verification/canonical-graph-root-checks.json) has SHA-256 `b7e06aa8506d7bbad5a5d375564fd944f6f175d6cb8dc36fce2e7be0acb3d8f4`; exact public copies match the private raw observations.

Before execution, source `f76ef907` passed 526 ordinary Rust tests with 31 native cases ignored, strict all-target Clippy/formatting, and 340 Python cases with four skips on each of two trusted development interpreters, normally and under optimization. At integrated `774a8fc`, root ran 19 graph-capture and three recipe-filter cases (one overlapping case), ten Python contract cases and strict all-target Clippy; all passed. The [source-check record](verification/canonical-graph-source-root-checks.json) retains their exact logs. This test-only recipe does not add an application command, durable analytical queue or canonical result publication. Production integration, larger capacity, other platforms, signing and clean installation remain open.

Published `db1674b` passed [all four hosted source jobs](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36163175070) and the [Windows confinement/coordinator development campaign](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36163174957). Retained final provider readbacks have SHA-256 `9fc81962af13f6db0241cc54ee46df77606660112800b6be1e6ac84493ac1cf5` and `a3e999cd6fa82d0dd3e792ec8bfdcd3dfe250b7bd41cf8e070e896ea42701ade`. The Windows host is Server 2022; this is not Windows 11 installed-artifact acceptance. Later source requires its own hosted result. All twelve release gates remain unpassed.


## Collection settlement outside the workspace mutex — 26 September 2026

The [v4 settlement path](discovery/OFF-LOCK-HTML-SETTLEMENT.md) captures bounded canonical metadata, releases the workspace mutex for historical replay and pending HTML interpretation, then consumes a private prepared handle after exact run, owner, receipt, body and original revalidation. A single canonical cancellation appended during preparation is preserved. Complete responses retain their originals and charge; cancelled results cannot promote text or links. Unrelated canonical changes are allowed only while the exact captured inputs still match, and the final revision-checked transaction closes the write gap.

Source `61399ef` passed 540 ordinary Rust tests, 143 focused collection cases, strict debug/release Clippy and formatting. Peer review exposed a clock-rollback defect in stop/cancel publication: two regressions failed before the repair. Their [retained evidence](discovery/verification/off-lock-html-settlement.json) keeps that failure, subsequent repair, and initial test compilation failure. Real synthetic local-TLS coordinator cases pause preparation and demonstrate workspace access, canonical cancellation without another fetch, complete-body retention, and ownership held until joined shutdown. Root independently matched all 11 source files, 100 historical schema-directory files and 13 retained artifacts; historical schemas and fixed v3 native proofs are unchanged.

Clean integrated `4c83d62` passed **546 ordinary Rust tests** (467 library/79 integration, 31 native/specialized exclusions), strict all-target Clippy, formatting, **340 Python cases** (336 pass/four platform skips), the production UI build and **13/13 affected collection browser workflows**. The [integration record](verification/integration-4c83d62.json) binds the logs and root handoff checks. Expected refusal-fixture reports printed by Python tests are retained; the suite passed. The existing large-chunk build warning remains. This integration check invoked no native candidate or public-network campaign.

This resolves mutex-held interpretation for coordinator settlement only. General inspect/cancel/start/advance still replay historical HTML under the mutex, as do bounded metadata capture and original rehash/write/sync at commit. Parser calls are not cooperatively preemptible. Capture admission may retain a pending response for explicit recovery; no hard memory, latency or cancellation-deadline claim follows. Normal native v4 collection remains disabled, and all twelve release gates remain unpassed.

Published `ebf7d65` passed [all four hosted source jobs](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36164971607) and the [Windows confinement/coordinator development campaign](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36164971629). Exact retained provider readbacks have SHA-256 `a6719dfed01277ffd4600d31c932e89cb50948ba8910d7f7295e45c1ced2c213` and `8e110e82b84db91672196064112518fd47a271540f5e2193fe72fad8f3523642`. These successes apply to that earlier revision; newly integrated source requires fresh hosted checks. Windows Server development results do not establish clean Windows 11 installation or approved release artifacts.


## Prepared collection reservation and durable graph foundation — 26 September 2026

The [v4 reservation path](discovery/PREPARED-COLLECTION-RESERVATION.md) captures canonical inputs, releases the workspace mutex for one historical replay, then consumes the prepared value after exact input, owner, original, protocol and mode revalidation. The canonical request charge still commits before fetching. A single canonical Cancel suffix is allowed; Cancel and Advance retain their separate expected-revision writes, so an intervening write cannot be adopted silently. An attached v4 driver cannot fall back to an older protocol if its stored record is replaced. Synthetic local-TLS tests cover committed-charge visibility and refusal without fetching; a pause before replay demonstrates available workspace access, not a measured full-duration latency bound.

Root independently checked all 11 source identities, 100 historical schema-directory files and 12 artifacts against the signed [source evidence](discovery/verification/prepared-collection-reservation.json). A focused integrated run passed 154 collection cases with two native exclusions. Public inspection/cancellation/resume, initial attach/start, startup recovery and direct-store Advance remain separate work. Bounded capture, original rehash and commit I/O still hold the mutex; no parser preemption or hard resource guarantee is established.

The [graph-job foundation](analysis/GRAPH-JOBS.md) adds internal queue admission, private attempt identity, atomic revision-bound result publication and frozen historical records. Production claims explicitly become Blocked before Running or capture because an exclusive automatic-work interval and supervised executor are not yet integrated. The test-only claim writes Running and actual revision C, then captures within the same immediate transaction; failure rolls back the whole claim. Publication validates originals, job/request/attempt/lease identity and the complete shortest-path result within its transaction, and commits the immutable record with Completed at P = C + 1. Database/retention-limit failures preserve the attempt for publication retry. Exact completed delivery is a no-write replay. Later source corrections preserve frozen values and report staleness; original corruption remains separately unavailable.

Independent source review found and repaired failed-claim rollback, database-error consumption, request/job/attempt substitution and insufficient fault-injection observations. Root matched all 18 files against signed source `5891fa6`, all 100 historical schema-directory files and 14 public/raw log references in the [graph source record](../packaging/evidence/graph-jobs-source-2026-09-26.json). Seventeen source files match the integrated tree exactly; `lib.rs` also retains the previously integrated private HTML module. New processing-job v5 and graph-analysis v1 schemas are additive; there is no database migration. This is source-level lifecycle verification, not a new native execution or termination observation.

Clean integrated source `7f95843` passed **577 ordinary Rust tests** (498 library/79 integration; 31 native/specialized exclusions), strict all-target Clippy, formatting, **340 Python cases** (336 pass/four platform skips), production UI build and **13/13 affected collection browser workflows**. The [integration record](verification/integration-7f95843.json) binds actual logs and handoff checks. Expected negative Python fixture reports and the existing UI chunk warning remain retained. No native candidate or public-network campaign ran. The earlier full 158-browser result remains pinned to its original source. Normal native collection remains disabled and all twelve release gates remain unpassed.

Prior published `aea1ada` passed [all four hosted source jobs](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36168164612) and the [Windows confinement/coordinator development campaign](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36168164702). Final readbacks have SHA-256 `cc4160870acf9f90f3a91af68e2e1fe04da8790dab935880e902804906b898b3` and `9436d63a016191877c2eb27656a64c9368758be813ec2def9f600c5c0adf7386`. Newly integrated source requires its own hosted checks. Windows Server source/development evidence does not qualify clean Windows 11 installation.


## Public Cancel preparation and refusal recovery — 26 September 2026

[Public v4 Cancel](discovery/PREPARED-PUBLIC-CANCELLATION.md) now captures once, replays historical HTML outside both workspace and collection-lane locks, and exactly revalidates before publication. The bounded acknowledgement is projected and serialized before any write. A successful canonical Cancel precedes signalling the currently matching token. Repeated cancellation and the exact single-Cancel suffix are no-write acknowledgements; unrelated source/run/owner changes return explicit refusal without signalling. Running still means cancellation intent until the ordinary settlement proves the outcome. Strict fresh-event timestamp rules remain separate from replaying an already-recorded Cancel after clock rollback.

Root reviewed the complete production change and eleven new regression groups, then independently matched **seven source files, 102 unchanged historical schema-directory files and 18 retained artifacts** against signed source `4773b54` and [its evidence](discovery/verification/prepared-public-cancellation.json). The initial zero-match test filter is retained rather than counted as coverage. The seven store groups cover exact legacy event/projection parity, reopen, clock, ownership/source/protocol drift and preflight/transaction failure. Four coordinator groups demonstrate access to both locks at a controlled pause before replay, actual committed-state visibility after token signalling, current unsignalled tokens on response/publication failure and refusal after recovery/quarantine/stopping. This is not a full-duration responsiveness measurement.

Clean integrated `93eccb3` passed **588 ordinary Rust tests** (509 library/79 integration; 31 native/specialized exclusions), strict all-target Clippy, formatting, the production UI build and **15/15 collection browser cases**. The existing large-chunk build warning remains. Thirteen cases use the existing real core flows. Two new refusal-recovery cases deliberately inject a stale enabled Cancel inspection: one sends the request to the actual refusing standalone core, and one injects a preparation-conflict HTTP response. Both verify cleared controls/outcome, an unconfirmed error, unchanged canonical state, refresh to actual disabled controls and no mutation retry. They test interface behavior and do not establish enabled native collection or an actual browser-to-coordinator conflict. Rust coordinator regressions cover the real canonical conflict.

The [integration record](verification/integration-93eccb3.json) binds every command exit and log. Python source and dependencies are unchanged from `7f95843`; its earlier 340-case result is referenced without implying a rerun. No native candidate or public-network campaign ran. Bounded capture, final original rehash, response serialization and canonical I/O still hold the locks; normal inspection/catalogue/resume/start/recovery and direct store paths retain existing replay behavior. No hard cancellation deadline or native activation is claimed. All twelve complete-release gates remain unpassed.

Published `c097f0f` passed [all four hosted source jobs](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36171538500), with final readback SHA-256 `d05a17bb50dfaae714a33e0abaa31e5d74baf5a9166236e509f4509b9c81c0da`. That source result precedes the Cancel change; the new head requires its own hosted checks.


## Exclusive graph scheduling, fixed adapter and frozen collection export — 26 September 2026

[Integrated source `3a48786`](verification/integration-3a48786.json) passed **554 library + 79 integration tests**, with 31 native/specialized tests explicitly excluded. Strict all-target Clippy and formatting passed. Python tooling passed 341 of 345 tests with four expected skips; negative synthetic fixture reports printed by that suite are not native executions. The interface build retained its existing large-chunk warning, and all **47 affected browser workflows** passed. The initial `npm --prefix ui run build` invocation failed because the package is at repository root; its error log is retained, and `npm run build` passed without changing source.

The signed [scheduler evidence](../packaging/evidence/graph-scheduling-source-2026-09-26.json), [adapter evidence](security/verification/python-graph-adapter-source.json) and [export evidence](discovery/verification/durable-collection-export.json) retain their independent source revisions and earlier failures. Integration verified 10 scheduler source files, 102 historical schema-directory files and 18 raw/public log pairs; 23 adapter files, 14 logs/artifacts and 12 test executables; and 12 export files, 102 historical files, three additive schemas and 20 artifacts. The sole subsequent scheduler-file difference is the separately reviewed [private configured executor composition](analysis/GRAPH-EXECUTOR-COMPOSITION.md). It derives scratch from the owning workspace and leaves normal startup unavailable.

Source regression tests establish shared admission, existing-run draining, Resume refusal before writes, atomic capture rollback, retained publication/retry ownership, counted cancellation/shutdown settlement and unknown-exit quarantine. Export regressions establish WAL writer exclusion through marker publication, exact directory inventory, no-clobber/uncertain-result behavior, frozen original checks and backup/restore re-export equivalence. These tests do not establish native interpreter execution through the composed coordinator, installed artifacts, power-loss durability or full discovery coverage. The adapter's attempted local Windows cross-check could not compile native dependencies because its cross-compiler was absent; no Windows pass is inferred.

The previous published revision `4bdcb5b` passed the [four hosted source jobs](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36172975551) and [Windows Server development confinement check](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36172975490). The current policy-only checker remains valid/incomplete with zero accepted gates and no verified candidate.


## Deterministic wheelhouse fixture repair — 26 September 2026

The Intel job in [source run 36176771759](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36176771759/job/108209167745) failed a synthetic manifest determinism assertion at `8b94b17`. The test created two ZIP inputs independently, allowing embedded timestamps to change their bytes. A retained forced-clock reproduction assigns seconds 0 and 2 to the two constructions and reproduces the failure. The repair constructs one input and stages those same bytes twice; the unchanged reproduction then passes with one construction. All 19 wheelhouse tests and the full 345-case Python suite pass (341 passed/four skips). Production staging and integrity checks are unchanged. The [evidence record](verification/wheelhouse-clock-fixture.json) retains both outcomes and the hosted failure log identity. A repaired-head hosted pass is still required; no release gate changes.


## Atomic collection publication retry — 26 September 2026

The [Windows source job](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36177541527/job/108211696804) at `0d97a6e` exposed a collection retry race. Consuming a retry cleared its request flag while leaving the lane briefly marked SettlementPending, so another caller could observe false retry availability. A deterministic observer reproduced that window and failed before the fix. Consumption now sets Settling under the same mutex. The regression verifies that both public and internal retry routes refuse a second request while the first is starting, the canonical revision and reserved request remain unchanged, and only one synthetic transport call occurs.

The [retained evidence](verification/collection-retry-transition.json) records the hosted failure, deterministic before/after runs, all 30 collection tests, **555 library + 79 integration passes**, 31 explicit native/specialized exclusions, strict all-target Clippy and formatting. An initial overly specific test filter selected zero cases and is retained without a pass claim. Native execution, unchanged Java/Python workers and browser behavior were not rerun for this Rust transition repair. Exact repaired-head hosted verification remains required; all release gates remain unpassed.


## Evidence search result consumption — 26 September 2026

The [UI contract](discovery/EVIDENCE-SEARCH-CONSUMPTION.md) preserves returned rank by mapping validated hit IDs to canonical evidence. Captured query/revision generations discard old responses after edits, navigation or workspace changes. One active plus one latest queued read bounds pending commands; invalidation does not pretend to stop an active index worker. Search pending/error is separate from workspace mutation state. Existing controls, layout and styling are unchanged.

[Signed source evidence](discovery/verification/evidence-search-consumption.json) retains **21 affected browser passes**, including eight new search cases, and the production build. The source/lane contract and all four source plus 15 artifact identities were independently checked at integration. Retained wide/compact screenshots were visually inspected, with zero axe violations and no horizontal overflow in those two views. [Combined source `c2dfd80`](verification/integration-c2dfd80.json) subsequently passed the build and **23 collection/search workflows**. Canonical imports, source inspection and delayed mutation replies use real Rust; new index replies are explicitly injected. No Lucene execution, ranking-quality measurement, total precision, new Figma frames or release acceptance is inferred.


## Intel Python runner compilation boundary — 26 September 2026

The [Intel job at `0d97a6e`](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36177541527/job/108211696671) passed the repaired Python tools and Rust tests, then failed strict Clippy because ARM-only Python application support was compiled without a production consumer on Intel. Local Intel-target cross-compilation reproduced all 18 production errors. The narrow target predicates now compile shared support for ARM64 production or test consumers, and restrict the fixed application executor and inventory constants to ARM64. Eight ARM helper source tests and strict all-target Intel cross-Clippy pass; no warning is suppressed and no Intel runtime is enabled. The [evidence record](verification/python-intel-compilation.json) retains hosted and local before/after logs. This compilation result does not establish native Intel execution or release acceptance.

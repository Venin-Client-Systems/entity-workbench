# Development verification — 2026-09-23

These observations describe the initial implementation, not completion of the full plan.

| Check | Observed result | Scope |
|---|---|---|
| Rust domain/workspace suite | 49 tests passed (42 integration, 7 unit) | Exact decimals/dates; atomic import; duplicate retention; review conflicts; correction propagation; transfer exclusion; currencies; reversible merges; backup/restore; corrupted originals; newer schemas; symlink paths; escaped reports; uncertainty; public-IP policy; worker-message limits; discovery budget; static HTML extraction; missing balances; acquisition provenance; general identity authoring; source-anchor validation/excerpts; identity decision recovery; assertion invalidation; robots/transport/rate-limit outcomes; redirect scope; bounded source expansion |
| Rust static analysis | Clippy passed with warnings denied | Workspace and test targets |
| UI production build | TypeScript and Vite passed | Bundled React/Cytoscape/ECharts/MapLibre assets, map worker and licensed Inter font |
| Browser workflows | Six passed against real Rust commands | Seed, transaction correction/acceptance, merge/reversal, local map, report snapshot and persistence; no external browser requests; no uncaught page errors. Empty-workspace authoring adds two namesakes, reviews cited observations, records separate/deferred decisions, corrects an observation and checks persistence. Statement workflows verify delimiter/mapping preview, saved profiles, original cells, duplicate rejection, invalid rows and delayed-response invalidation |
| Accessibility and keyboard | Eight tested section states and both transaction review layouts have zero axe violations; keyboard and minimum-width checks passed | Persistent desktop review, compact native modal, draft retention through resize, out-of-filter selection, nested source anchors, Escape, return to opener, 960×640 layout; remaining manual checks in `design/accessibility-results.json` |
| Native desktop build | Windows x64, macOS Apple Silicon and macOS Intel source builds passed in GitHub CI at `b6f80f0`; local Apple Silicon development app bundle built | Not signed/notarized product distributions; clean offline installation remains unverified |
| Java engines | Tika synthetic text parse and five Lucene queries passed on Java 21.0.12.1 | Boolean, phrase, proximity, fuzzy, fielded queries; revision returned |
| Rust → confined Lucene | Phrase and fuzzy query returned expected evidence and revision; native app phrase search returned the expected synthetic source | Staged app-local Java, local index, macOS development profile |
| Hostile Java development probe | Passed after dyld profile repair | Control worker could access sentinels/network; confined worker could not; assigned job I/O still worked |
| Python engines | 3 tests passed | Exact DuckDB/Parquet totals with record IDs; reviewed NetworkX paths; spaCy phrase candidates |
| Direct website collection | One public documentation page retained with original SHA-256 and static text | `https://example.com/`, 2 requests including robots policy; no search provider; not broad-web coverage |
| Complete release gate | Unpassed | Explicit machine-readable gates in `release-gates.json` |

The Java hostile probe exercises disposable synthetic sentinels only. It does not establish confinement for Python, Tesseract, Chromium, all JVM/native libraries, descendant processes or every supported OS version.

The source CI matrix targets Windows x64 and both Mac architectures. The native jobs initially passed in [the initial source run](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/35717611878). Its Ubuntu browser job could not launch Chromium under the hosted AppArmor policy. Browser verification now uses macOS with Chromium sandboxing still enabled. A later browser run exposed an intermittent identity-history assertion: tests now require the saved-history region, and decisions require a comparison at the current workspace revision. A deliberately delayed real comparison response verifies the stale-state guard.

CI source compilation is separate from clean offline installation, Windows 11 runtime acceptance, signed helper validation and downloadable artifact testing. Consult [the current pull request checks](https://github.com/Venin-Client-Systems/entity-workbench/pull/1/checks) for verification of the latest changes; successful earlier builds do not pre-claim current CI success.

The five collector unit tests use a synthetic transport script. They verify state-machine outcomes and bounded link expansion without any live network access. They are not evidence of live broad-web coverage, DNS behaviour or end-to-end source discovery.

No performance claim is made. The required 16 GB large-corpus benchmark has not been run. A locally fast synthetic test is not a substitute.

## Editable design pass

Real Figma Design frames, styles and component sets were created and exported. The first implementation applies the native foundations to the working UI; [handoff](design/HANDOFF.md) records frame links, comparisons, measured contrast pairs and remaining discrepancies. Final visual approval remains open.

The rebuilt Apple Silicon development application was launched with the new palette, bundled font and persistent transaction panel. Native source inspection returned the preserved CSV amount and exact row anchor. WebKit initially returned focus to the document when the source dialog closed; after the explicit focus fix, native accessibility readback showed focus returning to the source-inspection button.

The [complete pre-design CI run](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/35719487969) passed all four jobs at `b6f80f0`. Subsequent design changes have passed the local UI workflows, build and native inspection; their hosted checks remain independently visible on the pull request.

## Industrial design revision

The owner-selected industrial direction is implemented from native Figma frame `6:2`, with a graphite/amber palette, joined metric strips, square controls, compact ledger spacing and locally bundled JetBrains Mono. The revised UI production build, both browser workflows, all 34 Rust tests, strict Clippy and formatting checks pass locally. The eight section states and both review layouts retain zero automated axe violations. Ten declared text/background pairs meet 4.5:1; the smallest measured ratio is 5.30:1. The workflow tests retain their no-external-request and no-page-error assertions.

The Apple Silicon development bundle was rebuilt and launched. The revised native ledger and inspector display the original CSV amount and anchor; closing source inspection with Escape returns focus to the source button. Native inspection identified default WebKit select sizing, which is corrected with explicit control appearance while retaining the system option menu. These are development checks, not signed distribution or clean-install approval. Updated synthetic exports, image hashes and the exact remaining design scope are in the [current handoff](design/HANDOFF.md). Latest hosted verification remains visible on the pull request.

## Reusable statement mappings

Nine new Rust integration tests cover exact signed and separate debit/credit amounts, comma/dot decimals and strict grouping, explicit date formats, leading-zero accounts, multiline logical CSV anchors, profile reuse, overlapping imports, unchanged original bytes, whole-file rejection including errors beyond the displayed sample, stale/modified preview binding, and tab-separated BOM input with source-linked observations. The full suite is 43 tests. Strict Clippy, formatting and the production UI build pass. Java and Python adapters were unchanged.

Four browser workflows pass against real Rust commands. The new statement workflows add zero-violation axe checks for mapping and preview at 1440×1000 and 960×640, reachable import actions without horizontal page overflow, focus on the preview heading, preserved source inspection, saved-profile persistence, duplicate-original rejection, invalid-row blocking and a deliberately delayed real preview discarded after the analyst edits the mapping. No external requests or uncaught page errors occurred in the successful mapped-import flow. Screenshots and checksums are in [the statement design handoff](design/STATEMENT-IMPORT.md).

The Apple Silicon development bundle was rebuilt and launched. The existing synthetic workspace advanced from revision 7 to 8 through the guarded schema upgrade. A native semicolon statement with day-first dates, comma decimals, newest-first rows and fixed textual account/currency values imported three pending transactions at revision 9. The preview showed zero invalid rows and zero balance mismatches. Native source inspection returned `12,30` at logical CSV row 3, column `Paid out`, while the retained text correctly displayed the multiline description on separate physical lines. Escape restored focus to the source-inspection button. This is local development evidence, not clean-install or signed-distribution approval.

The v1→v2 tests verify that a backup contains both the original schema database and byte-identical referenced evidence. An injected SQLite event failure rolls back the version and revision while retaining a usable backup; restoring the v1 backup preserves evidence and then upgrades the restored workspace. Abrupt process termination and future migration paths remain unverified.

## Assessment authoring and explicit review

Six new Rust integration tests exercise question creation and revision, finding/question links, supporting and contradictory citations, explicit review decisions, pending-record rejection, stale writes, citation and field validation, altered-original rejection, correction propagation and preserved HTML snapshots. Version 2 migration tests retain the legacy database and originals, reopen legacy findings, preserve prior snapshots, test failed-migration rollback of finding status, and restore a usable workspace. The full core suite is 49 tests on this Unix development host; the existing Unix-only symlink test is omitted on Windows. Strict Clippy and formatting checks pass.

Six browser workflows pass against real Rust commands. The assessment flows author questions and findings, inspect both opposing source anchors, mark a finding reviewed, download a report, correct an observation, reject re-review of the pending correction, edit the finding and question, inspect decision history and verify that the prior report is unchanged. A concurrent import makes a stale question save fail without losing the draft or overwriting newer records. Four assessment states at 1440×1000 and 960×1000 have zero automated axe violations, with no horizontal modal overflow, external requests or uncaught page errors. The edit-flow check exposed ambiguous wrapped textarea labels when editing existing values; explicit accessible field names now make those controls stable. No test timeout increase was needed.

The editable Figma finding-review specimen, source export, application screenshots and remaining design scope are recorded in [the assessment handoff](design/ASSESSMENT-REVIEW.md). Java/Python engines and confinement policies were unchanged. Full workflow, packaging, sandbox, broad-web coverage and signed-release gates remain open. Hosted checks for this increment must be assessed on its pull request; earlier successful source builds are not evidence for a new commit.

The Apple Silicon development app was rebuilt and launched against its existing synthetic workspace. Opening advanced revision 9 to 10 through the guarded schema upgrade. Native finding review inspected `statement.csv`, row 10, column `amount`, returning the preserved `-180.00` value; Escape restored focus to the source button. Reviewing the pending transaction citation failed visibly while retaining the reason. Editing the finding then saved its revised limitations and reason at revision 11. Native development validation does not establish clean offline installation or a signed release.

The final rebuilt native app retained the revised finding after restart. Saving another recorded edit advanced to revision 12 and returned keyboard focus to **Review finding**; the fix defers focus restoration until the form's DOM changes have completed. Saving a native report snapshot then advanced the workspace to revision 13 while the snapshot correctly identifies source revision 12. The final six browser workflows also verify this editor-to-register focus path.

## Delivery tooling — EW-01

The offline runtime inventory verifier has 25 synthetic test methods. Local Apple Silicon: 24 passed, one Windows-junction check skipped as platform-specific. Tests exercise all three target policies, missing OCR/region assets, tampering, unsafe paths, links, malformed declarations, bounded errors and fail-closed behaviour under `python -O`. Native CI runs the same suite; source CI is distinct from installed-artifact acceptance.

The actual Java/Lucene staging inventory hashes 269 files / 168,205,077 bytes and fails completeness with 21 missing required components and the unlisted development marker. See the [sanitized negative report](delivery/evidence/runtime-inventory-2026-09-23.json) and [interpretation/limitations](../packaging/README.md). The tested directory is only the engine staging area, not the whole application. No release gate has changed.

The first Windows inventory run exposed zero identity/link fields from `DirEntry.stat`, which rejected ordinary files. The verifier now uses `os.stat(..., follow_symlinks=False)` to obtain real metadata without following name-surrogate reparse points. The existing positive inventories and hardlink/junction tests cover this regression on Windows. This follows the [Python filesystem API contract](https://docs.python.org/3/library/os.html#os.DirEntry.stat); the failed CI run remains available in the PR history.

The Windows rerun also exposed inconsistent path-versus-handle timestamp observations. Identity/size/link checks now bind the pathname to its opened handle; concurrent-change detection compares descriptor metadata before and after hashing. Regression tests cover differing path timestamps and a real file mutation during hashing. Exact manifest byte hashes remain mandatory.

## Acceptance evidence tooling — EW-02 — 2026-09-24

The ledger now maps twelve gates and ten scenarios to owner issues, required platforms and versioned procedures. Ten synthetic fixture files are hashed and tied to scenario assertions. Three published schemas describe the ledger, observations and review receipts. The checker verifies retained attachment/receipt bytes and exact source, artifact, policy and fixture bindings; it never executes attached data or procedure strings.

Local verification ran 51 Python tooling tests: 50 passed and the Windows-only runtime-junction test skipped. Twenty-six new release-evidence tests cover missing/stale/corrupt evidence, review binding and chronology, unsupported schemas, source-CI substitution, invalid OS versions, required minimum-version coverage, separate outcomes across OS versions, negative outcomes across policy changes and fail-closed behavior under Python optimization. All 26 also passed in a separate reviewer run. The actual `--check-policy` command reports a valid incomplete ledger, no candidate bytes verified, no accepted gates and twelve unpassed gates.

Cross-review identified and corrected vacuous platform policies, unbound review declarations, stale procedure/policy/fixture acceptance, hidden rejected failures and cross-OS failure suppression. Required OS support minima remain unknown and therefore block qualification. Review receipts prove consistency of retained declarations, not reviewer identity or the truth of an execution report. The [evidence guide](release-evidence/README.md) documents that trust boundary and the proposed 7/30-day freshness policy. Exact native CI results belong to this change's PR; earlier runs do not establish its portability.

The source run at `a819953355de25e06225e0fb23f3514ae91c81e4` passed [all four hosted jobs](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/35979007393): Windows Server source compilation, both Mac architectures and browser workflows. Separately, the discovery head `a0c414e3` passed [its four jobs](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/35978782990), and readiness head `12ff09f3` passed [its four jobs](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/35978875447). A local combined tree at `a3ebf29f80b9b0eb164ca230049592424966128e` ran 107 tooling tests: 104 passed and three Windows-only junction checks skipped. These are exact-revision source observations; later commits require their own checks. No installed-artifact release acceptance is inferred.

## Acquisition receipts — EW-05, 2026-09-24

The core suite now passes 67 tests on the current Mac: 25 unit and 42 integration tests. Nineteen new synthetic tests replace the old single acquisition test, covering complete empty/unsupported/error bodies, truncated streams without false hashes, redirect/link ancestry, charged failures versus refused reservations, exact budget accounting, duplicate bodies in both import orders, failed storage and final-transaction rollback, legacy/interrupted receipt absence, restored response-only originals, immutable export snapshots, malformed/future receipts, explicit elapsed-time overruns, and unsafe/dangling paths. A Windows-specific export-junction regression is included for hosted Windows execution; its result is not inferred from Mac tests. Strict Clippy, formatting and schema generation passed. Historical command v3 and workspace v3 schemas are byte-unchanged; command v4 and receipt v1 are additive.

The production UI build and all six real-Rust browser workflows passed. No visual controls changed. Collection receipt inspection/export currently use Rust dispatcher commands; designed review controls remain outstanding. The existing Vite large-bundle warning remains; this is not a performance benchmark.

A separate live smoke check on 24 September used the production broker with the explicit seed `https://example.com/`, zero expansion hops, two requests and 30 seconds. The robots response returned HTTP 404, and the page returned HTTP 200. Both complete responses contained identical 559-byte bodies: storage retained one SHA-256 original and both request/acquisition records, then accepted the page's static text derivative. The receipt recorded `live`, `successful`, two attempts and 1,180 elapsed milliseconds. Local export inspection verified its manifest hash and each exported original's hash and size. Raw bodies, receipts and export artifacts remain in ignored local evidence storage; no third-party page body is published here. This is not a benchmark campaign, terms approval, independent relevance label, in-app discovery-chain demonstration or broad-web coverage evidence.

The [receipt contract and limitations](discovery/ACQUISITION-RECEIPTS.md) distinguish received-original retention from successful content extraction, and application version from verified build identity. All twelve complete-release gates remain unpassed. Windows native-source verification and current PR checks must be assessed on the published head; this local result does not pre-claim those checks.


## Mac worker supervision — EW-04, 2026-09-24

The actual Rust Java/Lucene launcher now assigns private jobs, gives search workers read-only index access, clears inherited environment/descriptors, and supervises process groups, CPU/wall/file/descriptor limits and bounded outputs. Strict index acknowledgement validation precedes revision publication. Invalid derivatives trigger safe rebuilding, and explicit scratch cleanup rejects results when removal cannot be verified. A separate code peer review found and drove fixes to acknowledgement validation, damaged-index recovery and discarded cleanup errors; that review is not independent release/security sign-off.

At source `b8f31acec8823da78fb69908d60d09d7997f877e`, 55 ordinary core tests passed and two runtime-dependent tests were explicitly ignored in the ordinary suite. All seven native supervisor tests passed separately on macOS 26.6.2 arm64 with Java 21.0.12.1 and Lucene 10.5.1. The clean-source report binds source/runtime hashes and records `complete_release: false`; its SHA-256 is `22537fcb5db299620fba19a8a852537590d3412b4c9ebfe0833d6390ef0e04a4`. Raw diagnostics and exact runtime evidence remain local. Strict Clippy, formatting, Python compilation and staged public audits passed.

An unconstrained synthetic control proved the hostile actions work. The confined worker denied other-workspace/sibling reads, original/input writes, direct networking, child spawning, inherited environment and profile access; assigned work succeeded. Native Lucene checks cover Boolean, phrase, proximity, fuzzy and fielded queries, failed-query cleanup, failed-rebuild invalidation, and recovery from symlink/oversized/hardlinked index poison. Other probes exercise inherited descriptors, group timeout cleanup, oversized output and permission-restricted scratch while preserving outside targets.

The [development supervisor review](security/MACOS-WORKERS.md) documents access, unsafe-code invariants, reproducible probes and limitations. This uses deprecated `sandbox-exec`, not the signed-helper release design. The tree budget is monitored rather than a hard filesystem quota, and Java heap size is not an OS RSS limit. Intel runtime execution, signed helpers, other worker families, clean installation, crash cleanup and downloaded artifacts remain unverified. No release gate passed. Current hosted source checks belong to this PR's actual head.


The first Windows receipt run passed the new junction-rejection check but failed the positive export test with access denied. Export publication now closes the synced manifest handle before renaming its containing staging directory. The same positive snapshot test remains mandatory on Windows; a passing negative path test alone does not establish working export. The failed run is retained in PR history, and the corrected head requires a fresh hosted result.

## Integrated collection review, parser and durable jobs — 24 September 2026

Combined source `aacee9435b97d45e57bf4512a92de14bd8674a8f` passes 89 ordinary Rust tests on the current Mac (47 unit and 42 integration; five native-dependent cases excluded from this run), TypeScript/Vite build, and the 107-test delivery-tool suite (104 passed, three Windows-only skips). The collection increment passed 13 browser workflows against real Rust commands. Its two native editable Figma frames, compact/desktop axe results, screenshots and hashes are recorded in [the collection handoff](design/COLLECTION-REVIEW.md).

All twelve parser/search native tests passed separately at that clean combined revision on macOS 26.6.2 arm64 with staged Java 21. The actual confined PDF coordinator also completed, published an immutable derivative and restored its job, derivative and original from backup. The extraction remained unreviewed and did not replace the original's accepted text. Exact runtime/source hashes and raw synthetic diagnostics are retained locally; no case data or third-party source bodies are included in public fixtures.

Thirteen new durable-job tests cover idempotent queue keys, a two-worker limit, cancellation/success and shutdown/registration races, stale cancellation before a retry is claimed, bounded retries, database rollback after derivative insertion, replay without duplication, altered originals, exclusive coordinator ownership, interruption, cleanup-error precedence and backup/restore. The native desktop uses an explicit exit callback because the runtime may terminate the process without Rust destructors. Failed recovery does not claim that an orphaned worker was reaped. The [job contract](processing/JOBS.md) records remaining execution and presentation scope.

The cumulative changes are reviewed in [PR #1](https://github.com/Venin-Client-Systems/entity-workbench/pull/1); earlier source commits and checks remain in history. Its [combined hosted run](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/35994479583) must be assessed for the exact head. Main-branch review remains required. Source compilation, hosted browser checks and development confinement do not qualify installed artifacts or any of the twelve complete-release gates.


## Designed document review and app-local OCR — 24 September 2026

Combined clean source `304b127182563abf451e8a1f7d163644b938a70e` passes 98 ordinary Mac core tests (56 unit and 42 integration), strict Clippy, native-host compilation and the UI production build. Eight native-dependent cases are excluded from that ordinary run. The document UI increment independently passed all 23 real-core browser workflows at `daddd6bb76d563feb0e360ff060c86663a889642`, including ten new document flows; combined hosted results require the newly published head's own checks. The [design handoff](design/DOCUMENT-JOBS.md) retains three editable Figma frames, fourteen design/application images and five zero-violation axe observations.

At the clean combined revision, all thirteen parser/search boundary checks passed, all eight OCR contract/native checks passed and the native coordinator PDF publication/backup-restore case passed separately. OCR used a relocated 13 MB development Tesseract bundle with its exact native-library inventory, pinned English model and retained third-party notices. Hostile native probes denied other-worker reads, original/input mutation, direct networking, process creation and inherited state while assigned recognition succeeded. A blank image was reported separately from recognized text. Three portable tests verify that inventory rejection, compiler failure and timeouts replace stale success with retained failure observations; source CI now runs these explicitly. General image/PDF decoding, word regions, original-page mapping and recognition accuracy remain unverified.

The fresh Apple Silicon `.app` was built with the local Java/parser/Lucene runtime and English OCR assets. Through its native file chooser and document controls, the 650-byte synthetic PDF was imported, queued, shown running and published as one partial derivative by PDFBox 3.0.8. The review showed the expected synthetic text, matching original digest, distinct result digest and explicit no-source-anchors, no-OCR and embedded-document limitations. A normal quit and restart preserved the same job ID, attempt, derivative, text and hashes. The operator observation binds the executable, fixture and native-test reports; its retained SHA-256 is `101fdc1dee6f2c683c4ae22e5b8bee3eb99177914d89f2ec7782da159451cb22`. This confirms one native development workflow, not a clean-machine installation, application-wide network-traffic measurement or signed/notarized release.

Peer review found and corrected cancellation acknowledgement wording when the canonical outcome is failed/unverified, and request-key loss when leaving Evidence after an uncertain acknowledgement. Regression tests use actual canonical jobs, including a terminal transition before remount. No failed or unverified worker is labelled safely cancelled, and the open application reuses the original uncertain request key. Recovery across a full application restart still requires checking the durable ledger. Unknown worker termination now suspends new processing instead of silently continuing; verified OS-process recovery remains outstanding.

The [main review](https://github.com/Venin-Client-Systems/entity-workbench/pull/1) remains subject to its required human review. All twelve complete-release gates remain unpassed. The separate Windows development branch retains its actual failed startup observations and does not enable core worker execution until compatibility and confinement have been demonstrated.


The first combined Windows source run at `827e8606ef90f0583255ff45ba735d9345467381` exposed a verifier defect: timestamp-only OCR history names collided under the host clock, so a failed observation overwrote its predecessor. History files now use UUID suffixes with exclusive creation. A deterministic fixed-clock regression requires both success and failure records to survive and the latest projection to show failure. The portable failure-evidence suite now has four passing local tests; Windows rerun remains required. The failure was in evidence retention and is not evidence of Windows OCR runtime execution.

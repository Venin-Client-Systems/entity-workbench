# Readiness audit — 24 September 2026

This is a sanitized inspection record for EW-08, based on source revision `67e84c638444f3e1d1a6397ff28d568eea4759e9`. It records observed configuration and local development capability. It contains no access credentials or personal machine identifiers. Account availability, authorization and resource reservations were not inspected or inferred.

## Source build matrix

At this revision, `.github/workflows/verify.yml` specifies `windows-2022`, `macos-15` and `macos-15-intel` runners. Its native command uses `tauri build --no-bundle`; the development CI config disables bundling and resources. This demonstrates a configured source-build matrix. It does not establish Windows 11 clean installation, a clean Mac environment, artifact signing or a reserved release test host. This audit did not query new hosted runs or runner access.

## Distribution configuration

`desktop/tauri.conf.json` disables bundling and names only an app target. It does not declare an explicit minimum macOS version or a release signing configuration. `docs/VERIFICATION.md` distinguishes development app checks from installed signed distribution acceptance. `docs/release-gates.json` at the inspected revision leaves every release gate false. Missing repository configuration does not establish that signing accounts or certificates are unavailable.

## Local development host

A read-only local check returned `arm64`, macOS `26.6.2` and `17179869184` bytes (16 GiB) of physical memory. `codesign` and `xcrun` executables are present. Only architecture, OS version, memory and executable availability were read. No certificate identities, private keys, keychains, account details, serial numbers, network identifiers or tokens were inspected.

This host is a candidate for native development probes and a 16 GB-class benchmark. The observation does not confirm that it is reserved or suitable for the specified workload, that it can be reset into a clean environment, or that it represents the minimum supported OS. No benchmark was run. No Windows or Intel Mac release test host was inspected.

## Responsibility and decisions

The programme requires Windows 11 x64 and both Mac architectures, signing rehearsal by the second sprint and final downloaded-artifact tests in the sixth sprint. The plan leaves minimum macOS versions explicit. The inspected repository does not provide an accepted public assignment of signing custodian, clean-test operator, release maintainer or security reviewer. Their availability remains unknown. No owner decision, access grant or reviewer commitment was made during this audit.

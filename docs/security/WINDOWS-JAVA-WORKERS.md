# Windows Java engine recipes — development candidate

This adds **engine-only development recipes**, not Windows application support. The core parser, Lucene adapter, OCR and desktop routes remain unchanged and continue to block unverified Windows workers. The preceding [AppContainer foundation](WINDOWS-WORKERS.md) has a retained development-probe pass; that evidence does not establish these new recipes. The sixteenth campaign passed the complete development parser/Lucene/boundary suite on the exact retained checkout recorded below. This establishes that bounded engine recipe on the hosted development runner, not application integration, Windows 11 installation or release acceptance.

The separate Rust crate exposes three closed constructors: parse one byte buffer; index a bounded document manifest at a specified revision; and search a previously accepted in-memory index snapshot. Callers cannot supply relative input names, Java class names, JVM options or system properties through these constructors. The existing low-level launcher remains a trusted coordinator interface, not a user extension mechanism.

## Assigned data and runtime separation

| Process | Read-only inputs and executable assets | Writable assignment | Accepted output |
| --- | --- | --- | --- |
| Parser | Copied Java; parser-only worker classes and non-Lucene dependencies; one immutable document; immutable request metadata | Disposable scratch and its existing private AppContainer profile | At most 2 MiB JSON, bound to generated job ID, input size and SHA-256 |
| Index builder | Copied Java; search-only worker classes and Lucene/Jackson dependencies; one immutable manifest; immutable request metadata | A fresh index under disposable scratch, plus the existing private profile | Strict matching document-count/revision acknowledgement and bounded validated index files |
| Search | Copied Java; search-only worker classes and Lucene/Jackson dependencies; immutable query/request; a copied read-only index snapshot | Disposable scratch and its existing private profile | At most 1 MiB JSON, matching snapshot revision and known unique source IDs/names |

Every operation launches separately. The parser receives no search runtime, index grant or canonical workspace. Search receives no parser runtime or private original-document grant. The index build directory is disposable; no worker receives an existing writable cache or a canonical database path. Rust owns the in-memory handoff and verifies it again before staging a separate read-only search copy.

The production ACL masks, zero capability list, suspended token verification, one-process Job Object, strict handles, detached launch, no inherited handles, sampled resource limits and explicit stop/cleanup policy are unchanged. Native input metadata uses the same read-only package grant as the existing assigned input. A search snapshot receives that read-only grant for its directory and files. The builder's new index directory inherits the existing scratch grant. No additional network permission, process allowance or unconfined retry is added.

`FileWorker` is a small Java file-IPC bridge: it opens and bounds its assigned request, requires a fixed parse/index/search operation match, then supplies those bytes to the existing worker's stdin reader within the same process. There are no inherited pipes or helper processes. `Protocol.input` supports one coordinator-supplied absolute assigned-input property only for the exact request name `input.json`. It still runs the existing relative-name checks first. With that property absent, the macOS behavior is unchanged. Output continues to use the existing relative `result.json` contract in scratch.

## Runtime inventory and bounded handoff

[`stage_windows_engines.py`](../../scripts/stage_windows_engines.py) reads an already installed local Java 21 and the existing Maven build output. It performs no download or engine execution. It copies Java separately for parser and search, extracts only reviewed worker classes, excludes all Lucene-named dependencies from the parser and includes only Lucene/Jackson dependencies in search. Existing direct dependency pins remain Tika 3.3.2, PDFBox 3.0.8, Lucene 10.5.1 and Jackson databind 2.22.3. No dependency version changes are introduced. Rust additionally uses the workspace lockfile's existing `sha2` 0.10.9 and the existing UUID dependency's serde feature.

Each role gets a complete manifest of regular files, sizes, SHA-256 hashes and directories. A root development marker is created only after both roles stage successfully. Selected worker classes are checked against their declared ZIP size and read through a 1 MiB + 1 cap; duplicate archive entries and oversized classes reject staging. Partial staging is not a ready bundle and must be discarded before retrying with a fresh destination.

Before execution, Rust requires the exact role, required pinned library names, permitted role-specific asset locations, complete inventory equality and all file hashes. Missing and unlisted assets, case-colliding or unsafe names, links, Windows reparse points, hardlinked files and nonordinary ancestors are rejected. The copied runtime is verified again before execution. The existing native walk also rejects named streams. These manifests bind actual development bytes; they do not replace a signed release manifest or independent dependency review.

The bounded transport uses:

- At most 16 MiB assigned document or encoded index manifest, 10,000 index documents, 128-byte IDs and 1,024-byte names. Aggregate source-string lengths are checked before identity copies. A borrowed manifest is serialized directly through a 16 MiB capped writer, so JSON escaping cannot cause an unbounded intermediate value or output buffer.
- Fixed JVM heap of 256 MiB, two reported processors, serial GC, assigned scratch for temporary/home paths, a 768 MiB process commitment limit and a 30-second worker wall time. These are development limits, not throughput claims.
- At most 128 flat index files, each at most 8 MiB and together at most 24 MiB. Traversal, device names, streams, case collisions and missing commit files are rejected. The native collector retains the directory pin, rejects links/hardlinks and reads each file under a bound after process exit. Rust computes file hashes, checks the matching acknowledgement and validates the snapshot before returning it.
- A generated job UUID and input hash/size for parser results; exact revision/document count for index results; matching revision, finite scores, at most 100 hits and known unique source identities for search results. Unknown fields, duplicate struct fields and trailing JSON are rejected. These are transport checks; canonical domain validation and source-anchor acceptance still belong to the core coordinator when integration is implemented.
- Immutable input/request bytes and every assigned search snapshot file are checked again after worker exit. Results are rejected if assigned inputs changed. Existing cleanup failures continue to reject the result and retain preceding errors; cancellation is checked again after successful cleanup before returning an accepted Java output.

No snapshot is published to a workspace, no revision marker is written and no database migration occurs in this slice. A stale or malformed worker acknowledgement cannot create an accepted `IndexSnapshot`. Its fields are private; search accepts only a checked snapshot object, not a caller-selected filesystem directory.

## Native development verification

The extended [`windows-confinement.yml`](../../.github/workflows/windows-confinement.yml) keeps the existing foundation tests and complete hostile/copied-Java probe mandatory. It then builds the existing pinned Maven worker project and its Java file-path tests, stages separate local runtimes, and runs `ew-windows-java-probe` on Windows Server 2022. The Maven build may obtain pinned build dependencies; neither staging nor the launched application worker downloads assets. No OCR runtime, model or asset download is introduced.

The engine probe compiles synthetic fixtures from the repository into its binary. Its prepared cases are:

1. Actual UTF-8, searchable PDF and DOCX extraction with expected text and status; a text-free vector PDF remains explicitly partial; an unsafe ZIP yields the expected archive-limit failure; unsupported bytes remain unsupported.
2. A distinct Lucene process builds an index from the actual accepted extraction texts. Separate read-only search processes exercise Boolean, phrase, proximity, fuzzy, fielded and successful-no-results queries, checking exact revision and expected source identities/counts.
3. Each runtime executes the reviewed synthetic Java boundary worker. Matching unconfined controls run before and after its confined invocation. Twenty-three predicates cover assigned input/request reads and writes, runtime write access, the other actual runtime, a private workspace, the actual synthetic original, a real captured sibling Lucene snapshot, scratch I/O, direct loopback TCP, child-process creation, the real caller environment sentinel, classpath separation read-only assigned-index access, memory-directory class isolation and exact/absent directory policy. Parser/Tika classes must be absent from search, and SearchWorker/Lucene classes absent from parser.
4. The parent verifies each complete predicate map and empty job directories. A failed JVM launch, process exit, missing result or cleanup failure cannot substitute for a boundary result. Successful file-handle, socket or child creation is not converted into denial by a later I/O/cleanup error. Such later errors fail the probe. The assigned-index read/write predicates apply only to search; the parser has no such grant.

The workflow initializes a failed/incomplete Java receipt before building anything. Only the completed native executable can replace it with success. It retains both role inventories, source/OS/runtime identities, the compiled probe hash, the original foundation report and the separate Java report on failure or success. Reports contain fixed test labels, booleans, counts, versions and hashes, not private paths, account identifiers, arbitrary exception text or actual investigation data. All original foundation failures and its first pass remain recorded in the linked foundation document.

## Current evidence and remaining work

The first native run, [36019529542](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36019529542), tested PR head `e31e9965662dc83d4cb61deed186339dce543559` through actual checkout `1891cf1b52976806b0d815d8644e8944e580a5b6` (parents `f86989263b5fa33937433906df7d21bb73d54e38` and that PR head). Its **37 native Rust tests**, original foundation confinement/copied-Java probe, Java 21 build, **two JUnit tests**, **five Python staging tests** and distinct runtime staging passed. The new Java probe failed at `parse_notice.txt` with `tree disk budget exceeded`. No successful parser result was accepted; subsequent document, Lucene and JVM boundary checks were not reached. The parser/search inventories were approximately 404/354 MB, below the separate immutable-runtime bound. A JVM crash dump is a diagnostic hypothesis, not an established cause.

The complete failure artifact `10816311747` and exact metadata/logs are retained privately. Its ZIP SHA-256 is `87775458ca39f4ec7e44a00ec0187d64623567ff1e22203b59ed0d51fa8e893e`; Java failure-report SHA-256 is `c2f428b7a072ee673d99348c18dbbea1513e57945e7ecc1108f37c7c53a1f9b4`; compiled Java-probe SHA-256 is `e1a2e8da03fc67fe4e6f06351aa375b979f69fb90b877e9639d445593ccd277c`. This negative observation remains part of the history even if a later run succeeds.

The follow-up assigns the JVM's fatal error file to a fixed scratch name, using the documented [`-XX:ErrorFile` option](https://docs.oracle.com/en/java/javase/21/troubleshoot/location-fatal-error-log.html). Only the synthetic native harness requests failure diagnostics. After acknowledged process-tree termination, bounded metadata inspection records tree counts/bytes and counts of dump-like extensions. A rejected or unreadable tree is `null`, not an empty-tree claim. It reads no dump payload. The fixed fatal file must pass the ordinary no-link/hardlink output reader and a 256 KiB cap; only its first 32 lines can yield a numeric exception code and an allowlisted module name. A fallback accepts exactly one top-level `hs_err_pid<1–10 ASCII digits>.log` under the same bounded walk and file checks; ambiguous, linked, oversized or arbitrary names cannot supply a hint. No raw error text, paths, arbitrary filenames, addresses, process IDs, environment or dump bytes enter the report. These worker-controlled markers are diagnostic hints, never proof of successful confinement. Missing/unrecognized hints do not establish that no JVM failure occurred.

The second native run, [36021565678](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36021565678), tested diagnostic head `3d6b87b50c78db559b44acb7133804154b5295ae` through checkout `aa6fa3d283a457ab65f9d861b2abee68b90a07f6`. Its **41 native Rust tests**, foundation probe, Java build/JUnit and staging passed. The first parse again exceeded the disk budget. Post-termination inspection found three scratch entries totalling **85,174,255 bytes**, a largest file of **85,162,775 bytes** and one dump-like filename. Profile storage had five entries and zero file bytes; the fixed error file was absent and no fatal header was available. This establishes a large dump-like scratch artifact; it does not yet establish the underlying exception. Retained artifact `10817710076` has ZIP SHA-256 `db70d6a9f7e9475552d61a5515445bbb6e3660f07b4c83a90977e147018e27bc`, failure-report SHA-256 `b1c99c839150fbc7a36f10147fe636680e5e616131e82557d355a781d7a1fd26` and compiled Java-probe SHA-256 `33eafe0ce3b6f818b8628f3d5c926add2f22d3d1d2d767a95273efb0fb4e9a4b`. Both failures remain retained.

Source review then found that raw recipe-string suffixes used forward slashes with canonical verbatim Windows paths. [Microsoft documents](https://learn.microsoft.com/en-us/windows/win32/fileio/maximum-file-path-limitation) that those paths do not normalize forward slashes. The candidate now uses backslashes for ErrorFile, classpath and index suffixes, with an actual Win32 file-query regression comparing the former and corrected forms. This is a concrete path-construction repair; its role in the observed JVM failure still requires native evidence. The fixed recipe also sets `-XX:-CreateCoredumpOnCrash`: [OpenJDK's Windows implementation](https://github.com/openjdk/jdk21u/blob/master/src/hotspot/os/windows/os_windows.cpp) otherwise creates a minidump on fatal errors, while its [flag definition](https://github.com/openjdk/jdk21u/blob/master/src/hotspot/share/runtime/globals.hpp) defaults to enabled. Disabling these dumps prevents that diagnostic side effect from consuming the worker's unchanged scratch budget; it does not suppress the worker's failure or permit a result.

The third native run, [36022762617](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36022762617), tested head `4fdd2fc6b87120b1e00dbadcd5afb00bf1b35167` through checkout `3d103df8024e7e4cb9d5d0936d0500e31a134fa6`. All **44 native Rust tests**, including the direct Win32 old/new-path comparison, and prerequisite foundation/build/staging steps passed. The first parse exited with code 1. Post-termination scratch contained only a **23,052-byte fixed fatal log**, with zero dump-like files; its header was recognized but the available exception/module classifiers yielded no match. The path repair and suppression of crash dumps worked, but the underlying JVM fatal error remains unresolved. Artifact `10817587487` has ZIP SHA-256 `834c63563f04673aeed9e9ab9f82488015681eb7f1e4b5d8f256f688d5d66c39`, failure-report SHA-256 `7a625286ea4c39ed42c14d252e15b8db4e6dadbf573bd87563acb50e50b8c7c8`, and probe SHA-256 `01c5cd4af202f716e0320fce64960c2c0143da51a5388e0ffc56e656cef91076`.

The fourth candidate restored `-XX:-UsePerfData` and `-XX:+DisableAttachMechanism`, which the passing foundation Java recipe already used. Their [OpenJDK definitions](https://github.com/openjdk/jdk21u/blob/master/src/hotspot/share/runtime/globals.hpp) disable unneeded performance instrumentation and tool-attachment facilities. Omitting them was a mismatch with the established recipe. Fatal diagnostics additionally classify internal-error/out-of-memory markers and a short fixed allowlist of JVM source components plus numeric source lines. No arbitrary reason text, source path or module name is exported.

The fourth run, [36023635831](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36023635831), tested head `97014247ff2a1373f5d642c8d34b4c61c668bbc3` through checkout `8f40a77dae0376f90ca7022c835fbb66c7f37368`. Its **45 native Rust tests** and prerequisites passed; the first parse still exited 1. The 23,208-byte fixed fatal log was classified as an internal error, without a recognized source component or out-of-memory marker; dump count remained zero. Thus restoring those restrictions did not resolve the fatal error. Retained artifact `10818157035` has ZIP SHA-256 `dcaa46d4c8cc7bd2751fd89b07ec0129cf0e691f5f77ecc96073bf040cc8deb0`, failure-report SHA-256 `9d0488d48374d420061da6ef0d058fe14bfac139605a1054688812e9fd89e8e2`, and probe SHA-256 `403212b17720261a4c3551f55c8c0b37e9168a4668e5210ffdb6a086cd98e26e`.

The fifth candidate made the Windows-only `FileWorker` replace Java stdout/stderr with null streams before invoking the existing workers. Their console notices are unused by file IPC, and detached launch supplies no inherited console handles. This does not change the macOS stdin/stdout entrypoints or inherit new handles. The probe additionally requests six fixed entry/read/decode/dispatch/return checkpoints, read only after termination through the same single-link/no-follow output reader with a 64-byte limit. These hints do not prove confinement or change result acceptance. Numeric eight-hex internal-error codes are now recognized without exporting the remainder of a fatal line. The following native observation records their actual result.

The fifth run, [36024707780](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36024707780), tested head `32d22e1f58270bad52d3f412e1869ad5c53514fa` through checkout `d01ee0feea45669fd9098e9435210452c6a4ee1e`. All **46 native Rust tests** and prerequisite foundation/build/staging steps passed. The first parse still exited 1 with a 23,254-byte fixed fatal log, an internal-error marker, no recognized numeric code/source and no observed Java checkpoint. That does not prove a pre-main failure: the stream replacement precedes the first checkpoint. There were no dump-like files. Retained artifact `10819307570` has ZIP SHA-256 `69ef054c644ddf1e1710d16ba07d9ccab9c45b09e0bece65e97600713f0306ca`, failure-report SHA-256 `5fa30ab7b2ba6ee959b673b8d83276e50ceb30d1631bc58196b02213f44099a2`, and probe SHA-256 `403246328be25f63c3d349d5b8c59429d7f9da00bd2c89f661213f2ce83795a9`. All five failures remain retained.

The sixth candidate adds a benign `FileWorker` control **before** the first confined parse. It runs only the compiled synthetic text fixture with the same fixed Java recipe, OS environment allowlist, detached/no-inherited-handle launch, child policy, one-process Job Object, 768 MiB commitment, 30-second wall/CPU time and sampled scratch/handle limits. It deliberately has no AppContainer token or ACL grant and uses the verified staged runtime directly rather than a confined runtime copy; therefore it is a diagnostic positive control, never a production fallback or confinement result. A successfully reaped normal control failure is retained alongside the first confined outcome. Unacknowledged termination or cleanup failure prevents further launches. Bounded result reads and the usual job/content/schema checks must pass before a control success is recorded. Its result never substitutes for a confined result.

Fatal diagnostics now also permit SHA-256 of a validated source **basename**, plus its numeric line, from a bounded `Internal Error` location. The grammar accepts only a short ASCII source basename with a C/C++ header/source suffix, rejects paths, separators, traversal, invalid line numbers and oversized input, and never emits the raw unknown basename or JVM log. This follows [HotSpot's fatal-header format](https://github.com/openjdk/jdk21u/blob/master/src/hotspot/share/utilities/vmError.cpp); it permits comparison against trusted source files without exporting arbitrary worker text. This iteration makes no additional JVM flag changes.

The sixth run, [36026679568](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36026679568), tested head `b597acabd96d14bbe012b96c7d41e5e9b26d6fa6` through checkout `9c178e7ef9a8c40809f7bf6ba0b86808f11f7640` (parents `e9f5f9881bd49055cb07810fb5aed4c633d2e83b` and that head). Its **47 native Rust tests**, foundation probe, Java/JUnit and staging passed. The benign FileWorker control returned valid expected text and the `worker_returned` checkpoint; the first confined parse still exited 1 with no checkpoint and a 23,254-byte internal-error log. The source fingerprint was `eaec18485f0879b527b0cb0fbcb9c2756077efdf4b26836b3b3688b7a8315cfc`, line **372**. Artifact `10819831355` is retained with ZIP SHA-256 `069b9e27a19543889643cbca2155d7a1ed2c182cc2d6102b2295505d3db8d4e8`, failure-report SHA-256 `be77f4b33b11af525e2c9ecbb8a1d874a2c317554e76a7c18bceb5beddcaba25`, and probe SHA-256 `fa173ef3392e2ee1edfff20663950b500f6cca4f730208f30f57300b31582918`.

That fingerprint matches the public source basename `classLoader.cpp`. In the [staged Microsoft Java 21.0.12.1 source, line 372](https://github.com/microsoft/openjdk-jdk21u/blob/release/jdk-21.0.12.1_1/src/hotspot/share/classfile/classLoader.cpp#L372) guards a null canonical bootstrap-image filename. Its caller returns null when native `JDK_Canonicalize` fails. The corresponding [Windows canonicalizer](https://github.com/microsoft/openjdk-jdk21u/blob/release/jdk-21.0.12.1_1/src/java.base/windows/native/libjava/canonicalize_md.c) rejects every question mark as a wildcard before normalization, including the question mark in a verbatim `\\?\` prefix. The passing control used the normal DOS runtime spelling; the copied confined runtime used a canonical verbatim spelling. This source chain identifies a concrete path incompatibility; the repaired confined invocation still requires native verification.

The seventh candidate converts **only Java launch text** to a normal local-drive spelling: executable, runtime/classpath roots, assigned input/request/index, working directory and scratch-derived JVM/environment paths. Canonical paths remain in use for filesystem validation, ACLs, hashes, monitoring, result reads and cleanup. Before conversion it rejects UNC/device/unsupported namespaces, mapped/non-fixed drives, empty/dot/trailing-dot/trailing-space components, reserved device names, streams, wildcards and DOS forms of 260 or more UTF-16 units. It rejects reparses on the path and its parents and requires the DOS spelling to canonicalize to the exact same canonical path. There is no permissive normalization fallback. This deliberately limits development Java launch locations; unsupported paths fail explicitly. Unit cases cover namespace/alias/length rejection, and a native regression checks ordinary file/directory round trips. The same helper applies to the synthetic positive control. No JVM option, capability, ACL or resource budget changes in this repair.

The seventh run, [36028289925](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36028289925), tested head `a446f97638cea17a0bd5d561e7d64c191f6900d6` through checkout `d9ecc42916af01803307c495ce99fc2b282dbb53`. All **49 native Rust tests** and prerequisite steps passed. The first confined text parse now returned an accepted output (SHA-256 `183956dcd1d54e0fb58a9e7eccbe48d1b89ed7b012fb1f9cbbfecd57c212cf2b`), but the overall probe correctly remained failed because the benign control was rejected before launch. Its staging CLI path contained a forward slash preserved by the original `absolute()` join, which the strict DOS grammar rejects. Artifact `10820837303` has ZIP SHA-256 `26882a3f4492b976da81deec20893e906654ec1d9699eaf333d722664525e06a`, failure-report SHA-256 `799453f908753cf65e8a3595b704032d9400e84b456547b39860138b6e9104c1`, and probe SHA-256 `564bb906b7857a6f848dd5f38e60096c1910d9144a55d0035f86b2b83aab9cd7`. There is still no full native parser/Lucene pass.

The eighth candidate repairs the control setup: after the existing complete inventory and ancestor validation, it canonicalizes the staged runtime and only then applies the strict launch-spelling helper. The production copied runtime was already canonical. A native regression covers mixed separators and a dot component: direct conversion remains rejected, while separate validated setup canonicalization yields the same ordinary file identity. The grammar, AppContainer launch, permissions and resource policy are unchanged.

The eighth run, [36029076395](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36029076395), tested head `d09585e7f2ccda06d242f729f296b9f0719210fb` through checkout `ef29ab35859bce5c993205395adcc6c18a542cb7`. The new native regression failed on its final text-equality assertion: the runner's temporary directory used a legitimate 8.3 alias while canonicalization returned its long spelling. Earlier canonical-identity and rejection assertions passed. Engine tests were not started. Artifact `10820488468` is retained with ZIP SHA-256 `22d3480b12769e98b1fae1b278dd78019f3a8c6d259f6751966bb26b1e46e20b` and incomplete Java-receipt SHA-256 `b90b3e66ed11c4fff320a6ebaa7d8ace636647503292570286f65c2ce9eb0eb8`. The ninth candidate corrects only that test expectation to compare canonical identities, preserving malformed-spelling rejection. No launcher, grammar, control setup or permission changes accompany the assertion repair.

The ninth run, [36029513825](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36029513825), tested head `ec8dbcb5087fbc4dea08abe2cbbe8b7d599582af` through checkout `bc25ec541e58a73cf466a2b6b8480790ce33fa7c`. All **50 native Rust tests**, foundation, Java/JUnit and staging passed. Both the benign text control and confined text parse passed. The first PDF reached `parser_selected`, then hit the unchanged **30-second wall-time limit**; the supervisor acknowledged termination and cleaned up. Scratch contained only its 17-byte checkpoint, without a fatal log or dump. Later document/Lucene/boundary phases were not reached. Artifact `10820504936` is retained with ZIP SHA-256 `42a11953a1ba2124e1505deceae6ce76070dbc3f30b5a58ef4f5349ce7906977`, failure-report SHA-256 `2075238c50572b5e19a3acada4ec39fd4350e105f06972effc6c203abc073eb7`, and probe SHA-256 `dd4b641e69955acc4f1266a1eddb2ac2b07f1ad77b41936eb75a92f454b9aec4`. This PDF timeout is distinct from the resolved bootstrap path failure.

The PDF adapter directly calls PDFBox `Loader.loadPDF`, constructs `PDFTextStripper` and extracts text. It does not invoke Tika PDF autodetection, OCR, embedded parsers or rendering. PDFBox's [default mapper](https://github.com/apache/pdfbox/blob/3.0.8/pdfbox/src/main/java/org/apache/pdfbox/pdmodel/font/FontMapperImpl.java) can lazily initialize a [filesystem font provider](https://github.com/apache/pdfbox/blob/3.0.8/pdfbox/src/main/java/org/apache/pdfbox/pdmodel/font/FileSystemFontProvider.java), including font discovery and cache construction. That is a hypothesis to investigate, not an established cause of this timeout.

The tenth candidate adds six fixed, opt-in PDF checkpoints around load, stripper construction and text extraction, using the existing bounded checkpoint file. `Protocol.checkpoint` accepts only the closed set of bridge/PDF codes and performs no work when the probe property is absent, preserving ordinary macOS behavior. A compiled synthetic PDF control runs before the confined PDF with the same recipe and unchanged bounds. Both outcomes remain visible after normally reaped control failures; cleanup uncertainty still stops further launches. Extraction behavior, font-provider choices, JVM options, time/memory/disk bounds and permissions are unchanged.

The tenth run, [36031161059](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36031161059), tested head `2b7203f9a8de34b284a117fbdd065d6d5b26d133` through checkout `240d38c08232577e98ca22918bc089d8861201f3`. All **51 native Rust tests**, foundation, Java/JUnit and staging passed. The text control and expected confined text extraction passed. The benign PDF control reached `pdf_text_started`, after load and stripper construction, and then hit the unchanged 30-second wall-time limit. Its scratch held only the 18-byte checkpoint; there was no fatal log or dump. The subsequent confined PDF returned schema/hash-bound output with SHA-256 `070f8373eead38affdaee479d2b400abd0a09c297fc88b83fdc762dacb995b99`. The overall probe correctly failed on the control timeout **before the PDF fixture-content/status assertions**: that transport result is not a verified PDF case. Later document/Lucene phases were not reached. This differential does not establish an AppContainer-specific font-access problem. Artifact `10823005979` is retained with ZIP SHA-256 `6b9198ba199462bbc000a7d1e91e900b0598c62a031ca10aa8a3e7e588552c3a`, failure-report SHA-256 `3055b2e0012422ce810997fc1262d4d3ecd5fd431b27524090d1522759b60385`, and probe SHA-256 `f3b017b4e0de5fb4bf1d5eb7d12a28f183536ea4f3cf0c0187b16d3f2ccfbb27`. All ten negative attempts remain retained.

The eleventh candidate adds one opt-in observation of the FileWorker main platform thread after 20 seconds. A daemon calls Java 21's [bounded `getThreadInfo(id, maxDepth)` API](https://docs.oracle.com/en/java/javase/21/docs/api/java.management/java/lang/management/ThreadMXBean.html#getThreadInfo(long,int)) with a maximum of 64 frames. It never enumerates threads, enables CPU/contention monitoring, requests locked monitors/synchronizers, starts a remote management service or changes the existing 30-second supervisor deadline. The [matching Microsoft JDK implementation](https://github.com/microsoft/openjdk-jdk21u/blob/release/jdk-21.0.12.1_1/src/java.management/share/classes/sun/management/ThreadImpl.java) was inspected. The sample only records a closed thread state, frame count and six booleans. `java-sample.json` is at most 512 bytes and is read only after confirmed termination through the existing no-follow/single-link reader. The original error and cleanup precedence remain authoritative. A missing, malformed or unrecognized sample is unknown; a false category only means no exact allowlisted class appeared in this one bounded sample. It does not prove that activity never occurred or distinguish cold initialization from a hang by itself.

The exact class-name allowlist below is based on PDFBox/FontBox **3.0.8** sources and Microsoft OpenJDK **21.0.12.1** sources. Prefix matches are deliberately excluded. No method, file, line, classloader, module, arbitrary class, thread name/ID, raw frame or hash is emitted by this sample.

| Boolean | Exact class names |
| --- | --- |
| `font_provider` | `org.apache.pdfbox.pdmodel.font.FileSystemFontProvider` |
| `font_directory_walk` | `org.apache.fontbox.util.autodetect.FontFileFinder`; `org.apache.fontbox.util.autodetect.WindowsFontDirFinder` |
| `font_decode` | `org.apache.fontbox.ttf.FontHeaders`; `org.apache.fontbox.ttf.TTFParser`; `org.apache.fontbox.ttf.OTFParser`; `org.apache.fontbox.ttf.OpenTypeFont`; `org.apache.fontbox.ttf.TrueTypeFont`; `org.apache.fontbox.ttf.TrueTypeCollection`; `org.apache.fontbox.type1.Type1Font` |
| `pdf_text` | `org.apache.pdfbox.text.PDFTextStripper`; `org.apache.pdfbox.contentstream.PDFStreamEngine`; `org.apache.pdfbox.text.LegacyPDFStreamEngine` |
| `class_loading` | `java.lang.ClassLoader`; `jdk.internal.loader.BuiltinClassLoader` |
| `file_io` | `java.io.FileInputStream`; `java.io.RandomAccessFile`; `sun.nio.ch.FileDispatcherImpl`; `sun.nio.ch.FileChannelImpl` |

The provider's [font-loading call sites](https://github.com/apache/pdfbox/blob/3.0.8/pdfbox/src/main/java/org/apache/pdfbox/pdmodel/font/FileSystemFontProvider.java), [directory walker](https://github.com/apache/pdfbox/blob/3.0.8/fontbox/src/main/java/org/apache/fontbox/util/autodetect/FontFileFinder.java) and [Windows font-directory selection](https://github.com/apache/pdfbox/blob/3.0.8/fontbox/src/main/java/org/apache/fontbox/util/autodetect/WindowsFontDirFinder.java) motivate these fixed diagnostic categories; they do not establish the actual timeout cause. Extraction, fonts, JVM options and permissions are unchanged. Only the existing development-probe property enables sampling. Ordinary API/macOS entrypoints do not request it.

The eleventh run, [36033358101](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36033358101), tested head `d4698306ca2750f8e662630f1ae2c26ee3f611a3` through checkout `e608c9068e2666d18c06d05ad699057fe371a7c9`. All **53 native Rust tests**, original AppContainer controls, **five Java tests**, staging and expected text extraction passed. The PDF control again timed out at `pdf_text_started`. The 20-second sample reported `RUNNABLE`, 33 frames, and `font_provider`, `file_io`, `pdf_text` true. Other categories were false. It identifies observed provider/I/O activity, not an exact operation, persistent hang or AppContainer-specific denial. Confined PDF transport returned output but fixture assertions were again unreached because the control failed. Artifact `10823618363` is retained with ZIP SHA-256 `cf34cee96e801d2f1bfcd0bc94e1bbbf777ef6d8334d0512ab9b533f5fbfcc7a`, report SHA-256 `3085943a78435fb9300776777bbfae539e423f85aaeef8427ebe5b4cb99b4806`, and probe SHA-256 `769730f07bd975299730a089f16acefe4f515665bd6fdd4f49277ae044d6c447`. All eleven negative attempts remain retained.

## Fixed extraction font policy candidate

The next candidate removes host font discovery from the **Windows PDF extraction recipe** through PDFBox's public [FontMapper](https://github.com/apache/pdfbox/blob/3.0.8/pdfbox/src/main/java/org/apache/pdfbox/pdmodel/font/FontMapper.java) and [FontMappers.set](https://github.com/apache/pdfbox/blob/3.0.8/pdfbox/src/main/java/org/apache/pdfbox/pdmodel/font/FontMappers.java) interfaces. Its fixed recipe selects `liberation-sans-2.1.5-extraction-v1`; parser results identify `pdfbox-3.0.8-local-fonts-v1`. The control and confined worker use exactly that recipe. The ordinary macOS recipe does not set the property and retains its default path. Its parser JAR includes the optional adapter class solely to satisfy JVM linkage; a regression checks that property absence preserves the existing mapper and parser identity.

The adapter reads only `/org/apache/pdfbox/resources/ttf/LiberationSans-Regular.ttf` from the already inventoried PDFBox 3.0.8 JAR. It requires exactly **410,712 bytes** and SHA-256 `76d04c18ea243f426b7de1f3ad208e927008f961dc5945e5aad352d0dfde8ee8`, bounding reads before parsing. These bytes match the tagged PDFBox source and identify Liberation Sans Regular **2.1.5**. Missing, oversized, changed or unreadable assets fail explicitly. The known classloader must expose exactly one candidate for that fixed resource name; a second candidate rejects before either is opened, with actual duplicate-resource and bounded-enumeration regressions. One job owns its in-memory font and closes it after the document. No document font name becomes a path, no default mapper is delegated to, and no host font/cache discovery is performed by the selected policy.

The mapper supplies an explicit fallback whenever PDFBox requests a non-embedded font. Such results remain partial with `font_substituted` and `font_coverage_unverified`; embedded-font handling remains in PDFBox and an embedded-only regression requires no fallback claim. This is **not a rendering policy or complete script-coverage promise**. Standard 14 AFM metrics remain available, while ToUnicode/encoding maps may recover text even when the fallback has no corresponding glyph. Substitution can affect spacing and layout. CJK/Arabic/dingbat sample glyphs are absent from this one font. Unsupported coverage must remain visible rather than being promoted to complete extraction.

Staging checks the bounded resource and required AFM/licence entries before producing ready markers, then retains the complete original JAR and its existing SHA-256 inventory binding. Only parser worker JARs include the adapter and its fixed exception class. Search has no font adapter or PDFBox dependency. The font's SIL OFL 1.1 copyright/reserved-name terms and Adobe AFM distribution notices remain in PDFBox's original `META-INF/LICENSE`; `META-INF/NOTICE` is also preserved. The [upstream component licences](https://github.com/apache/pdfbox/blob/3.0.8/pdfbox/src/main/appended-resources/META-INF/LICENSE) govern those assets, separately from original Apache-2.0 application code. No additional fonts are downloaded or advertised.

The [synthetic corpus](../../fixtures/parser-fonts/README.md) covers all Standard 14 faces, embedded text, unknown fonts/widths, custom Differences, ligatures, simple/CID ToUnicode and missing coverage. Both compiled corpus and embedded-only fixtures get independent, freshly launched positive controls and AppContainer runs before DOCX and Lucene. Typed fixture assertions require the font-policy identity, exact page counts, distinct per-page BEGIN/END sections, expected text and honest limitations. Simple-font and CID ToUnicode sections are checked separately; the unmapped-CID section must remain empty and the overall result partial. Windows transport also rejects the old PDF policy identity, a complete-PDF claim or unpaired fallback limitations. All original permissions/resource/cleanup tests remain required. No sandbox capability, ACL, deadline, memory or scratch budget changes.

The twelfth run, [36037364435](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36037364435), tested font-policy head `cda81856a1197706d68553c18292da4022040c6d` through checkout `b479ae6090a3f3ceb6fc80584c3ff3c43f2ddbff`. All **56 native Rust tests**, original AppContainer controls, **13 Java tests** and staging passed. The text/PDF/corpus/embedded cold controls and their confined counterparts passed full expected-text/status/policy checks. The 21-page corpus passed its separate page/Unicode/coverage assertions; the embedded-only case correctly had no fallback limitation. DOCX extraction also passed. These are actual extraction results within the unchanged deadline; they do not establish rendering or universal script coverage.

The overall run failed later at `parse_no-text.pdf` because the harness required a literally empty text string. The fixture is a **one-page text-free vector rectangle**, not an image/scan. Local reproduction with both the prior default mapper and the app-local mapper yielded one LF; an explicit Windows-line-separator development invocation yielded exactly CRLF. PDFBox 3.0.8 [initializes its page-end string from System.lineSeparator()](https://github.com/apache/pdfbox/blob/3.0.8/pdfbox/src/main/java/org/apache/pdfbox/text/PDFTextStripper.java). The failing native receipt did not retain that fixture's exact text, so the follow-up run must still establish the corrected expectation natively. Lucene and Java permission phases remained unreached. Artifact `10825244686` has ZIP SHA-256 `cbbbf76172d0c59736e92aff8a89ac0c85e68a03dd1c58709c508a7705827254`, report SHA-256 `df68c8008b86357667c4b104d6d7fa5562d303c677b75c38db35ba4166f60204`, and probe SHA-256 `fa5e438ecb31c8333a33af59dfaed92d44eaf0de16d1271a343cb593846cee98`. All twelve negative observations remain retained.

The thirteenth candidate repairs **only the synthetic no-text assertion**: exactly one Windows CRLF, one reported page, the selected parser identity, partial status, no error and the exact no-anchor/OCR-not-performed/embedded-excluded limitations. It rejects empty output, LF alone, extra separators, spaces/tabs, text, false complete status and wrong pages/limitations. A Java drawing-only regression checks the platform page separator. Extraction, font behavior, JVM arguments/line-separator settings and confinement/resource limits do not change.

Diagnostic capture cannot replace the original failure, make an unacknowledged termination appear quiescent or override cleanup failure. The ordinary API does not request these diagnostics. No scratch/profile/process/time budget, ACL or capability is increased. Local verification of the assertion follow-up passed **35 Rust tests**, host and Windows GNU target strict Clippy, **nine Java font/non-text regression methods**, **seven staging tests** under normal and optimized Python, formatting and diff checks. Java compiled with `--release 21` on the available JDK 25; the native Maven run will execute all **14 Java tests** on Java 21. An actual locally staged Mac parser JAR also extracted the embedded fixture with the property absent, preserving its old identity and no new fallback limitation; this is a linkage/default-path check, not a confinement test. The prepared Windows suite contains **57 Rust tests**. Follow-up native results remain pending; the existing failure remains authoritative. Cross-compilation and Java tests on the development Mac do not execute Windows confinement.


The thirteenth run, [36039346817](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36039346817), tested assertion-only head `2e41f6f227aff063a2e1192cf906d6c9d84f8051` through checkout `94985072cae278a9d7f75c6c580f81acf2287837`. All **57 native Rust tests**, **14 Java tests**, original AppContainer controls and staging passed. Every parser fixture passed, including the exact two-byte Windows CRLF on the text-free page, archive traversal rejection and explicit unsupported input. The overall campaign then failed at `lucene_index` with exit code 1 and final checkpoint `search_selected`; there was no JVM fatal header or thread sample. The scratch observation counted three entries and 17 file bytes, but does not identify the failed call. Lucene query and Java permission phases were not reached. Artifact `10826107135` has ZIP SHA-256 `873921529d896a8b09fa577878954cdab7838267083c7043e1bf554bd1b81d16`, report SHA-256 `671719f93e80e1610f3fa33c8612c5c3dedde76bc85eec12d81d07d97fcefe26`, and probe SHA-256 `2c922d5552a5fe27356e04eac93aebbab13332ad08b8fa1dcc6aa97ba56baaf4`. All thirteen failed attempts remain retained.

The fourteenth candidate adds **diagnostics only** for this distinct failure. The staged Lucene/Jackson dependency hashes match the locally built pins, and a synthetic local Mac/JDK 25 invocation with the selected classes/dependencies can index successfully; that is not a Windows control or confinement result. Lucene 10.5.1 [FSDirectory](https://github.com/apache/lucene/blob/releases/lucene/10.5.1/lucene/core/src/java/org/apache/lucene/store/FSDirectory.java) selects MMapDirectory and resolves the index's real path, while [NativeFSLockFactory](https://github.com/apache/lucene/blob/releases/lucene/10.5.1/lucene/core/src/java/org/apache/lucene/store/NativeFSLockFactory.java) creates and locks the write-lock file. No particular API or permission failure is inferred from exit code 1.

Under the existing probe property, eight fixed checkpoints now distinguish request/index validation, directory construction, manifest reading, writer construction and committed index output. A separate `java-failure.json` holds only one of `access_denied`, `missing_file`, `file_exists`, `filesystem`, `io`, `security` or `other`, using exact known exception types; unknown subclasses map to `other`. Messages, causes, class names and stack traces are never inspected or exported. The existing no-follow/single-link reader accepts at most **128 bytes**, after confirmed termination. Diagnostic absence, write failure or invalid/unknown content cannot override the original failure; tests reject extra fields, duplicate fields, trailing data, oversized files and hardlinks. An actual Java subprocess regression requires exit 1 with diagnostics absent, successfully written and deliberately unwritable.

A fixed synthetic index positive control now runs before confined indexing, using the same prepared recipe, selected search runtime and process/time/output budgets. It accepts only the compiled notice text, fixed identity and revision 7, then validates the exact acknowledgement and bounded, hash-checked snapshot. Both control and confined outcomes are retained; an unacknowledged termination or cleanup stops further launches, and a failed control cannot turn a confined output into a campaign pass. No caller-selected documents or unconfined application fallback are introduced. JVM arguments, FSDirectory choice, ACLs, capabilities and limits are unchanged. Local verification passed **36 Rust tests**, host and Windows-target strict Clippy, **three Java failure-diagnostic and three existing sample tests**, including the actual exit-preservation controls, and formatting/diff checks. The prepared Windows suite contains **59 Rust tests** and **17 Java tests**. Fourteenth native results remain pending; the thirteenth failure is authoritative.


The fourteenth run, [36041443185](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36041443185), tested diagnostic head `882185f54f70fd0d1bbff2c017e10a9813157d81` through checkout `611c7c56e33e8fd52c5b97a093c027fad8b03c2a`. All **59 native Rust tests**, **17 Java tests**, original AppContainer controls, staging and every parser fixture passed. The new unconfined index control also passed with a validated revision-7 snapshot. Confined indexing still exited 1 at `search_directory_started`, now with the closed category `access_denied`. Its scratch observation contained four entries and 54 file bytes, no fatal header and no thread sample. This locates the construction stage; it does not identify an exact denied ancestor or establish that any access grant should change. Query and later Java permission phases remain unreached. Artifact `10827425641` has ZIP SHA-256 `b2f6bef1920c4b7d526b2fe4eb193fb6f6f0bc9e05e1f4d4bf7effd86ce5c7b0`, report SHA-256 `8a7b0d308e0dac6f924532e7785e03d80ebaaec6d9f61c49080a69ac7734eb86`, and probe SHA-256 `ecc18eb9e23f8ab6ef4f58b38ce044a4d93f281a6d40e8b92b8e54157e8af906`. All fourteen failed attempts remain retained.

The fifteenth candidate adds just two fixed probe checkpoints around `index.toRealPath()` before the existing FSDirectory construction. The [selected Microsoft JDK 21.0.12.1 implementation](https://github.com/microsoft/openjdk-jdk21u/blob/release/jdk-21.0.12.1_1/src/java.base/windows/classes/sun/nio/fs/WindowsLinkSupport.java#L240) queries each ancestor with `FindFirstFile`; Lucene's FSDirectory superclass calls that API during construction. This is a source-backed hypothesis until the next matched native observations. The call and checkpoints are absent when the probe property is absent. There is no new path grant, FSDirectory selection, JVM flag, timeout, memory or output budget, or application fallback. The prior failure remains authoritative while this confirmation runs.


## Confirmed filesystem construction failure and explicit memory-directory candidate

The fifteenth campaign, [36043070355](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36043070355), tested head `2c9f56f34a76d37dce1cd223cbbe1d5edf3d3163` through checkout `347e2702a94c4955ec3ca7a7cad30189af998d86` (parents `4fe56471f72ff7c847b88cf8ea75fbc055d97a71` and that head). **Attempt 1** passed all 59 native Rust tests and the original confinement campaign, then stopped before Java compilation because Maven Central returned HTTP 403 for the existing `maven-resources-plugin:3.4.0` dependency. Java engine verification was `not_started`. Artifact `10827461797` remains retained with ZIP SHA-256 `57677bebc823e9e79984bfe5735461fd73e290dde30746545d18cab5fb75404b` and Java receipt SHA-256 `ec82999ee0e6d5673a21251bc8b37d303d1c80d0d1f65c363f2368b3d4955ed9`. No dependency, repository or code changes were made for that failure.

The explicitly authorized **attempt 2** reran the failed job at the same head and checkout. All 59 Rust tests, 17 Java tests, original confinement controls, staging and every parser fixture passed. The matched unconfined index control passed with validated snapshot/output SHA-256 `9cdf0fd747d0f42808c27f8364873d084112285f745651a184294b7faddf3174`. Confined indexing exited 1 at **`search_realpath_started`**, with `access_denied` and no `search_realpath_ready`. This confirms denial at the exact `Path.toRealPath()` call before FSDirectory construction; it does not identify the denied ancestor. Scratch held four entries and 53 bytes, without a fatal header or thread sample. Queries and the Java permission phase were unreached. Artifact `10827992638` remains retained with ZIP SHA-256 `ee6cb256865db436bb90871e8bb1a55c377d181271591a83237ee6677d5a6ba3`, report SHA-256 `e9bce39351ae91f5f6511205fa2d4344ad1137cea8f1b9e495dda9b14d66e77d` and probe SHA-256 `46e260ed62fa960b24c9d6d125a53fb16eccaf2d179cb69f2df59ab86c3f1cb5`. All fifteen campaigns and both distinct fifteenth attempts remain retained as negative observations.

The next candidate explicitly selects **`lucene-10.5.1-bytebuffers-v1`** in the fixed Windows Search-role recipe. It uses Lucene 10.5.1's public [ByteBuffersDirectory](https://github.com/apache/lucene/blob/releases/lucene/10.5.1/lucene/core/src/java/org/apache/lucene/store/ByteBuffersDirectory.java), wrapped through public [Directory](https://github.com/apache/lucene/blob/releases/lucene/10.5.1/lucene/core/src/java/org/apache/lucene/store/Directory.java) and [IndexOutput](https://github.com/apache/lucene/blob/releases/lucene/10.5.1/lucene/core/src/java/org/apache/lucene/store/IndexOutput.java) interfaces. It does not attempt FSDirectory first and does not fall back after an access failure. An unknown policy is rejected. The property-absent macOS/default entrypoint retains FSDirectory and its existing result/commit shape; the fixed parser recipe has neither this property nor the memory-directory classes or Lucene dependencies. Native permission observations additionally check that role separation and exact policy presence.

`CappedDirectory` reserves every write **before** delegating to Lucene's buffers. A private lock protects shared accounting across concurrent outputs. The limits are at most **128 live files**, **8 MiB per file** and **24 MiB live logical bytes**, including temporary and concurrent merge outputs. Rename preserves accounting, successful deletion reclaims it, open-output deletion/rename is rejected, and a failed write poisons the directory so it cannot later be acknowledged. The default many-buffer representation avoids whole-file coalescing. Query import seals the directory before opening a reader; output creation, temporary output, delete, rename, synchronization and write-lock acquisition then fail.

`FlatIndex` retains ordinary, flat Lucene segment files. Import/export use **64 KiB buffers**, bounded enumeration, portable names, case-insensitive collision rejection, no-follow ordinary files, exact lengths and EOF checks. Rust's existing pinned-handle/reparse/hardlink/stream checks remain authoritative around assigned paths and snapshot copying. Export requires an empty assigned index directory and creates each file with `CREATE_NEW`, verifies the final inventory and reimports the actual export to check its committed identity before emitting an acknowledgement. Partial exports are never acknowledged. Rust again validates the reply, bounded file inventory and SHA-256 hashes before accepting the owned snapshot; those same bytes are checked before subsequent query assignment.

The policy and workspace revision are bound in Lucene commit userdata, the index/search replies and the Rust snapshot. Missing, unknown, mismatched or legacy policy data is rejected; rebuilding is an explicit coordinator decision. Query input also identifies the expected revision/policy. There is no new serialized index format, filesystem provider, reflection, host-directory grant or privileged path-resolution bypass. The byte buffers use one per-job in-memory lock factory; snapshots remain rebuildable derivatives, with no ephemeral-memory durability claim.

**Capacity remains limited and unverified beyond this foundation.** Lucene documents ByteBuffersDirectory as heap-based and experimental and generally prefers filesystem mapping for larger indexes. The fixed policy pins its exact Lucene version. Logical bytes are not a heap guarantee: buffer blocks, deleted-but-still-open readers, Java objects, indexing/query state, JSON and export validation add overhead. The existing **256 MiB JVM heap**, **768 MiB Job memory**, 30-second deadline and all other launcher/ACL/network/output limits remain unchanged; exhaustion fails rather than raising those limits. No 10,000-page or performance claim follows from this implementation. Larger-scale application integration needs a separately reviewed capacity design.

Local candidate verification passes **37 host Rust tests**, strict host and Windows GNU-target Clippy, **30 Java regression methods** and **eight staging tests** under normal and optimized Python. Java was compiled with `--release 21` and executed on the development Mac's JDK 25; this does not replace actual Java 21/Windows verification. The 13 new Java methods cover exact/+1 production file/member/aggregate limits, simultaneous-output accounting, failure poisoning, sealing, ordinary filesystem-to-memory and memory-to-filesystem interoperability, all six query cases, corrupt/missing commit files, policy/revision rejection, `CREATE_NEW` preservation, bounded imports and actual FileWorker subprocesses with explicit/absent/unknown policies. Rust checks policy bindings and staging rejects missing helper classes while keeping them out of parser runtimes. The prepared native suite has **60 Rust tests** and **30 Java tests**. Peer review and the sixteenth native campaign remain pending; the fifteenth failure is still the latest actual engine outcome. Windows application adapters remain disabled.

## Sixteenth campaign: verified bounded development pass

[Native run 36047073221, attempt 1](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36047073221) tested reviewed head **`4387168a3199138fa3934f4e4a4355dac9418671`** in job `107793140605`. The actual executed checkout was **`34c238886a249b5799e8e5cac90d828d325f127e`**, with parents `eb91cbd622600b63aa1b716e2bf731c36aa46a1e` and that head, and tree `e3bef0afcdf9a8bb26fc393f38562a44450da688`. Both the runtime environment record and probe identity name that checkout. This evidence must not be relabelled as a test of later integration commits or base updates.

All **60 native Rust tests** (48 library and 12 probe-binary tests), **30 Java 21 tests**, eight staging tests, the original complete AppContainer campaign and the new complete Java campaign passed, with no skipped Rust/Java tests. The staged runtime identifies Java **21.0.12.1**. The Java receipt has `passed: true`, `failure: null`, `tests.phase: complete` and **`complete_release: false`**.

The retained predicates establish:

- All compiled parser fixtures passed their full text/status/policy assertions: text, notice PDF, 21-page font corpus, embedded-font PDF, DOCX, exact two-byte Windows CRLF for the text-free vector page, rejected archive traversal and explicit unsupported input. The five synthetic cold positive controls passed too.
- Confined indexing accepted policy `lucene-10.5.1-bytebuffers-v1`, revision 7, **four files / 3,165 bytes**. Boolean, phrase, proximity and fuzzy queries each returned three expected source hits; fielded search returned one; the no-results query returned zero. Each query used a separate read-only copied snapshot and its accepted result carried the expected revision.
- Both Parser and Search matched all **23 exact predicates** in before/confined/after maps. The confined workers could read assigned input/request and write scratch. They could not modify input/request/runtime, access another workspace/runtime or the sibling index, access the original, create a child process, read the caller's environment sentinel or connect to the tested loopback TCP listener. Search alone could read the assigned snapshot and could not write it. The memory helper and exact directory policy were present only for Search. These tests do not establish all possible network, DNS or IPC paths.
- The original foundation campaign passed its retained handle, directory-ACL, named-stream, timeout, cancellation, sampled memory/disk/handle, output and cleanup controls. Its wider-platform/installer/network limitations remain in [the foundation document](WINDOWS-WORKERS.md).

Artifact **`10829612263`** is retained with:

| Evidence | SHA-256 |
| --- | --- |
| Downloaded artifact ZIP | `45a7ddd1d2e7d15e93c8ed227cbc0b666a142e3df31f0b09285f82257f73252d` |
| Java engine report | `68d882041f1dc5450d6c469e9324b0f9e0cba53202aa0b50d524c63089fec07d` |
| Executed Java probe binary | `fc6d1c427ebc3d3631c6a4e93acc2ab7343151287fa03d6697185f08d685e563` |

The tested environment was Windows Server 2022 Datacenter `10.0.20348`, runner image `windows-2022` / `20260913.307.1`. [Source CI 36047073106](https://github.com/Venin-Client-Systems/entity-workbench/actions/runs/36047073106) also passed all four jobs: browser, Windows, Apple Silicon Mac and Intel Mac. Every source-job checkout log identifies the same `34c2388` merge. This does not extend the native engine evidence to macOS confinement or to newer source.

The raw job log, environment/runtime inventories, original foundation report, Java report, source commit/parents/tree and both probe identities are retained separately. The original fifteen campaign histories and the fifteenth repository-access failure remain unchanged. No retry, ACL, capability or resource increase was used for campaign sixteen.

The integration candidate starts from **`c585712a3a6e569f410de6c346efcb2c0295cd96`** and consolidates the reviewed PR53 range `c40892e433bc4b5196fb7db8e9dce1d2bb422aed..4387168a3199138fa3934f4e4a4355dac9418671`. The merge applied without source conflicts; Cargo.lock retains the current base's packages and adds only the windows-worker edge to already-locked `sha2`. The original published head and actual campaign checkout remain intact. The integration also adds both new directory helpers to the two existing Mac worker source-fingerprint lists, so later evidence cannot omit linked helper changes. No engine behavior differs from the tested candidate. This integration base is newer than the tested `eb91cbd` parent; combined affected verification is still required.

Activation remains a separate implementation/review step: connect fixed recipes to canonical processing jobs and the shared cancellation token; validate domain/source anchors and atomic result acceptance; manage durable, revision-labelled derivative snapshots and interrupted-job recovery; prove Windows-specific cancellation, corruption/rebuild and stale-result handling through actual integrated jobs; and validate fully staged installer/runtime inventories on clean Windows 11. Capacity beyond this bounded development snapshot needs measured design work. No core Windows parser/search/OCR route is enabled by this integration commit.

The work remains blocked from application use pending integration and its verification. Additional work includes the actual selected release runtime, Windows 11 downloaded-installer testing, coordinator integration and atomic workspace publication, durable recovery, cancellation/restart scenarios for real engine jobs, broader hostile documents, filesystem race stress, non-loopback/DNS/IPC coverage, signing and independent security review. This candidate does not change a release gate to passed.


## Canonical parser integration candidate — 25 September 2026

The fixed parser role is now connected to the Rust document coordinator; [the adapter contract](../processing/WINDOWS-PARSER.md) records its exact source/request checks and typed outcomes. The historical recipe campaigns above remain engine evidence. The new import/queue/extraction/recovery campaign has not yet executed, so canonical Windows behavior is not claimed verified. Search/OCR/render application routes, Windows 11 installation and signed distribution remain separate unpassed work. Trusted-runtime inventory I/O is classified at its narrow verification boundary as an unavailable prerequisite; cancellation, scratch/execution I/O, cleanup and unverified termination retain their distinct outcomes.
